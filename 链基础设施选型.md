# 链基础设施选型（阶段0/1结论）

日期：2026-02-11

本页用于回答：TMV（尤其是“元宇宙学院”MVP）应该选择 **自建私链**，还是走 **以太坊 + MegaETH（开源改造）**。

## 结论（先闭环，再升级）

- **阶段0/1（元宇宙学院MVP → 极简原型）**：优先选择 **Hardhat 本地链 / 单机EVM（必要时 Geth PoA 私网）**，以交付闭环为第一目标。
- **阶段2+（出现明确性能瓶颈后）**：再评估是否需要引入高性能 L2/专用栈（MegaETH 可作为候选之一），并以“可私有化部署范围 + 安全审计范围 + 全栈复杂度”做尽调。

核心理由：阶段0/1 的关键不是 TPS，而是把“学习 → 贡献 → 评测 → 激励”的系统闭环稳定跑通。

---

## 选项对比（面向 TMV 阶段0/1）

### 方案A：自建最小链（Hardhat / Geth PoA）

#### 优点（方案A）

- 交付最快：开发与演示成本最低。
- 可控性强：规则、数据、部署边界都在你掌控之内。
- 适配学院闭环：徽章（ERC-1155）、贡献记录（事件/合约）即可满足“可审计”。

#### 代价/风险（方案A）

- 对外生态弱：资产/流动性/第三方集成需要额外建设。
- 若走 PoS 私链（Geth+Prysm）运维复杂度更高；阶段0/1 通常不需要。

### 方案B：以太坊 + MegaETH（开源改造/私有化部署）

#### 基于公开仓库的初步观察

- MegaETH 组织有多个仓库（如 `mega-evm`、`stateless-validator` 等）。其中 `mega-evm` 仓库声明 **MIT OR Apache-2.0**。
- `stateless-validator` 的 README 明确区分“执行正确性验证（STF）”与“链的 canonical 性确定”，并提到在 OP-Stack 场景中 canonical 性通常依赖 `op-node` 等派生组件。

#### 优点（方案B）

- 理论上更贴近以太坊兼容生态，并可能在交互与吞吐上有优势。

#### 代价/风险（方案B，阶段0/1 不划算的原因）

- 全栈复杂度高：要搞清楚并搭建完整的派生/共识/数据可用性/节点角色分工；学习与排障成本显著高于“最小链”。
- 安全边界不确定：即使存在审计文件，也需要核验“审计覆盖的组件/版本/结论/限制”，否则不应按生产级依赖。
- 私有化改造成本高：你最终会把大量精力投入到链底层，而不是学院闭环与产品。

---

## 什么时候需要从方案A升级？（触发条件）

满足任一条再进入阶段2+ 选型评估：

- 学院闭环已稳定跑通，并且出现明确的 **写入频率/确认延迟/并发** 瓶颈，影响核心体验。
- 需要对外部生态产生强耦合（外部资产、流动性、跨链、第三方 dApp 互操作），且“自建最小链”边际成本过高。
- 团队具备持续投入链基础设施的能力（运维/安全/升级/监控/应急），并且愿意承担全栈复杂度。

---

## 阶段0/1建议落地方式（最小可行）

- 开发与演示：Hardhat 本地网络。
- 需要更接近“长期运行/多节点”时：切 Geth PoA 私网。
- 如需贴近合并后 PoS 私链（Geth + Prysm，含源码编译与联调排障）：见 [meta-recognition/以太坊私链部署.md](meta-recognition/%E4%BB%A5%E5%A4%AA%E5%9D%8A%E7%A7%81%E9%93%BE%E9%83%A8%E7%BD%B2.md)

---

## 下一步（如果要做 MegaETH 尽调）

- 核验“可私有化部署的最小闭环栈”包含哪些组件（sequencer/派生/op-node/验证/数据层）。
- 对关键仓库逐一确认：许可证、版本成熟度、审计报告的覆盖范围与限制。
- 以 PoC 的方式验证：与现有学院合约（ERC-1155/贡献记录）兼容性、开发者体验、监控与故障恢复。
