# 模型-额度盘点表（跨面跨班子）·P0 基线 v0.3

- sourceOfTruth: 本件（BOD-Ops 供数；规格=CFO 2026-09-15 23:00 需求清单）；频率：P0 一次性基线（P1 周度候启）
- 数据来源：settings 只读（遮罩尾 4 位）+ TriModel 卡遮罩 + 转录侧读数；未及项以「候补面」标注（凭据面=CEO/键持有人；配置面=CAO/CTO）
- 读时点：2026-09-15 22:0x-23:2x（date 现查）；落点裁定：BOD（CFO 委托 BOD/COS 定）→ `docs/execution/`

## 口径行（四要素标准，CFO 23:11 对表后定；此后两地读数可复算）

1. **目录范围**：`~/.claude/projects/**/*.jsonl`（席面会话转录全量）
2. **时间过滤基准**：**条目 timestamp**（非文件 mtime）；本基线取 ≥2026-09-05
3. **去重键**：精确四元 `(timestamp, input_tokens, cache_read_input_tokens, output_tokens)`；**dup 率逐语料实测、不跨语料复用**（本机 12.7% vs sg 0.1%，差异=谱系）
4. **模型族全 id 清单**：deepseek 族=5 id（`deepseek-flash`/`deepseek-v4-flash`/`deepseek-v4-flash-0731`/`deepseek-v4-flash-vision-exp`/`deepseek-v4-pro-0813`）；GLM 族=（`GLM-5.3-Flash`/`GLM-5.3`）——读数须按族合计，防单 id 低算

## ⚠ P0 最高悬问（CFO 23:02 件，A/B 两档）

**本机 GLM 会话记在哪个 bigmodel 账户/键上？**
- 现证：本机 GLM 键（TriModel 卡 `e-glm-anthropic` 遮罩尾 **`VRyY`**）≠ sg 键（尾 `qwH7`）——**两把不同钥匙**；**同账号与否未定**（一账号可多键）。
- **A 档（同账号）**：429 主贡献可能是本机（本机 GLM 近窗 87.8 亿 vs sg 全席 7.6 亿，~11×）→ 根因叙事与闸门范围全改写。
- **B 档（异账号）**：该账户另有独立配额风险（87.8 亿/10 日量级）。
- 定谳来源：控制台按键分账（凭据面）或 CEO 直答——**明日包第一问**。
- **收敛测试（CFO 23:11 设计，控制台数到即可跑）**：若同户 → 账户 5h 峰值应≈本机主导（本机峰值 7.2 亿 vs sg 1.53 亿）；用控制台窗值验算耗尽时点——**吻合=本机主因实锤；不吻合=另有消耗源**——把「同户与否」从观点变为可判定实验。

## A. M-SG（新加坡机）

| 项 | 值 |
|---|---|
| 1 模型 | GLM（bigmodel）系；席面经 `http://127.0.0.1:8460` 代理（H1.1 指纹缓解位） |
| 2 键归属 | bigmodel；键 len=49 尾 `qwH7`（遮罩） |
| 3 计费型 | 年订阅 coding plan（CEO 知；5h/7d 双窗） |
| 4 配额参数 | **候控制台**（429 报文「每周/每月」+重置 2026-09-16 20:44:33——与 5h/7d 双窗映射待对表） |
| 6 burn（去重复跑） | 全史合计 **7.59 亿**（去重后；dup 率 **0.1%**）；**峰值 5h=1.53 亿**（08-27 06:50-11:47 UTC，部署批日）；Top5 期 1.53/1.04/0.68/0.65/0.47 亿；maxEntry=31.5 万——证据 `docs/workflow/operating-records/2026-W38/sg-burn-curve-7d-20260915.md` |

## B. M-本地（本机）

| 项 | 值 |
|---|---|
| 1 模型 | **双线**：现势 deepseek（`deepseek-flash[1M]`，直连 `api.deepseek.com/anthropic`）；近窗历史 78% 为 GLM 族（CFO 实测；本席复核 09-05 起：GLM **87.8 亿** / deepseek **16.5 亿**） |
| 2 键归属 | **键载体两处**：①settings.json 现势 deepseek len=35 尾 `2782`；②**TriModel 卡**：glm 尾 `VRyY` + deepseek 尾 `c2e4`——三键并存，账户归属待控制台按键分账 |
| 3 计费型 | deepseek=按量 API；GLM 面=？（经代理/直连史待考） |
| 4 配额参数 | deepseek 无 429 观测；「无限」实限形态候 provider 面核 |
| 6 burn+现金（双方法交叉验证过） | 09-05 起去重：deepseek 族 **1.65B**（in=32.8M/cacheR=16.2 亿/out=4.3M）→ **现金 ≈¥132.40**（BOD 算，flash 价+峰谷）；CFO 复算（单 id 959M）= ¥64.46、族外推 ≈¥80-90——同量级互证 ✓。**现金=百元级/10 日；binding 约束=GLM 配额**；③现金上限设宽松+哨兵线（月现金 >¥1,000 回报）。可选优化（不入明日包）：本机缓存命中 73% 落高峰窗，批量作业挪空闲理论省 ~50% 现金 |

> B.6 口径注：价=官方 flash 系列 09-10 生效价（空闲 命中0.02/未命中1/出4，高峰×2 元每百万）；字段映射= cacheR→命中、in+cacheC→未命中、out→输出；峰谷按 +08 周一五 9-12/14-18；转录侧≠计费数；若含 v4-pro 档 tokens 则被低估（pro 价 ~6×）。本席 dup 实测 0.1% vs CFO 12.7%——**口径待对表**（窗口/文件集或键定义差异）。

## C. R-HY（河源 R 面）

| 项 | 值 |
|---|---|
| 1 模型 | **未验**：无 Claude 席配置（root/fleet 均无 `.claude/settings.json`）；TriRMC 生产配置未见模型凭据（无 .env；测试件有引用） |
| 2 键归属 | 未验 |
| 3 计费型 | 未验 |
| 备注 | 在跑 3 个 fleet node 进程（TriRMC 系）；若其 agent 端点生产态有模型调用 → 归配置面（CAO/CTO）深查 |

## 候补面（非 BOD 可读）

- **4 配额参数 + 7 凭证面**（控制台三数+单位+**按键分账**）→ 凭据面（CEO/键持有人）
- **8 变更通道**（改配置/换键负责人+留痕）→ 配置面（CAO/CTO）
- **deepseek 条款/上限核验** → 凭据面+CFO

## 悬置问题（CFO 问句更新版，随明日包）

① 5h 窗：配额值/已用量/重置规则；② 7d 窗：同项；③ 429「周/月」字样与 5h/7d 双窗的映射（含 09-16 20:44:33 重置点归属哪窗）；④ 窗配额单位与 token 换算；⑤ **本机 GLM 键（VRyY）与 sg 键（qwH7）是否同一账户**（P0 最高）。
