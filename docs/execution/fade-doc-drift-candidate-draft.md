# FADE 候补提案草案：文档漂移勘误域（模块文档自主维护）

## 文档同步元信息

- sourceOfTruth: TriMetaverse/docs/execution/fade-doc-drift-candidate-draft.md
- syncMode: source-only
- lastSyncedAt: 2026-09-06

**状态：候裁草案**（RDT 小吴起草，2026-09-06 CEO 定调扩域；本件不动真源不实施；新实例入册须 CPO/CTO/COS 三方常设联审裁，本件供其候裁）。体例先例：lg026-bl-session-body-draft.md。

---

## 一、提案缘起：四个真实样本（同一类漂移，四次人肉处理）

| # | 漂移 | 发现方式 | 处置方式 |
| --- | --- | --- | --- |
| 1 | manifest `adeLifecycle.closeCli: "pending"` vs CLI `--close` 已落地 | 培训勘验（旧深度教程记为细则 7(b) 活教材） | 周检盯防，正文未修 |
| 2 | project-source-document-sync-ade.md L23「生成 runId…待实现」vs runId/Close CLI 已落地（V1.1=2026-08-07 快照） | **CEO 亲读发现**（2026-09-06） | 勘误注记（TriCompany 5bf48b4，CTO 认可 5bf48b4 收口） |
| 3 | fade-002-deep-dive.md 行号锚 4344→4688 代差 | RDT 培训实勘（四版教程重制时） | 版本差注记+四版重制（0c54abd） |
| 4 | operating-records README 周指针仍指 W36——**自动化链自身的声明漂移**：迁移自动化覆盖目录翻页、未覆盖 README 翻页，SOP「人工必查」老坑 W35→W36 同款再证 | COS 迁移后人工巡出（2026-09-06 同日） | 人工翻页 e2aed21f |

**共性**：发现全靠人（亲读/对表/培训勘验/人工巡检），处理全是**账外手工**——没有 run、没有终态、没有可复算的发现记录。细则 7(b) 周检漂移核对**有纪律、无生命周期**：它要求周检做"声明载体 vs 实际载体"核对，但核对出漂移之后，勘误本身不走任何 FADE 流程。样本 4 尤其值得注意：**漂移者可以是自动化链本身**（迁移自动化与其 SOP 声明面之间的覆盖差），且同坑复发两次——靠人记"人工必查"清单，正是这套候选域要卸掉的负载。

## 二、设计要点（CEO 定调 2026-09-06：扩为模块级自主维护，不局限字样扫描）

一句话愿景：**全员模块负责人 agent 自主维护本模块代码文档——文档漂移从"人读人修"变"机器发现+负责人自愈"。**

| 段 | 设计 | 分权属性 |
| --- | --- | --- |
| 事件触发 | **cron 每日全模块代码扫描**（程序触发）：按模块持**扫描水位**（last-scanned commit，持久化工件），检出代码修改即开 run | 确定性 |
| 路由 | 按模块路由**拉起模块负责人 agent**（TriCompany→小赛；TriMetaverse→CodeRegistry 域；类推全员）——接线 spec §8.6 检测即触发 + M-004 直达派工 | 确定性路由 |
| Qualify | 负责人 agent 依代码修改**资格审查：判定漂移程度**——改动是否触及对外契约面（命令族/路径/schema/行为/成熟度声明），决定文档是否需要更新、更新哪些件 | **语义**（Agent） |
| Plan | 起草更新/勘误方案：注记 vs 正文（历史冻结口径）、目标件清单、验收判据 | 语义（Agent） |
| DCE | 文档落盘+commit（确定性可 CLI 化）；**发布下游复用 FADE-002 管线，不重复建设** | 确定性 |
| Score/Close | 域 owner 裁决（技术文档→CTO 域；纪律册→CAO 域；培训件→RDT 自认）+ 终态审计 | 语义+CLI |
| 登记 | 运行标识=模块+日期锚；去重=同扫描水位不重扫；恢复锚=水位工件+run 审计 | 四不变量 |

**关键设计判断**：Qualify 的一半其实**机械可判**——「X 待实现」类声明可对 CLI 参数清单/函数存在性做确定性断言；只有断言不了的才升语义段。漂移程度可分词表（无漂移 / 注记级 / 正文级 / 结构级），机械门先筛、语义门精判，避免"每次代码提交都惊动文档流程"的误报灾难。

## 三、与既有件的关系（防重复建设）

- **FADE-002**：管源→发布 copy 同步（字节/元信息级）；本域管**声明-现实语义对表**。上下游关系：本域改真源，002 管发布，零重叠。
- **细则 7(b) 周检漂移核对**：本域=它的**机器化与生命周期化**；周检保留人工抽验位（防扫描器自身漂移）。
- **FADE-006**：触发机制同族（cron/hook→tick→编排拉起角色），但 006 拾取**计划任务**，本域拾取**漂移**——同骨架不同载荷。
- **小赛维护 Agent**（§8.6 检测即触发，已立法未接线）：本域=该立法的**第一个具体域实例载体**，接线即还债 automation-backlog 的"发现自动化"半边。

## 四、待裁点（联审必答）

a. **触发面归属**：TriRMC/TriMMC cron 新 job vs 挂 patrol 同窗；扫描水位工件落点（各模块仓 `.fade/`？registry？）。
b. **模块-负责人映射真源**：D-13 名址表扩展 vs registry 新键（CHO/CTO 域裁）。
c. **Qualify 判据分级**：机械存在性断言的覆盖面清单；漂移程度词表定稿。
d. **与 automation-backlog 的关系**：本域触发面是否吸收"文件/Git 事件自动触发"旧债（CTO 2026-08-21 裁决项）。
e. **档位路线**：候补观察起步（两次周检窗口攒实跑样本）vs 直接完整实例提案。

## 五、拟入册形态（登记册候补区条目草案）

> ## 候补：文档漂移勘误域（doc-drift remediation，模块文档自主维护）
> 触发=cron 每日模块级代码扫描（水位持久化）；Qualify=模块负责人 agent 判漂移程度（机械断言+语义精判）；DCE=文档勘误落盘（发布走 FADE-002 下游）；Close=域 owner 裁决+终态审计。候补理由：四真实样本在卷（含自动化链自身漂移一例，见提案草案 §一）；§8.6 检测即触发的首个域实例；细则 7(b) 的生命周期化。入册前置：三方联审过待裁点 a-e + 两次周检窗口实跑样本。

## 使用依据

- 样本锚：TriCompany 5bf48b4（勘误注记）/manifest `adeLifecycle`/fade-002-deep-dive.md 头部版本差注记/ TriCompany 3b380a3（D 系教训入册）
- 协议依据：fade-protocol-spec.md §2.8 细则 7(b)（周检漂移核对）/§8.6（检测即触发）/§六 未收编候选表/头部三方联审条款
- 定调来源：CEO 2026-09-06 亲笔扩域指令（触发扩为模块级 cron 扫描→拉起负责人 agent→Qualify 漂移定级→Plan）
- 体例先例：lg026-bl-session-body-draft.md（候裁草案落 docs/execution/，CHO 预审过门流程）
