LG-033 M 面值班双位体系·CTO 技术面主审意见（对提案 00272b33，候周一 P4 后窗联审）

- sourceOfTruth: TriMetaverse/docs/execution/lg033-cto-tech-review.txt
- syncMode: draft｜lastSyncedAt: 2026-09-07

【技术面四视角】
1. **服务器 claude 会话环境形态**：值班位=sg Linux ECS headless claude session 常开——资源面可容（轻量 Node 进程，sg 现役负载+值班 session 同机无冲突）；环境原语四件=claude CLI Linux 安装形态+GLM auth（Wave 0 密钥治理；GLM 部署点位三端参照）+蓄水池工作仓 clone+node env 对齐。**裁点 4 采纳立法化**：核查清单作 D-15 v2 附表入册，逐项实勘后批签（P1 与裁点 4 合并执行——实勘即 P1 前置）。
2. **机械 watcher 实装位：双 daemon 实装（修正单侧表述）**——蓄水池正身在 sg（git 三端同步磁盘真源），本地可见=git 同步副本——故：①**TriRLC（8711 在役）=交互位唤醒器**（watch 蓄水池本地副本 mtime+hash，事件唤醒本地 COS）；②**sg 侧 daemon（TriMMC 或 sg 工作仓 daemon）=值班位侧**（watch 蓄水池正身）；③TriMLC（8713 候 cmd 重挂+重注册）=第三位可选非首实装。监视形态=mtime+hash 双锚零 token ✓；**git 同步延迟=交接延迟上界**（轮询周期定标候联审定量）。
3. **re-baseline 触发与防环**：条件式（CAO 勘定纪律正解）+靶向（变更分区）+单向推（值班位写盘推/交互位自写不推）=**防环充分**；加固建议一条：re-baseline 执行前**分区 hash 与上次 baseline 比对**（同则跳过）——机械判据零 token 双保险，防「推消息丢失致漏刷」反向缺口。
4. **分期可行性**：P1 轻环境件今天启动可行；P2/P3 依赖蓄水池写分区+watcher 实装（工序合理）；P4' 一周观察=稳。**关键依赖=P1 与裁点 4 实勘清单先行**（环境原语逐项过，P1 才是「环境就绪」而非「装完即就绪」）。

【四裁点意见】
1. 服务器选址 **sg 采纳**（M 面工作仓已在+TriMLC/TriMMC 同侧口径一致）；
2. GLM key 治理 **Wave 0 采纳**（方案明文禁现值纪律已有 cmd 重建方案同款先例）；
3. 轮换触发者：**事件驱动为主+时钟兜底混合**——纯时钟丢上下文语义、纯事件有挂起风险（事件源丢失=永不轮换）；混合=状态事件（交互密集/值班自动化判定）触发+时钟兜底（N 小时无事件强制评估），判定语义归 COS（裁点三选项之「COS 判断」正解）；
4. D-15 核查清单立法化采纳（附表入册+实勘前置批签）。

【风险注记一条】蓄水池「git 三端同步」的**同步机制本身**（push/pull 频率与冲突处置）为本域前置依赖——LG-032 案 a 三线拓扑经验（本机≫sg=GitHub 分叉）直用：蓄水池写入端多（值班位/交互位/watcher）须**单写分区纪律**（§三写分区已立）+同步冲突处置预案（union 归账纪律 FADE-001 版复用）——候联审补「蓄水池同步 runbook」一条。

【依据】提案 00272b33 全文+LG-032 案 a 三线拓扑勘定+cmd 重挂降卫生项裁+GLM 部署点位图。