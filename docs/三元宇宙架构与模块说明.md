# 三元宇宙架构与模块说明

版本：V0.2
日期：2026-04-23
状态：当前阶段说明

## 文档同步元信息

- sourceOfTruth: TriMetaverse/docs/三元宇宙架构与模块说明.md
- publishedFrom: 当前文件（source）
- syncMode: source-only
- publishTier: source-only
- lastSyncedAt: 2026-06-03

当前文件是 TriMetaverse 项目级架构与模块说明的本地真源，用于维护 `reference` 吸收链、模块边界与中央项目级说明；它不是 TriCompany 公司级 workflow 或产品真源。

## 1. 文档定位

本文用于承接 TriMetaverse 项目级的架构说明、模块说明，以及 `reference` 层与开源吸收链的统一口径。

这里记录的是项目整体架构与模块边界层面的说明，不是某个具体代码仓、运行时模块或实现细节的开发规则。

当前该部分内容先由总助协助收口到项目文档层；待 `CTO` 正式上岗后，再交由 `CTO` 作为技术真源 owner 持续维护。

## 2. TriMetaverse reference说明

- 任何开源代码吸收，必须先从 TriMetaverse 整体架构、模块职责和长期边界判断开始；先确定它服务哪个模块、解决哪一层问题，再决定是否进入吸收链。
- 上游源码必须先统一放到 `TriMetaverse/reference/`，作为 reference 层；reference 只用于阅读、对照、拆解、留档和版本锚定，不得直接当作本地运行时真源。
- 一旦确认某个开源项目需要被 TriMetaverse 长期融合为独立模块，必须创建与其他主模块同级的模块目录；不得把 `reference/` 目录直接冒充模块本体。
- 该模块内必须建立 `vendor/` 目录，作为上游代码的冻结基线和渐进式吸收层；vendor 默认优先保持上游原貌，用于从最小版跑通到后续完整复刻之间的持续参考。
- 开源吸收链统一写为：`TriMetaverse/reference -> 模块/vendor -> 模块真实实现`；不得跳过 reference 直接散改上游代码，也不得把 vendor 误写成自研主干。
- 当前阶段必须先跑通最小版闭环，再决定是否继续扩展到更完整的复刻；能通过 wrapper、adapter、配置覆盖和最小 patch 复用上游的，不先重写平替。
- 吸收上游代码时，优先复用其成熟、稳定、已被验证的优势能力；对与现有体系重复、弱于现有实现、或不服务当前阶段目标的功能，明确列为排除项，不必复刻。
- 不得为了“看起来完整”而复制上游全部能力；凡是不属于当前模块职责、不符合当前阶段最小闭环、或会造成重复功能的部分，直接砍掉或后置，避免双份实现和边界混乱。
- 项目级目标不是机械复刻上游，而是形成符合 TriMetaverse 总体架构的最优解：弱势功能砍掉，优势功能吸收，重复能力合并，模块边界保持清晰。
- 如开源吸收动作会引入新的长期主模块或改变既有模块边界，必须先咨询 `BusinessStrategy`；在中央真源口径更新前，不得把边界变化写成既成事实。
- 架构表中的模块一旦被写成正式模块面，默认应补齐独立 git 仓、`README.md`、`docs/` 六件套、根级 `.gitignore` 与本地 `CodeGraph`；占位模块也应先补齐这套骨架，再继续标注为待初始化。
- 任务命中既有正式模块时，`Discovery` 阶段应先形成 `ModuleTargetingReport`，并由 `TriDev` 完成 `ModuleReadinessInit`（标配审计与缺口 init）后再进入后续开发。
- 新增正式模块时，`Discovery` 阶段应先由 TriCompany 输出 `NewModuleBaselineRelease`（含 `vendor-extraction-profile`），经签核后由 `TriDev init` 执行模块骨架初始化；未达到 `approved` 前不得写成既成模块。

## 3. 当前阶段运行与宿主边界

- “赛博公司”是当前所有人格 Agent 与非人格 Agent 的经营和交互核心载体，不再把经营自治单独命名为第三个基础设施主控；赛博公司实施侧规则先由 `TriCompany` source 侧维护，再按发布规则同步到中央摘要层。
- `TriMC` 是当前统一的 agent runtime 与 interaction core，负责运行时、planner、context 整理、tools 接入、模型调用以及相关交互切片。
- `TriMC` 当前以 `TriMC/vendor/openclaw/` 中的 OpenClaw 核心作为 shadow 基线，正在吸收其心跳、cron / 定时任务、agent harness 与服务器端主控设计；后续再按真实集成进度吸收 Hermes、Claude Code harness 和其他 agents 集群调配优势能力。
- shadow 与正式接管当前都直接运行在 `copilot` 宿主上；正式切换通过 `TriModel` 的 Provider/Model 配置实现，而不是把 `Tride` 写成正式宿主。
- `TriModel` 负责 Provider 与 Model 的统一配置，为 `TriMC`（公司级 agentic orchestration）与 `Tride`（本地编码 agentic orchestration）提供统一的模型接入点；支持多 provider 适配、模型路由与 fallback 链；在真实实现落地前，应明确标注为待初始化或待接入。
- `TriPilot`、`Tride`、CLI（如 `opencode`、`claude code`、`codex`）与 `vscodium` 共同组成 PC 端软件层；其中 `Tride` 是 PC 端调用 vibe coding 工具的适配层，不再承担正式宿主语义。
- PC 端软件层一方面配合 `TriLC` 完成本地化任务、本地工具调用与部分服务域下发任务，另一方面也保留给用户直接使用的桌面自动化、PC 软件自动化与 `vibe coding` 入口语义。
- `TriSkill` 是未来的统一 skill 提供模块；在真实代码、README 或结构文档落地前，只能标注为待初始化，不能写成现役能力。

## 4. 模块主旨与期望能力基线

本节是“模块摸底 / registry 收口 / 小白教程化梳理”的中央主旨真源。后续任何模块级扫描，都必须先把真实代码、真实文档与本节的“功能主旨 / 期望能力 / 当前状态提醒”做对照，再写 gap、风险和后续计划。

如某模块真实职责准备发生长期变化，必须先更新中央 `BusinessStrategy` 口径，再改模块级 registry；不得先在模块里把边界变化写成既成事实。

| 模块 | 功能主旨 | 期望能力 / 关键职责 | 当前状态与边界提醒 |
| --- | --- | --- | --- |
| `TriMetaverse` | 中央战略仓与项目级真源约束 | 维护总商业模式、阶段门禁、workflow、中央 registry、reference 吸收链规则，并承接赛博公司实施落地侧的中央发布摘要 | 不直接承担各模块业务实现；赛博公司实施规则先由 `TriCompany` source 侧决定 |
| `TriCompany` | 赛博公司研发仓与经营编排孵化仓 | 沉淀产品/工程/registry/workflow/execution/training 六层文档、岗位对象、Hermes 记忆系统吸收、当前阶段 Copilot 宿主资产，并负责赛博公司的流程、员工参与、门禁资料组织与书面核签；对开发型产品，`TriCompany IPD` 作为公司侧参与 / 协同 / 放行层挂接到 `TriDev` 的项目级十阶段主线 | 试运行宿主资产承载中，不等于已成为正式主控宿主；TriCompany 控制员工参与与公司流程，不直接取代十阶段 phase engine |
| `TriMC` | 统一 agent runtime 与 interaction core | 负责 runtime、planner、context 整理、tools 编排、模型调用、服务域执行与研发工作流运行切片；吸收 OpenClaw 心跳 / cron / harness 与 Claude Code harness 设计，形成服务器端 agent 集群主控 | 当前 shadow 基线在 `TriMC/vendor/openclaw/`；模型接入通过 `TriModel` 统一配置，不能提前写成完整生产主控 |
| `TriLC` | 本地域控制器 | 作为配合 `TriMC` 调度龙虾 / Hermes / 其他 agents 的本地适配层，负责 detached local runtime、本地节点升级、planner、tool bus、本地执行生命周期 | 面向本地域执行，不替代服务域主控 |
| `TriPilot` | VS Code 扩展与 webview 用户入口 | 负责桌面聊天入口、本地域工具入口、本地控制与显示入口；通过 `TriMC` 与 `TriStaciss` 接通可用模型与 agent 集群调度 | 当前物理路径为 `TriPilot/`；只维护模块事实，不重写总体战略 |
| `Tride` | PC 端 vibe coding 工具适配层 | 接入 `opencode`、`claude code`、`codex` 等开发能力，承接 runtime/CLI/SDK 与 agentic orchestration | 不再承担正式宿主语义 |
| `vscodium` | IDE 宿主基础设施 | 承接 `TriPilot` 配套宿主、上游同步、桌面开发工作台与本地自动化环境 | 必须区分上游能力与本地定制 |
| `TriModel` | Provider/Model 统一配置层 | 负责多 provider 适配、模型路由、fallback 链配置，为 `TriMC` 与 `Tride` 两个 orchestration 提供统一的模型接入点 | 当前待初始化 / 待接入 |
| `TriSkill` | 统一 skill 提供模块 | 负责未来统一 skill 封装、分发与跨宿主供给 | 当前待初始化，不能写成现役能力 |
| `TriStaciss` | 模型路由中转站与 API 调用平台 | 负责多提供商适配、模型路由、接口转换、官方 SDK 能力边界适配与 API 后端配合 | 当前物理路径为 `TriStaciss/`；前端登录、聊天、仪表盘与群聊体验归 `TriAvatar` 产品侧承接 |
| `TriAvatar` | Web 入口、数字宠物、赛博分身与赛博任务模块 | 负责 Web 端聊天入口、群聊体验、未来数字宠物 / 赛博分身 / 赛博任务，以及近期浏览器插件形态 | 当前物理路径为 `TriAvatar/`；必须区分已实现 Web 能力与未来 3D / 4D / 游戏规划 |
| `TriDeployment` | 部署能力历史兼容占位 | 历史上承载自动部署与交付完备性讨论；当前 canonical 口径已把 Deployment 实现并入 `TriDev`，未来优先收敛为 `deployer-xxx` 员工 adapter | 物理路径 `TriDeployment/` 仍在，当前视为待迁移 / 待归档兼容仓；不得继续写成长期独立执行主模块 |
| `TriTest` | 测试能力历史兼容占位 | 历史上承载系统级测试与安全测试讨论；当前 canonical 口径已把 Verify / Redteam / QA / Assurance 实现并入 `TriDev`，未来优先收敛为 `tester-xxx` 员工 adapter | 物理路径 `TriTest/` 仍在，当前视为待迁移 / 待归档兼容仓；不得继续写成长期独立执行主模块 |
| `TriDev` | 开发型项目十阶段流程与执行模块 | 承接开发型项目的 `DISCOVERY -> INTELLIGENCE -> DESIGNING -> CODING -> VERIFY-INTEGRATION -> REDTEAM -> QA -> DEPLOYMENT -> ASSURANCE -> DELIVERY` 主线，沉淀 phase engine、PRD 分叉、版本号签发、产物归档、阶段 gate 和 evidence；当前也承接 Deployment / Assurance / Delivery 的实现能力，其中 `QA` 是 release readiness gate | `super-dev` reference -> vendor 吸收链已启动；当前 source-side 已开始把 ten-phase runtime 与 phase contract 一比一落下，并把 `businessOwner / actingOwner / moduleExecutor / gateOwner` 挂到各阶段，但 PRD 分叉并行、多分支 delivery 聚合、独立 package schema 族和完整岗位 adapter 仍未完成，也不要把 phase engine 和 TriCompany 的公司员工流程混写 |
| `TriGateway` | 社交通道连接与消息队列管理模块 | 服务于各社交通道连接、消息排队管理和未来网关真源落位 | 规范目录已修正为 `TriGateway/`；为平滑迁移，历史路径 `TriGatway/` 暂作为兼容别名保留；在 README / AGENTS / 实现出现前，具体职责仍待中央补定 |
| `TriMobile` | 本地域移动端入口 | 负责移动端触达、本地域移动体验与未来和 `TriAvatar` 协作的入口形态 | 当前占位，能力均应标为待初始化 |
| `TriMem` | 用户系统模块 | 负责用户体系、身份绑定、区块链身份关联与数据库设计 | 当前占位，禁止虚构 schema 或身份流程 |
| `TriWeb4` | Web3 / Web4 模块 | 负责钱包、合约交互与 Web3 / Web4 能力 | 当前占位，禁止虚构协议栈与进度 |
| `TriChain` | 公链模块 | 负责链上执行、公链能力与链上集成 | 当前占位，禁止虚构链型、合约部署状态或进度 |
| `TriTraining` | 培训学院功能主承载模块 | 负责课程组织、lesson/lab contract、课程图谱、训练运行规则，以及与 `TriAvatar` / `TriStaciss` 的协作接口 | 当前处于最小模块基线初始化阶段；产品 / 技术边界待 CPO / CTO 联审确认 |

当前阶段涉及 `TriCompany` 源仓、`TriCompany-copilot-host-assets` 支撑包、`TriMetaverse/.github` live 宿主入口与 `TriMetaverse/docs` 中央摘要层之间的治理关系，统一参见 `docs/workflow/tricompany-copilot-host-assets-governance.md`。
若只是定位当前阶段的模块事实、published-copy 规则或 host operator 动作，默认先回 `TriCompany/` 真源、`TriCompany/.github/manifests/tricompany-published-copy-manifest.json` 与治理页；只有在确实需要当前宿主专用 runbook、phase 证据或 support-object-set 时，才补看 `TriCompany-copilot-host-assets/**`。

## 5. 命名与别名治理

- 中央 registry、计划文档、教程文档统一使用 `TriGateway` 作为规范化模块名称。
- 物理目录已修正为 `TriGateway/`；为保证已有 workspace、脚本和外部引用平滑迁移，历史 typo 路径 `TriGatway/` 暂作为兼容别名保留。
- 后续自动扫描、批处理 prompt、报告聚合和教程生成都必须维护 `TriGateway <-> TriGatway` 的 alias 映射，避免把两个名字误判成两个模块。
- 产品 canonical name 统一采用 `TriPilot`、`TriStaciss`、`TriAvatar`；`TriDeployment` 当前仅保留为历史兼容路径名，不再作为长期 canonical 产品模块。
- 模块摸底与教程化实施计划见 `docs/workflow/module-registry-baseline-rollout-plan.md`。
