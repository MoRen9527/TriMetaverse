# 任务书 TASK-TRIMODEL-RHY-DEPLOY-01：TriModel 部署 R-HY（部署波·服务域正身位）

- **授号**: LG-054（候 COS 核号防撞照流程确认）
- **发令**：CEO 2026-09-26 16:08 三件裁决之二「2、现在部署」（date 现查 16:08 星期六）；BOD 落任务书
- **承接**：SDE 主办（部署执行）＋CTO 技术门＋STE 验收；经 COS 流转 COO 拆派
- **face**: R 面（河源机 R-HY，8.155.54.79）＋跨机通路勘验（本机/sg→R-HY）
- **执行窗**：CEO 令「现在部署」——立即开窗，方案面已批免再批

## 一、方案正身（免重审）

部署三问方案=**joint-plan.md 双签终版问5/6/7**（CPO 3d893e02＋CTO c130c6c0→终版 24ba1ccc，CEO 09-25 22:57 批全案），本任务书只落执行拆派，方案面零重开。要点摘录：

- **问5 部署形态**：Node 服务 systemd unit（Restart=always）——systemd 即 R-HY 的 L1；端口 3333、`TRIMODEL_HOST=0.0.0.0`；数据目录（policies/卡/预设）集中 R-HY 位；下发通道=**拉取制**（两面 daemon 改 `TRIMODEL_API_URL` 即切，拉取失败不阻塞本地运行）；**全公司配置真源唯一=R-HY 位，machine 四象限分区隔离**。
- **问6 网络安全**：公网鉴权三件加固（TLS 终端 Caddy 反代建议位／ADMIN 写面独立强 token＋限流／安全组最小开位——443/反代口开、3333 直口不开公网）；token 生命周期 UX 三条（生成/轮换/吊销命令化、全值只展示一次、**轮换双 token 并行窗**——违反即 FREEZE）；通路勘验=执行窗首项（本机→R-HY、sg→R-HY 双通路 curl 三态实测留痕）。
- **问7 恢复梯立体化**：服务域梯=systemd 重启→各面 daemon key-cache 拉取连续失败 N 次→各面独立降级本地直连（钥源=各域自己的 .deploy-key）→toast→人工；**本地直连=唯一本地恢复锚为永久红线**。

## 二、本单范围=M1（里程碑制，M2/M3 不在本单）

- **M1（本单执行）**：R-HY 部署＋四象限配置迁移＋通路三态验绿＋公网鉴权三件加固＋token 生命周期三命令可用；
- **M2（候排不在本单）**：两面 daemon 全改指 R-HY＋观察周（拉取成功率/降级误触发率读数）；
- **M3（候排不在本单）**：本机 3333 停用（watchdog Disabled＋启动项撤＋演练副本清理）。
- 与 LG-053 波⑤（D1 回头测）**并行互不阻塞**（BOD 15:5x 裁决令勘正版）。

## 三、执行序（首两项为前置勘验）

1. **R-HY 基座勘验**：TriCode 同机在位断言（技术债⑩在册——部署态依赖）＋Node 版本/端口占用/systemd 可用性实勘，读数留痕；
2. **通路勘验**（活体优先诊断法）：本机→R-HY、sg→R-HY 双通路 curl 三态（TCP/TLS/带 token 200）实测留痕；**涉 sg 侧走 BOD 通道**（D-24：BOD 亲勘 SSH sg-server 或 m-duty-cos 值席留痕制）；
3. 部署落地：代码位→构建→systemd unit→健康端点活体断言；
4. 四象限配置迁移＋真源唯一=R-HY 对表（sg 若存在 TriModel 实例归并行实例收敛清单候窗对表）；
5. 公网鉴权三件＋token 生命周期三命令（全值零回显纪律沿用：len+头4尾4 掩码）。

## 四、边界

- **R-HY 生产冻结面不因本令解冻**（CEO 09-25 令边界延续）：部署动作清单逐项对表「只触 TriModel 部署位」，越位即停（方案风险节3 照守）；
- 跨机操作守机位断言纪律（D-24）：R-HY 活走 R 面通道，sg 勘验走 BOD 通道，禁跨面中转；
- 活体 settings.json 事故案补丁门全程（本单涉配置写面时）；测试隔离+值面验证+备份锚；
- token 轮换自锁=硬判据（双并行窗，违反即 FREEZE）；
- 五防线继承语义不重开（LG-053 已验基线）；停工冻结族（TC 88a6988 等）不因本令解冻；
- 禁越波：M2/M3 动作（daemon 改指/本机退役）候 BOD 另单。

## 五、验收锚（BOD 哨位读数）

- **A1** R-HY 活体三态：systemd unit 拉起＋Restart=always 在位＋健康端点 200；
- **A2** 通路三态验绿：本机→R-HY＋sg→R-HY 双通路 curl 三态留痕（TCP/TLS/带 token）；
- **A3** 四象限配置迁移毕＋真源唯一=R-HY 对表读数；
- **A4** 公网面三件加固毕＋**3333 直口公网不可达断言**；
- **A5** token 生命周期三命令可用＋双 token 并行窗实弹读数（全值零回显）；
- 部署回滚锚：部署前快照＋回滚步骤成文（systemd stop/disable＋数据目录锚）。

## 六、回报链

- 执行面回报经 COO 汇 COS 合账回 BOD 哨；里程碑读数随到随转；
- 涉 sg 勘验读数由 BOD 亲勘回填或 m-duty-cos 留痕回执；
- 本单收口=M1 五锚全绿＋BOD 验收毕，候 CEO 知悉；M2/M3 排窗另呈。
