# CTO 定谳·O-2 写后断言域（TASK-INCIDENT-SDE-SETTINGS-01 候裁项）

- sourceOfTruth: 本件（O-2 定谳树内正身；已直回 COO 转 BOD 备案+FSD 小单）
- syncMode: final
- lastSyncedAt: 2026-09-25 11:3x +0800（date 现查回合链）
- 实勘: TriCompany f887b27 `restore-claude-config.ps1` L325-343（写后断言）+L244-251（修-1 预设侧健康门）+ 现役活体 settings.json 凭据键形态现勘（len-only）

## 裁决

**裁「排除空串残留」——写后断言域收窄到「本次写入键」（预设凭据键），非维持全族严格。**

## 决定性实证（现勘）

现役活体（CEO 2026-09-25 04:09 手工恢复形）：`ANTHROPIC_AUTH_TOKEN` len=49（有效钥）+ `ANTHROPIC_API_KEY` **空串残留**——该形态恢复后持续正常工作全天（CTO 会话即活证）。当前全族断言（L340-343 凭据族键存在即空→fail+自动回滚）会判死此形态：direct 切换（keyShape=AUTH_TOKEN 单键制预设无 API_KEY 键）覆写后活体残留 `API_KEY=""` →断言失败→自动回滚——**部署日主路径必触发阻断**，紧要目标「3333 挂时一条命令内完成」被断言自伤。O-2 系主路径缺陷非边角案例。

## 理由四条

1. **语义锚正位**：写后断言目的=拦截事故形态（本次切换把有效凭据打空/值不符/半写），非文件面全族洁净度。Claude Code 认证面 fallback 语义（AUTH_TOKEN 优先、空/缺回落 API_KEY、同值消解）下，非写入键的空串残留非故障态；
2. **键形历史漂移实证**：急救夜 L0 冻结件=API_KEY 制（SDE 转录 1360 锚）vs 现役恢复形=AUTH_TOKEN 制——键制漂移系历史事实，空残留=漂移遗迹，全族断言=把遗迹当故障；
3. **事故形态零漏**：收窄后仍全覆盖事故链——①预设凭据键写后非空+与预设值一致（键名错位/空串穿透全拦）②预设全键写后值一致（覆写完整性）③JSON 合法（半写拦截）；
4. **预设侧健康门不联动**：修-1（L244-251）维持全族严格正确——预设系受控产物，有空凭据键=生成缺陷应拦。O-2 只动活体回读侧。

## FSD 小单实施指引（30 分钟级，18:00 挂案前收口）

- L340-343 全族循环→收窄为「预设 env 中凭据族键」断言（非空+与预设一致）；
- 预设外凭据键：空串→RESULT 行 `warn=stale-empty-key:<键名>`（信息性不阻断）；非空→不动不断言；
- `-SelfTestBreakAssert` 钩子语义保留；
- README 部署序删「遇残留须先清理」前置（若已写入），改「残留空键 RESULT 行 warn 提示，择机清理非必须」。
