# 走查段②·四族排查（TASK-LG035-WALKTHROUGH-RESTART-01）

- sourceOfTruth: 本件（走查树段②读数正身）
- syncMode: final
- lastSyncedAt: 2026-09-25 20:0x +0800（date 现查 19:59:32）
- 检具: LG-035 CEO 七条否决教训清单（对照 W37 COS 预走查核销记录反推全七条：①界限可辨②「当前生效」只读③命名全落地零「子栏N」④条目表形态零「覆盖」⑤下拉级联⑥策略区归位卡内⑦独立密钥区废除）+ 术语/命名/布局/逻辑双路径四族
- 走查对象: TriModel 实现树 `d012e1e`（ui/index.html 1231 行全量实读）

## CEO 七条核销态复查（现树）

七条在现树全部维持核销态，零回潮：①卡片实线（`.card-main` 1.5px L26）②「当前生效」区零输入控件（L83-88 只读 kv+刷新）③命名零「子栏N」（TriMMC 信息/机器信息/模型信息/模型集/规则/活动策略全落地）④条目表形态（L136 六列表头+空态提示+添加按钮，零「覆盖」）⑤下拉级联（供应商→模型 L643-657）⑥策略区归位卡内（L188-215）⑦独立密钥区废除（密钥=条目列 L136）。

## 术语族

| # | 发现 | 锚 | 严重度 |
| --- | --- | --- | --- |
| T1 | 「层2 开放中」「层2 全量开放中」内部开发阶段词汇入用户面 | index.html L209/L210/L217 | **实锤**（「结构词汇禁入 UI」纪律族；用户视角「层2」为零语义黑话） |
| T2 | 切换确认反馈显内部 id：「已切换至策略 st_lx3k2abc1（保存卡片后生效）」 | L1176（id 形=`st_`+timestamp，L1157） | **实锤**（CEO「切换=选名字」定谳下反馈应显策略名；内部 id 系结构词汇） |
| T3 | 保存失败拼 HTTP 码入文案：「保存未成功（4xx）：…」 | L743（`'保存未成功（' + r.status + '）：'`） | **实锤**（spec I11 验收「人话无 HTTP 码」被本路径自破；对照 fb-restore 同场景用 humanize L484——同页双标） |
| T4 | 删除条目错误文案以偏概全：「该条目正被时段规则使用」（引用检查实际覆盖时段/默认/额度三型 L595） | L597 | 轻微（文案与检查域不符；default/quota 引用时误导） |
| T5 | placeholder 露 env 名（TRIMODEL_API_TOKEN/TRIMODEL_ADMIN_TOKEN L74/76）+折叠面文件名（machine.json L131） | — | 注记不翻案（COS 预走查 09-11 已核销+CEO 复走查在卷，属已接受形态；列此处仅为完整） |

## 命名族

七条③维持 ✓：全页命名零「子栏N」零「覆盖」零自创术语（策略/模型集/规则/活动策略/默认模型全对齐 sg 在役词汇，spec §2.1 词汇对齐承诺成立）。「活动策略」词汇定稿一致（L821 注释+L188 区头+L1069 详情）。

## 布局族（**本段主发现：三处列头/内容错位，同族引入于 v4 段C**）

| # | 发现 | 锚 | 严重度 |
| --- | --- | --- | --- |
| L1 | **策略列表表头 5 列 vs 行 6 单元格**：thead=名称/模型集/规则数/状态/操作（5 th），tbody 行=名称/模型集/规则数/**默认模型**/状态/操作按钮（6 td）——「默认模型」列无表头，「状态」头对「默认模型」内容，「操作」头对「状态」内容，按钮列悬空 | 表头 L202 vs 行 L1115 | **实锤·布局破坏**（列语义全表错一位；v4 段A 加「默认模型」列时表头未跟） |
| L2 | 策略详情规则表第 3 列头「优先级」内容=规则名（「规则『三窗切换』」） | 表头 L198 vs 渲染 L1092/L1101 | **实锤·列头语义错位**（表头无「规则」列，规则名挤进「优先级」列） |
| L3 | 生效时段规则表第 5 列头「操作」内容=规则名文本（只读表无操作可放） | 表头 L218 vs 渲染 L784 | **实锤·同族**（只读展示表挂「操作」头，内容填来源规则名） |

三处同族定谳：v4 层2 段C 改造（规则名进表行）时三表表头未同步——09-14 走查（spec frozen 时点）后引入，**属走查重启窗口抓出的净新增回归面**。

## 逻辑双路径族

| # | 发现 | 锚 | 严重度 |
| --- | --- | --- | --- |
| D1 | **策略删除→保存→复活**：前端删除策略仅 `delete tcStrategies[id]`（L1125），声明的删除通道 `tcDeletedStrategyIds`（L441）全文零 push，PUT doc 不带 `deleted_strategy_ids` 字段（L719-733）；服务端 strategies 合并=`{...base, ...card}` upsert 式（api/trimmc-card.ts L130）+删除须走显式通道（L157-166）——**删除策略后点「保存卡片」，服务端合并使被删策略复活**（重拉即见） | index.html L441/L719-733/L1123-1128 + api/trimmc-card.ts L130/L157-166 | **实锤·功能性缺陷**（删除操作静默失效；对照模型集/规则双通道齐——`tcDeletedModelSets`/`tcDeletedRules` 均 push+随 PUT 发送，唯策略断线）。jsdom/单测若只断言「删除后本地渲染」测不出——需保存→重拉→断言消失完整周期（第四型盲区同族形态） |
| D2 | 模型下拉双写路径竞态：`fillModelSelect`（/v1/models 全量，L401-419）与 `tcFillModelSelect`（硬编码 PROVIDER_MODELS 级联，L645-653）双源写同一控件 `tc-e-model`——refreshAll（连接后/刷新钮）用全量版覆盖，打开表单/切厂商用级联版覆盖；catalog 与硬编码表若不对齐（catalog 新增模型硬编码表未跟），级联版选不到或全量版保存级联外值 | L401-419 vs L643-657 | 中风险（值域一致性未守恒；建议单源化：级联过滤改为对 /v1/models 结果前端过滤） |
| D3 | 首启禁用链键盘旁路：`setDataPanelsDisabled` 对模型集/规则/策略区按钮只加 `disabled-panel` class（pointer-events:none，L297+CSS L51）——挡鼠标不挡键盘 Tab+Enter；且 `tc-ms-add`/`tc-r-add`/`tc-str-add` 等无 `disabled` 属性（对照 `tc-open-add` 有真 disabled L295） | L294-298 | 低（API 401 兜底无破坏性写；但真「鼠标路径 vs 键盘路径」双路径形态，与 LG-035 逻辑双路径教训同构） |

## 段②结论

四族排查完成：**术语族实锤 3（T1/T2/T3）+轻微 2、布局族实锤 3（L1/L2/L3 同族列错位）、逻辑双路径族实锤 1（D1 策略删除复活，阻断级候选）+中风险 1（D2）+低 1（D3）**。命名族零发现。全部发现集中于 v4 段A/段C 演进窗（spec frozen 09-14 16:10 之后引入），CEO 七条核销态零回潮。
