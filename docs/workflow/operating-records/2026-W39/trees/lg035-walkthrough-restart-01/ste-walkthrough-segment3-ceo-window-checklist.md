# STE 走查门段·段三：CEO 真人测试窗清单＋令牌附送件（TASK-LG035-WALKTHROUGH-RESTART-01）

- sourceOfTruth: 本件（CEO 窗检具正身；本窗=policy/card 冻结的合法解冻窗，窗毕重新冻结）
- syncMode: segment
- lastSyncedAt: 2026-09-25T12:1xZ（北京 20:1x，date 现查）
- 备料席: STE 小柯（m-ste）；页面: `http://127.0.0.1:3333/ui`（活体健康 20:10 实测 200/0.008s）
- 预计用时: 主线 20-30 分钟（D 分支另加 5 分钟，候 BOD 裁）

## 〇、窗边界（先读）

1. 本窗内 CEO 的保存/切换/删除写入=**授权窗内合法写**；窗毕 BOD 知会 STE 即做窗后指纹封存，冻结回归席位态。
2. 测试写入全部可逆：测试策略用后即删；「应用到本机」对现活动策略=幂等重放。
3. 发现随手记（浏览器开着不用关）；问题不必当场定位，录树后走查。

## A. 连接与首启（约 3 分钟）

| 步 | 操作 | 预期 |
| --- | --- | --- |
| A1 | 浏览器开 `http://127.0.0.1:3333/ui` | 页面出「TriModel 模型配置」标题+「连接设置」区 |
| A2 | 若令牌已存（上次用过本页）：直接看右上状态点 | 绿点「已连接」→跳 B；灰点/黄点→A3 |
| A3 | 填 API 令牌+管理令牌（见附送件）→点「连接」 | 绿点「已连接」；策略列表/生效值自动刷出 |

## B. 策略面走查＋reload 全周期（核心环，约 10 分钟）

| 步 | 操作 | 预期 |
| --- | --- | --- |
| B1 | 看「活动策略」区下拉 | 回显「时段切换策略（活动中）」；详情=名称/目的（闲时用 glm 忙时用 deepseek）/引用模型集/默认模型 GLM-5.3/规则 2 条 |
| B2 | 看「生效时段规则」表 | 三行窗：00:00–14:00 GLM-5.3 / 14:00–18:00 deepseek-v4-pro / 18:00–23:59 GLM-5.3（对应现在时刻的行=当前正在用的模型） |
| B3 | 看「当前生效」区 | 当前模型+来源+一致性「✓ 一致」（21:00-21:30 窗应显 GLM-5.3·策略） |
| B4 | **reload 全周期环**：「新增策略」→名称填「CEO-走查临时」→选模型集/任意规则→保存策略→「保存卡片」 | 提示「卡片已保存，等待应用」；策略列表出现「CEO-走查临时」 |
| B5 | **按 F5 刷新页面**（关键步：验证真刷新后不丢） | 「CEO-走查临时」仍在列表；活动策略回显不变；连接保持已连接（令牌没丢）；三窗规则表仍在 |
| B6 | 删除「CEO-走查临时」（列表行「删除」）→「保存卡片」→再 F5 | 删除后列表消失，刷新后仍消失（删除回环闭合） |
| B7 | 顺手试「切换至选中策略」→看成功提示 | 记录观感：提示里显示的是策略名还是 `st_xxxx` 一串代码（已知发现，验实机观感） |

> B4-B6 即「保存→真刷新→断言仍在」完整周期——自动化测试盖不到的真浏览器盲区面，此环过=第四型盲区闭合。

## C. 已应用态验证（默认分支，约 5 分钟——apply 按钮当前隐藏时的走法）

| 步 | 操作 | 预期 |
| --- | --- | --- |
| C1 | 看「当前生效」区来源 | 「策略」（说明正在按策略时段出模型，政策已生效中） |
| C2 | 对照现在时刻与 B2 三窗表 | 当前模型=时刻所在窗的模型（如 21:0x→GLM-5.3） |
| C3 | 看卡片徽标（【TriMMC 信息】旁） | 预期显「待应用」——已知漂移（系统实际已生效运行，徽标停在 09-14 保存态），如实记录即可，不是新问题 |

## D. 应用到本机全环（候 BOD 裁，约 5 分钟）

> 前置：活体服务当前未开本机应用开关，「应用到本机」按钮**不显示**。若 BOD 裁以 `TRIMODEL_LOCAL_APPLY=1` 重启服务后开走本节；不裁则本节候后窗，C 节已覆盖验证实质。

| 步 | 操作 | 预期 |
| --- | --- | --- |
| D1 | 确认「应用到本机」按钮出现 | 按钮可见（开关生效） |
| D2 | 按下「应用到本机」 | 提示「已应用到本机：…」；徽标翻「已生效」（C3 漂移同时翻正）；「当前生效」自动刷新 |
| D3 | 再按一次（幂等验证） | 再次成功，三窗表不变，无重复条目 |

## E. 观察项单（顺手看，不专程）

1. 「模型集」「策略」区空态文案里是否出现「层2」字样（内部建造词，已知×3 处）；
2. 打开「添加模型条目」表单看「模型」下拉（观感即可，已知双目录链发现，勿真保存）；
3. 「直连兜底」区按钮「还原兜底」名称观感（实为写入动作）；
4. 「生效时段规则」表最右列「操作」表头下实为只读文字（名不符实发现）；
5. 界面任何让你「看不懂/别扭/要问人」的文案——**这是最高价值观察**，见一条记一条。

## F. 窗毕

1. CEO 测试结束告知 BOD → BOD 知会 STE → STE 窗后指纹封存（配置文件前后对表）+ 冻结回归声明；
2. 发现记录随窗收口入树（BOD 代录亦可）；走查结论清单候 CEO 终验收。

## 附：令牌附送件

- **API 令牌**：＿＿＿＿＿＿＿＿（连接设置第一栏）
- **管理令牌**：＿＿＿＿＿＿＿＿（连接设置第二栏）
- 附送说明：两令牌=本地环回面（127.0.0.1，不出本机），W38 就绪声明先例=值随信附送不入仓。**本件不留值**——随窗由 BOD/COS 附实际值发出；若自 W38 后有轮换，以服务现役值为准（CEO 亦可从启动TriModel的命令行窗口直接读到两个 TRIMODEL_ 开头的值）。
