# 拆派单·波①连接配置页＋波②分层恢复结构（TASK-TRIMODEL-RECOVERY-LADDER-01）

- sourceOfTruth: 本件（波①②执行拆派单正身；D-15 分派枢纽留痕件）
- syncMode: final
- lastSyncedAt: 2026-09-25 23:0x +0800（date 现查 23:07:56 hook 链）
- 接令记录: CEO 22:57 批（批准记录 ccf10dff 三面同值，双签终版 24ba1ccc）→BOD 执行令→COS 23:00 流转→COO 23:07 转达本席；「批后立即执行免再请」；本席接令即拆派（D-15 枢纽）
- 方案正身: joint-plan.md（双签终版）——**本单所有范围表述以方案为准，本单只做拆派分工与门禁落点**

## 派工纪律四条（COO 执行令③，随单下传全执行席）

1. 禁止越波（波次表=方案§四）；范围爬爬动议一律回方案§五 MVP 边界对表；
2. 活体 settings.json 操作必过事故案补丁门（测试隔离/值面验证/备份锚）——**本波配置写入全走沙箱，真活体零接触**；
3. **真活体 settings.json hash 零变化=演练总判据**（随到随验，不留到波④）；
4. 防线继承五防线为验收门非备忘录（对照 STE 25/25 基线）。

## 波①·连接配置页（派 FSD 席）

- **载体**：TriModel `ui/index.html` 现役 fb-zone 扩展为「连接配置」页（方案问1 裁决：禁自由 JSON 编辑器）。
- **只做**：模板/形态切换（选模板后只填钥一个必填项）；`.deploy-key` 注入按钮（服务端读独立钥回填，禁从活体读钥回填）；写入 env 子集 diff 预览（只读）；备份清单＋一键回滚；页头常驻明示「服务不可用时请用命令恢复」。
- **写路径五门逐条落地**（方案问1 五门表=验收对照表，一条不许丢）：备份先行＋轮换近 5 份＋哨兵豁免＋幂等短路（无变化不写不备份）／键名服务端锁定／凭据健康门三态全拒 fail-closed／写前整文档 parse＋diff 确认＋写后回读断言失败自动回滚／掩码红线＋结构化审计行。
- **禁区**：LG-035 冻结面零触碰（trimmc-card.json/policies/卡/生效值/策略区）；env 子集以外任何键；完整凭据显示/导出/入日志；页面不进 TriMMC 卡。
- **验收门**：①五门对照表逐条勾验；②产品验收=不懂后端的人拿到钥 60 秒完成切换；③UI 交付渲染验证门硬条款（LG-035 教训族）：非作者手测＋jsdom 首启链冒烟（保存→reload→断言仍在完整周期）；④沙箱纪律：页面开发期配置写入全走 `-TargetDir` 沙箱，真活体零接触。
- **回报形态**：完工读数含全量测试四项读数+既有失败逐族归因（全量读数回报纪律）；五门对照表逐条读数；沙箱/真活体 hash 前后对照。

## 波②·分层恢复结构（派 FSD 席，与波①并行）

- **L1**：TriModel-Watchdog ps1 v2 改造＋计划任务 Disabled→Enabled。改造点（方案问2 判定表）：探针双层化（/health＋业务探针 `/v1/config/keys` 带值断言——401/空值=auth 死态）；重启 3 轮计数（重启→10s 复探→60s 窗）；auth 死态跳 L1 直 L2；连续 3 轮无效→L2 触发标记落盘 `.fade/trimodel-l2-flag`。日志留痕沿 v2 现役格式。
- **L2**：TriMLC cron 2min 扫描任务（扫 L2 标记＋自探活双层同款）→命中调 restore-direct。**依赖注记：restore-direct 本体=波③共享 core 交付物——本波 L2 先立「标记扫描＋探活判定＋调用桩」（桩=结构化日志行 fail-closed 不写真活体），波③命令族落地后接线换真**。
- **L3**：Windows 原生 toast（零第三方依赖）＋TriMLC cron 30min 重提醒＋VBS 包装启动（D-29 合规，Run 第二参=0）；文案四要素（①出了什么事②现在什么状态③要你做什么④去哪看详情），禁术语禁恐慌体；toast 携带 restore 命令全文可复制。
- **禁区**：不动本机其他 watchdog 任务族（TriMLC-Watchdog/TriRLC-Watchdog/TriHubWatchdog/Seat-Watchdog 零触碰）；不写真活体 settings.json（含经桩路径）；L2 钥源 `.deploy-key` 读写逻辑候波③ core，本波只定义标记文件契约（路径/内容格式/清理时机）。
- **验收门**：①判定表全分支单测（进程死/auth 死/3 轮无效/恢复）；②L1 可真做（进程≠活体配置）；③toast 文案四要素逐条人话验收（非工程角色读得懂）；④全部演练态写入走沙箱/桩，真活体 hash 零变化。
- **回报形态**：同波①（全量读数+判定表分支对照+hash 对照）。

## 波次依赖与哨位读数对位

- 波①②并行开动（BOD 波次令）；③复用①②内核候①②；④候①②③毕；R-HY 部署与①②③并行不阻塞本地梯（M1 通路三态勘验=执行窗首项族，随部署波另派）。
- BOD 哨位四读数对位：①波①②启动读数=本单落树+派工回执+首项勘验（本席随报）；②R-HY M1 通路三态（部署波）；③F1-F5＋toast 实弹（波④）；④防线继承 STE 25/25 对照（restore 退役前最后一门）。

## 首项勘验（本席枢纽亲执，23:0x 实勘毕，随启动读数报）

正名判据勘验读数（package.json bin 字段实勘 + 本机 PATH Get-Command）：

| 仓 | 现役 bin | 方案正名 | 判定 |
| --- | --- | --- | --- |
| TriMLC | `trilc` | trimlc | 改名 |
| TriRLC | `trilc` | trirlc | 改名——**与 TriMLC 同名冲突实锤**（双仓 bin 字段逐字相同 `./dist/cli.js`） |
| TriMMC | `trimc` | trimmc | 改名——**与 TriRMC 同名冲突实锤** |
| TriRMC | `trimc` | trirmc | 改名 |

- **两组同名冲突成立**（本机组 trilc×2／服务域组 trimc×2）——方案问3 正名判据全面命中，且较预判多一组（服务域组）；四族全旧名，三名一致差距=4/4。
- 本机 PATH 现勘：`trilc/trimlc/trirlc` 三名 Get-Command 全不可达——npm global bin 未装或不在 PATH，即期无打架实害；正名照裁（波③执行首项：四仓 bin 改名+旧名 alias 过渡一版+弃用标注）。
- 波②/波③执行席注记：正名落地前，凡文档/命令示例一律用正名（trimlc/trirlc/trimmc/trirmc），禁新增旧名引用。

## 使用依据

joint-plan.md 双签终版；CEO 22:57 批记录（ccf10dff/24ba1ccc）；BOD 执行令（COO 23:07 转达）；D-15 分派枢纽纪律；M-004 席位派工口径；事故案补丁门（trees/incident-sde-settings-01/）。
