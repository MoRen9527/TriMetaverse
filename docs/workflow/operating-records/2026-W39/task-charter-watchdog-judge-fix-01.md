# 任务书 TASK-WATCHDOG-JUDGE-FIX-01：看门狗判定面根修+fsd 双实例归一（两阶段）

- **发令**：BOD（CEO 批 2026-09-22 19:31 +08:00）
- **承接**：m-sde（ops-watchdog 域，经 COS 流转占新 LG 号）
- **face**：M 面（本机）
- **收口回报截点**：2026-09-22 22:00 +08:00

## 一、背景（诊断结论，证据全在卷）

2026-09-22 19:16 BOD 勘验：看门狗在岗判定仅认命令行 `--resume <席名> `（尾空格）单一特征；今日 11:54 FSD 按 D-32 弃转录重生（`-n m-fsd` 无 --resume）对看门狗不可见 → **11:46–14:11 盲拉 22 次**（`.fade/seat-watchdog.log` 实录），其间 resume 形态实例多为短命（深水转录恶性循环系统级复现）；14:11 拉起实例存活后判定恢复，双实例并存至今。定性=D-32 与 seat-watchdog（09-17 写）接口裂缝。CEO 批两件：根修+归一，顺序不可反。

## 二、阶段一：判定面根修+超员检测（先做）

真源=`TriCompany/scripts/ops/watchdog/seat-watchdog.windows.ps1`（部署位 `.fade/seat-watchdog.ps1` 走 sync.ps1 追平，禁直写）：

1. **在岗判定扩展**：由 `--resume <席名> ` 单特征改为**双参数特征任一命中**：`-n <席名>` 或 `--resume <席名>`（后跟空白或行尾，正则 `(-n|--resume) <名>(\s|$)` 形）。已核 13 席名两两无子串关系，无误报互串面；弃转录重生形态（D-32）自此可见。
2. **超员检测补装**：同一席名命中 >1 个 claude.exe 进程 → 写日志告警行（如 `超员告警: m-fsd ×2 [PID列表]`）——**只告警不杀**。
3. 拉起逻辑/stop-flag 机制/计划任务链**不动**；D-30 归档纪律头注随改。

**阶段一验收锚**：
- A. 判定复演修前/修后对照：对现役 PID 34924（`-n m-fsd` 形态）修前 0 命中→修后命中；对 13 席全集复演每席恰命中自身（无误报读数）。
- B. 超员告警真实场景实证：现役双 fsd 下跑一轮，日志出现 `m-fsd ×2` 告警行（原文）。
- C. 双解释器解析 0 错+编码纪律+头注。
- D. 部署位 sync.ps1 追平+追平后看门狗下一轮日志正常（禁直写）。

**⚠ 阶段门：D 锚（追平）验证过并出读数后，方可进入阶段二。**

## 三、阶段二：fsd 双实例归一（后做）

1. **杀前留证**：PID 15492（14:11 resume 形态实例）命令行快照+起时戳入读数件。
2. **精确终止**：`Stop-Process -Id 15492`（仅此一个 PID）——该实例背 D-32 深水转录（97%），高危冗余。残留 wt 窗（pwsh 提示符）如实报告处理方式，不强扩动作。
3. **保留** PID 34924（11:54 弃转录重生健康实例，浅水）。

**阶段二验收锚**：
- E. 杀前快照+杀后名册读数：m-fsd 单实例（452e2b/34924 在册）。
- F. 归一后看门狗连续两轮日志：m-fsd 判定在岗零动作（新判定看见 34924，**不再盲拉**）+ 超员告警消失。
- G. 其余 12 席零误伤（全集在位读数）。

## 四、红线

- 杀进程仅限 PID 15492 一个；**禁杀 34924、禁动其他任何席位进程**；
- 阶段顺序不可反；部署位禁直写（D-30）；sg 侧无涉；
- m-fsd 名址派工在归一完成前处于双实例态——期间对 m-fsd 的新派工挂起（COS 知会面）。

## 五、回报格式

两阶段各锚读数全量原文（修前/修后对照+超员告警原文+杀前快照+归一后名册/日志读数）+TC commit 号+部署位追平读数+树指针（读数件落承办席所在树 W39）。BOD 验收销账。
