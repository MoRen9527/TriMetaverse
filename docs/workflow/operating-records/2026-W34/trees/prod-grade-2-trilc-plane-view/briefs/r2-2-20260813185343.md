# r2-2 工作简报

- **节点**: r2-2
- **agent**: FullStackDeveloper (小全)
- **树**: prod-grade-2-trilc-plane-view
- **完成时间**: 2026-08-13T18:53:43+08:00（编排层机器时间口径）

## 做了什么

按 r2-1 方案（四要素定案 + §三实现清单）完成 TriLC 仓周平面读取路径两轨化，只动 TriLC 仓（工作路径核查：落点 src/project/，与 multi-project-router 同级；TriMC/agent-core/周平面文件零触碰）。

**P1 核心**
1. `src/project/weekly-plane-root.ts`（新）：`resolveWeeklyPlaneRoot()` 纯函数——① `TRILC_WEEKLY_PLANE_ROOT` env 显式（须存在，幽灵路径回退不误读）→ ② workspace sibling 探测（`<TriLC-root>/../TriMetaverse/docs/workflow/operating-records`，existsSync 确认；src/project 与 dist/project 同深度两层上即 TriLC 根，推导成立）→ ③ undefined。绝不创建目录。先例 = env.ts `resolveFromWorkspace()` 模式。
2. `src/project/multi-project-router.ts`：`ProjectPaths` 加 `companyWeeklyPlaneDir?: string`（仅配置/发现成功时存在）；`resolveProjectPaths(projectRoot?, weeklyPlaneRoot?)` 可选第二参（默认调 `resolveWeeklyPlaneRoot()`）；`ensureProjectDirs` 透传第二参且**绝不创建公司轨**；模块头注释补两轨边界声明（pipe3-1 禁令面仅 `.tricompany-cognition/` 存储，周平面非 memory store，`enforceProjectIsolation()` 有意零改动）。
3. `src/config/env.ts`：`TriLCEnv` 加 `weeklyPlaneRoot?: string`（env 纯透传，解析归 router 层）。
4. 装配点：摸底确认 src 内**无** `resolveProjectPaths` 直接调用点（仅 router 自身与测试）——默认发现即装配，"不注入 = 逐字节旧行为"自动成立，零装配改动。

**P2 测试与门禁**
- 新增 7 用例全绿：weekly-plane-root 4 用例（env 命中 / env 幽灵路径 undefined / env 空串走 sibling / sibling 条件断言——命中环境断言路径、非命中环境断言 undefined，任何环境可跑）+ router 两轨 3 用例（注入命中 / undefined 回退 / `ensureProjectDirs` 对不存在的公司根**不创建**）
- **既有 24 个 router 测试零改动全过**（src/project/multi-project-router.test.ts，逐个核对无预期变更）
- `npm run check` tsc clean
- 全量 `npm test`：285/286，1 失败为 `test/tui/components.test.ts` **既有环境失败**——`ink-testing-library` 未声明于 package.json（devDeps 无此项）也未安装，错误发生在模块加载阶段（import 即抛，任何测试逻辑执行前），与本次改动（仅 env.ts + router 两文件）无关；证据链：git status 确认该测试文件未被触碰 + 失败点在 ERR_MODULE_NOT_FOUND 加载期

**P3 文档**
- env.ts 注释 + router docstring 两轨语义（已随代码）
- `docs/execution/multi-project-isolation/VERIFICATION.md` 增补 §2.2a（公司轨只读语义 / 不自动创建 / 隔离边界不变 / 向后兼容声明）；自查结论：G1-G3 项目轨契约未变，原断言全部保持有效

**P4 跟进项登记**
- TriCade 打包侧（TriMetaverse/scripts + MSI 安装链）在安装/升级时注入 `TRILC_WEEKLY_PLANE_ROOT`（指向 CEO 本机 TriMetaverse 检出路径，可配）——归仓 TriCade 打包通道，本树不做，由编排层路由

## 关键决策记录

- env 幽灵路径处置：`TRILC_WEEKLY_PLANE_ROOT` 显式但目录不存在 → undefined（不返回幽灵路径），与 §2.1 第三层"发现失败 → undefined"一致；router 层读取优先级守卫（目录存在才用公司轨）双保险
- 测试文件落点：`src/project/weekly-plane-root.test.ts`（与既有 router 测试同目录；TriLC `npm test` 只跑 `test/**`，router 域测试历来单独跑，两轨测试并入新文件避免触碰既有文件）
- 计数口径：r2-1 提及"既有 17 个 router 测试"，实测文件内为 24 个 it（计数方式差异），门禁口径 = 既有测试零改动全过，不以具体数为准

## 使用依据

- r2-1 方案 brief（briefs/r2-1-20260813184459.md，四要素定案 + §三清单）
- TriLC/src/project/multi-project-router.ts（pipe3-1 现状契约）、src/config/env.ts（resolveFromWorkspace 先例 + REQ-014b）
- TriLC/docs/execution/multi-project-isolation/VERIFICATION.md（G1-G6 验证面）
- 生产级开发期方案 §5.3/§5.4（归仓路由 / 中改通道）
