# r2-3 工作简报（独立验证）

- **节点**: r2-3
- **agent**: TestEngineer (小柯)
- **树**: prod-grade-2-trilc-plane-view
- **完成时间**: 2026-08-13T19:04:03+08:00（编排层机器时间口径）
- **判定**: **FAIL（阻塞性缺陷 1 项）**——源码态 sibling 自动发现不生效（本机实测未命中）；其余三验证面全过。修复为一处深度参数（3 层上跳）+ 测试期望同步修正，修复后重验即可转 PASS。

## 一、测试判断

本树核心交付「源码态零配置自动发现公司周平面」当前**不成立**：本机（D:/Code/ai 工作区，TriMetaverse 同级存在）无 env 运行 `resolveWeeklyPlaneRoot()` 实测返回 `undefined`。根因是 sibling 探测深度少一层：模块位于 `src/project/`（编译后 `dist/project/`），两层级上跳只到 TriLC 根，落点变成 `D:/Code/ai/TriLC/TriMetaverse/...`（不存在），而设计意图（r2-1 §2.1）是 `<TriLC-root>/../TriMetaverse/...`。单测因 `expectedSiblingPath()` 使用同一错误深度，条件断言走了 else 分支，将此缺陷掩盖为全绿。

## 二、测试策略

- 四验证面（照树）：①源码态 sibling 发现 ②模拟安装态三态 ③公司轨只读断言 ④隔离语义不回归。
- 方法：diff 审查（4b575b3 全文）+ 代码走读 + 门禁重跑（新增 7 用例 / 既有 24 router 用例 / tsc / 全量 npm test）+ 活体四态实测（真实模块 + 模拟安装态副本）+ 既有文件零改动复核（git diff 计数）。
- 纪律：只读验证；W34 迁移状态零触碰；真迁移零触发；周平面写权不变。

## 三、测试结果

### 3.1 验证面① 源码态 sibling 发现 —— FAIL（阻塞）

- 实测：本机 `TRILC_WEEKLY_PLANE_ROOT` 未设，`resolveWeeklyPlaneRoot()` 返回 **undefined**；而真实 sibling `D:/Code/ai/TriMetaverse/docs/workflow/operating-records` 存在（ls 确认）。
- 根因定位：`weekly-plane-root.ts:39` `resolve(scriptDir, '..', '..', ...WEEKLY_PLANE_SEGMENTS)` —— `src/project`（或 `dist/project`）两层级上跳 = TriLC 根，再拼 `TriMetaverse` 得 `D:/Code/ai/TriLC/TriMetaverse/...`（实测不存在）；正确落点需**三层级**上跳（`..','..','..'`）到工作区根再拼 `TriMetaverse`。r2-1 方案意图「`<TriLC-root>/../TriMetaverse`」明确，实现未达；r2-1 brief §三.1 的代码草样亦为两层级（设计文档同源笔误，登记）。
- **测试掩盖分析**：`weekly-plane-root.test.ts` 的 `expectedSiblingPath()` 以同一两层级计算期望路径 → 本机 `existsSync(expected)=false` → 条件断言走 else 分支（断言 undefined）→ 用例全绿但「命中环境断言路径」从未真正执行。修复期望深度后，本机上该用例将真实断言命中（并会对当前错误实现报红——回归守卫恢复）。
- 影响面：源码态零配置发现失效（本树核心价值之一）；env 显式路径不受影响（3.2 实测通过）；安装态本就不依赖 discovery（P4 打包侧 env 注入路线）。
- 修复建议（一行 + 测试同步）：实现 `'..','..'` → `'..','..','..'`（或 WEEKLY_PLANE_SEGMENTS 首位加 `'..'`）；`expectedSiblingPath()` 同步修正；可选加固：把深度推导收敛为单一共享 helper，避免实现与测试双份重复。

### 3.2 验证面② 模拟安装态三态 —— PASS

- env 显式（有效路径）：返回解析绝对路径 ✓（活体实测，真实模块 + 模拟安装态副本双跑）。
- env 幽灵路径（不存在）：返回 undefined（不误读幻影根）✓（活体实测）。
- 未配置（无 sibling 上下文）：返回 undefined = 旧行为 fallback ✓（模拟安装态副本置于无 sibling 目录活体实测；env 空串同样 fallthrough 行为正确）。
- 备注：源码态「未配置」本应命中 discovery，当前因 ① 误落 undefined —— 同一根因，随 ① 修复解决。

### 3.3 验证面③ 公司轨只读断言 —— PASS

- 代码走读：`ensureProjectDirs()` 仅 mkdir `cognitionDir` 与 `operatingRecordsDir` 两项目轨目录；公司轨零创建（注释 + 无对应代码路径）✓。
- 单测：用例 7 无条件断言 `existsSync(fakeRoot) === false`（真实断言，非条件式）✓。
- 活体复跑：注入不存在的公司根 → `companyWeeklyPlaneDir` 返回注入值、目录**未创建**、项目轨两目录正常创建 ✓。

### 3.4 验证面④ 隔离语义不回归 —— PASS

- `enforceProjectIsolation` 零代码改动：4b575b3 diff 中该符号仅出现于新增头注释（"intentionally unchanged"）✓。
- 既有 router 测试零改动：`git diff 4b575b3^ 4b575b3 -- src/project/multi-project-router.test.ts` = 0 行 ✓；24/24 用例全过 ✓。
- `resolveProjectPaths` 签名向后兼容（第二参可选，`??` 语义正确）；未注入时项目轨字段逐字节不变（用例 1 断言 operatingRecordsDir 旧路径 ✓）。

## 四、门禁与纪律核查

- 新增 7 用例 7/7 绿（含上述掩盖说明）；既有 24 router 用例 24/24 绿；`npm run check` tsc clean。
- 全量 `npm test`：285/286，唯一失败为 `test/tui/components.test.ts` 既有环境失败（ERR_MODULE_NOT_FOUND: ink-testing-library，模块加载期即抛）——与本次改动无关，确认与 r2-2 报告一致。
- TriLC 工作树干净；agent-core/TriMC/周平面文件零触碰；W34 迁移状态零触碰。

## 五、质量门禁评估

- 代码质量与设计一致性：②③④ 全过，① 为单点深度错误。
- 判定：**FAIL**（阻塞 ①）——建议打回 r2-2 owner 修复（一行改动 + 测试期望同步），修复后我 1 分钟重验（本机 no-env 应返回 `D:\Code\ai\TriMetaverse\docs\workflow\operating-records`；单测命中分支应真实执行）。

## 六、使用依据

- tree-op.json + r2-0/r2-1/r2-2 briefs（范围、方案、实现声明）
- TriLC 4b575b3 diff 全文 + `src/project/weekly-plane-root.ts` / `multi-project-router.ts` / `src/config/env.ts` / `weekly-plane-root.test.ts` 代码走读
- 活体实测：真实模块四态 + 模拟安装态副本（`D:/Code/r2-3-install-sim/`）
- TriLC `npm test` / `npm run check` / git diff 零改动复核
