# r4-2 独立验证 brief — prod-grade-4-lscwd-fix

- 时间：2026-08-14T03:35:15+08:00（UTC 2026-08-13T19:35）
- agent：TestEngineer（小柯）
- 动作：独立验证 r4-1 实现（小狄门禁六口径 + 树内 r4-2 行动项⑥观察项）
- 验证对象：TriLC a6df674 + TriMC 7e15ecf（均 dev 分支，未合并，符合"迁移验收前不合并"护栏）

## 测试判断

**PASS（建议放行至 r4-3 小狄终审）**。六口径全部通过，无阻塞性缺陷；1 个既有基线失败与本次改动无依赖关系（父 commit 复验确认），1 个观察项（模型行为面）单独记录不判回归。

## 测试策略

- 代码审查层：两个 commit 的完整 diff 逐文件审查（`git show`），与 r4-0 定案逐条对照。
- 测试执行层：TriLC/TriMC 全量 `npm test` + 定向复跑新增测试文件 + 两仓 `tsc --noEmit`。
- 交叉验证层（第二方法，不依赖小全测试文件）：自写独立脚本（`xke-r4-2-verify.mts`，临时，跑完即删）直接调 `executeTool`，在 dev（修复后）与父 commit（修复前）上对比运行，验证"跟随 ctx.cwd"与"无 ctx 零变化"两型。
- 基线失败独立归因：`components.test.ts` 在父 commit 3002424（TriLC 修复前）上复跑同样失败，确认既有。

## 测试结果（六口径）

### 口径 1：五读工具 ctx.cwd 统一 — PASS

代码审查（TriLC）：
- file-ls.ts:90 / file-read.ts:65 / file-glob.ts:159,165 / file-grep.ts:552,569：基准统一 `ctx?.cwd ?? process.cwd()`，相对路径 `resolve(base, ...)`，与定案一致。
- shell-exec.ts:108：特例 `(args.cwd as string) || ctx?.cwd || process.cwd()`，spawn 消费 cwd 变量（shell-exec.ts:127），模型显式参数优先语义保留。

代码审查（TriMC）：
- tools.ts:231 shell_exec 同款特例；tools.ts:286-296 glob_search `base` + `basePath`，walk 入口 `walk(basePath, 0)`（tools.ts:340）与 relative 均消费 basePath；tools.ts:391 task 子代理 spawn `cwd: ctx?.cwd ?? process.cwd()`。

行为实测（独立脚本，dev 上）：
- LS ctx=dirA → path=dirA 且 entries 仅 a.txt；ctx=dirB → path=dirB 且 entries 仅 b.txt（不同 cwd 跟随成立，dirB 无泄漏）。
- shell_exec ctx=dirA → stdout=dirA。
- TriMC glob_search ctx=dirA → base=dirA matches=[a.ts]；ctx=dirB → base=dirB matches=[b.ts]；shell_exec 同款跟随。
- 修复前父 commit 对照：同样传 ctx 时 LS/glob/shell_exec 全部忽略 ctx 落 process.cwd()（glob_search 在 dirA 下搜不到 a.ts，matches 为空）——与 BUG-20260814-001 根因吻合，反证修复有效性。

### 口径 2：REQ-014b 注释与实现一致性 — PASS

- 权威定义：`tricade-implementation-playbook.md:358`（REQ-20260806-014b）——工具执行 cwd 必须用 agent loop 工作区而非 daemon 启动目录。
- TriLC 五个工具文件 + TriMC tools.ts 均带 REQ-014b 注释，语义与实现一致："resolve relative paths against the agent loop cwd (ctx.cwd), not the daemon launch dir; ctx absent in legacy call sites → fall back to process.cwd() (unchanged legacy behavior)"。
- shell-exec 注释正确标注三级优先级（模型显式 args.cwd > ctx.cwd > process.cwd()）。

### 口径 3：周平面根提示注入（C 定案限定形态）— PASS

- app.ts:1973 改动为定案限定形态：`body.systemPrompt ? body.systemPrompt + buildWeeklyPlaneHint() : defaultSystemPrompt()`；diff 中 app.ts 仅此一处逻辑改动（另有一处 buildWeeklyPlaneHint 注释补记，属定案允许项）。
- 其余 5 个 defaultSystemPrompt 调用点与 OpenAI 兼容端点（1165/1560/1685）零变化（diff 确认）。
- buildWeeklyPlaneHint 注释补两注入点互斥标记（defaultSystemPrompt 内部 / tasks-submit 外部 append，二选一叠加）——防双注入标记落地。
- 实测：C 侧测试 2/2 过——传 client systemPrompt 时 prompt 含 hint 恰好 1 次且 client 原文保留；不传时走 defaultSystemPrompt 原路径含 hint 且不双注入。

### 口径 4：回归基线 + tsc — PASS

- TriLC 全量：299 用例，298 过，1 失败 = `test/tui/components.test.ts`（缺 ink-testing-library devDependency，ERR_MODULE_NOT_FOUND）。在父 commit 3002424 上复跑同文件：1 用例 0 过 1 失败——既有基线失败，与本次改动无关。
- TriMC 全量：503 用例，502 过，0 失败，1 既有 skip。
- 两仓 `tsc -p tsconfig.json --noEmit` 全过。
- 新增测试定向复跑：TriLC 13/13（tools-ctx-cwd 11 + tasks-submit-weekly-hint 2）、TriMC 7/7 全过。
- 既有测试预期零改动：两仓 commit diff stat 中无既有测试文件，仅新增 3 个测试文件。

### 口径 5：agent-core 零改 — PASS

- TriLC/TriMC 两个修复 commit 的文件清单不含 agent-core。
- TriCompany 仓库 `packages/agent-core` 工作区干净；最近提交 d64ad93 属 r3 共享 core 缺口通道，与本树无关。
- `ToolContext` 类型（tools.ts:13）与 loop 内 `executeTool(name, args, { cwd: options.cwd })` 传递（loop.ts:589）均为 3288e13 迁移时既有实现——上游早已传 ctx，本修复只补消费端，agent-core 无需改动，零改护栏成立。

### 口径 6：开发态行为零变化（未传 ctx.cwd 时与旧版一致）— PASS

- 代码层：回退分支即原始表达式（`ctx?.cwd ?? process.cwd()` 在 ctx 缺失时等价于旧 `process.cwd()`），diff 逐行核对无其他语义变化。
- 测试层：新增测试 ctx 缺失回退型用例全过（TriLC 5 型 + TriMC 2 型）。
- 交叉验证：独立脚本无 ctx 调用输出在 dev 与父 commit 上逐字节一致（LS path、shell_exec stdout 均等于 process.cwd()）。

### 口径 5 附项：executeTool 透传防误用标记核实 — PASS

- TriMC tools.ts:42 包装补第三参透传，注释标注"无现役调用方传 ctx——legacy 直调行为不变"。全仓 grep 确认：TriMC src 内无 executeTool 现役调用方（仅定义处），标注属实。现役执行路径为 agent-core loop 直接调 coreExecuteTool 并传 ctx（loop.ts:589），链路闭合。

## 观察项（不判回归）

1. **模型行为面**（树内 r4-2 行动项⑥）：修复后若模型仍显式传 `C:\Users\jedih\Documents` 之类路径，属模型行为面，非代码缺陷——本次验证轮单独记录，不判回归。验证轮内未做模型端实测（无需；代码路径已由工具层断言覆盖）。
2. **TriMC executeTool 包装无现役调用方**：第三参透传当前无消费场景，属为将来调用方预留（定案已标注）。若后续 TriMC 内部新增 executeTool 调用点，应传 ctx 以继承 loop cwd。
3. **既有基线失败跟踪**：TriLC components.test.ts 缺 ink-testing-library devDependency，建议另行登记低优先级修复树（不在本树范围）。

## 质量门禁评估

六口径全部满足小狄门禁：既有测试零改动、ctx 两型断言落地且实测通过、C 侧限定形态与防双注入标记落地、回归基线维持（298/299 + 502/503）、agent-core 零改、开发态行为零变化。无阻塞性缺陷。**PASS**，建议放行至 r4-3 小狄验收口径终审。

## 使用依据

- 树文件：`docs/workflow/operating-records/2026-W34/trees/prod-grade-4-lscwd-fix/tree-op.json` + `briefs/r4-0-20260814031349.md`（小狄定案）
- REQ-014b 权威定义：`docs/execution/tricade-implementation-playbook.md:358`
- 修复 commits：TriLC a6df674、TriMC 7e15ecf（完整 diff 审查）
- 测试文件：TriLC `test/tools-ctx-cwd.test.ts`、`test/server/tasks-submit-weekly-hint.test.ts`；TriMC `test/agent-loop/tools-ctx-cwd.test.ts`
- 交叉验证：临时脚本 xke-r4-2-verify.mts（dev 与父 commit 对比，已删除）
- 父 commit 基线复验：TriLC 3002424、TriMC c60250e
