# TriMetaverse 项目级 AI 共学周记（2026-W29）

## 文档同步元信息

- sourceOfTruth: TriMetaverse/docs/workflow/operating-records/2026-W29/project-ai-community-weekly-2026-W29.md
- syncMode: audit-record
- lastSyncedAt: 2026-07-17

时间窗口：2026-07-13 至 2026-07-19

维护：秘书处（当前由 `CEOChiefOfStaff` 代管）

状态：本周持续更新中（draft，待周末提交 CEO 确认签发）

版本：draft-2026-W29

对外用途：用于每周向其他使用 AI 做项目的社区成员分享本周在 `TriMetaverse` 推进过程中观察到的大模型能力问题、使用体验和实践经验，供共同学习与讨论。

说明：本文件记录的是当前周的项目实践观察，不自动上升为长期制度、通用理论或模块正式结论；如形成稳定方法论，应另行回写对应真源文档。

## 1. 本周项目背景

本周继续在本地 Copilot-host 上推进赛博公司的研发与宿主接管工作，包含长会话任务编排、工具调用、上下文压缩、会话恢复以及 TriMC 工程任务。项目同时使用 Copilot 内置模型与自定义 OpenAI Provider，需要验证的不只是普通问答兼容性，还包括长上下文和 Agent 工具链兼容性。

## 2. 本周观察到的大模型能力问题与体验

### 2.1 Copilot CLI 与 DeepSeek 在长会话压缩时重放空 assistant 消息并返回 400

- 现象：
  在 Copilot CLI `1.0.71` 中使用自定义 OpenAI Provider 接入 `deepseek-v4-pro`。普通新会话能够正常回复，但长会话达到自动压缩阈值后，后续消息稳定返回 `400 Invalid assistant message: content or tool_calls must be set`。
- 具体表现：
  现场日志显示，DeepSeek 曾把一段完整的最终答复放入扩展字段 `reasoning_content`，而标准 `content` 为空，结构化工具调用列表也为空。Copilot CLI 仍把该轮当作成功消息持久化。下一次会话在约 `40,101` 个 conversation tokens 时启动自动压缩，压缩请求重放了这条既无正文又无工具调用的 assistant 历史，DeepSeek 随即以 HTTP 400 拒绝。使用同一 CLI、同一模型和同一 Provider 新建无历史对照会话，只要求回复 `OK`，请求成功，证明问题不在网络、密钥或模型服务整体可用性，而在 reasoning-only 消息的持久化与压缩兼容链。类似故障若发生在连续工具编辑中，会中断后续步骤，并可能在本地工作树留下尚未完成、尚未验证的部分改动。
- 解决方案：
  当前先停止恢复已经包含坏历史的会话，改用全新会话，并通过会话摘要、工作树差异和窄范围测试恢复任务现场。长 Agent 任务暂时控制单会话长度；关键编辑后及时检查 `git diff`、类型检查和相关测试。上游修复方向应是在消息持久化、恢复和压缩前，拒绝、丢弃或规范化 `content` 为空且 `tool_calls` 为空的 assistant 消息；若有效答复只存在于 `reasoning_content`，适配层也不能把它作为空标准消息继续重放。
- 问题影响：
  该问题影响自定义 Provider 下长时间 Agent 工作的连续性、工具调用可靠性和会话恢复能力。它不会直接修改已提交或已发布产物，但可能让一次多步骤编辑停在中间状态；如果操作者把 400 当作普通网络抖动后反复恢复，容易遗漏半成品改动和构建错误。它也说明“兼容 OpenAI Chat Completions”不等于已经覆盖 reasoning、工具调用、自动压缩和恢复等完整 Agent 协议。

当前经验：

- 项目经验：
  评估第三方模型的 OpenAI 兼容性时，不能只做单轮聊天和简单工具调用测试，还要覆盖长上下文阈值、自动压缩、工具调用后的最终答复、会话恢复以及中断后的工作树一致性。出现会话层 400 后，先用同配置的新会话做最小对照，再检查原会话最后一条 assistant 消息和项目工作树，能够快速区分服务故障、历史消息损坏与项目代码问题。
- 模型自查：
  后续 Agent 在接手异常中断的任务前，应先确认工作路径、未提交差异和最近验证结果，不要直接从自然语言“继续”推断代码处于完整状态。若宿主暴露的历史消息同时缺少正文和工具调用，应将其视为协议异常并停止恢复，而不是继续把坏消息带入压缩或下一轮请求。

### 2.2 推理模型内容错位的三层机制——从一次会话崩溃反推

上述 400 错误的根因比"Cli 持久化了空消息"更深一层。我们在排查过程中梳理出 DeepSeek 推理模式下三条叠加机制，对其他使用推理模型的团队有参考价值：

- **推理链连续性要求**：DeepSeek 推理模型要求对话历史中所有 assistant 消息都保留 `reasoning_content` 字段（即使是空字符串）。一旦某条历史消息被中间件剥离了该字段，推理链断裂，模型会把本该写入 `content` 的正文全部错位到 `reasoning_content`，最终返回空的 `content`。
- **中间件无感剥离**：部分 OpenAI 兼容 SDK 的消息转换逻辑会在重放历史时自动丢弃 `reasoning_content`——因为标准 Chat Completions API 没有这个字段。工具调用多轮场景下尤其容易触发。
- **`<think>` 标签的兼容歧义**：有些客户端把推理内容包在 `<think>` 标签里塞进 `content`，和 DeepSeek 要求的字段级结构冲突，间接加剧错位。

**可迁移的经验**：
1. 评估第三方推理模型时，不能只看"兼容 OpenAI API"——要专门测长上下文压缩后的 assistant 消息字段完整性。
2. 如果自己写 Agent Loop，消息持久化前应该校验 `content` 和 `tool_calls` 至少一个非空；流式响应必须消费到 `finish_reason=stop`。
3. 出现 400 后，可以新建同配置空会话做最小对照，快速区分是模型服务故障还是历史消息损坏。

上述经验已同步录入本周待办，其中涉及我们自建 Agent 引擎的防护设计部分已单独路由给产品和技术条线跟进。

## 3. 本周有效做法

- 使用同一 CLI、同一模型、同一 Provider 的全新会话做最小对照，快速排除网络、密钥和模型服务整体故障。
- 同时核对会话事件、压缩触发点与本地工作树，把“协议错误”和“项目实际影响”分开判断。
- 对异常中断后的工程任务执行窄范围类型检查，确认是否留下了新的构建错误，而不是仅凭会话报错推测影响。

## 4. 适合发给社区一起讨论的问题

- 大家在测试 OpenAI 兼容 Provider 时，是否覆盖过 reasoning-only 响应、工具调用和长会话压缩，而不只是普通聊天？
- Agent 宿主是否应该在持久化前强制过滤既无 `content` 又无 `tool_calls` 的 assistant 消息？
- 长会话异常中断后，如何把会话恢复与工作树恢复组合成可自动执行的安全检查？

## 5. 下周续写入口

- 跟踪 Copilot CLI 后续版本是否修复 reasoning-only assistant 的持久化和压缩问题。
- 观察缩短会话、切换模型或新建会话交接是否能稳定规避该问题。
- 如向上游提交 Issue，补充最小复现、版本信息和脱敏后的消息结构。

## 6. 本周维护检查点

- 周开始创建：本文件已于 2026-07-17 补建，当前为 `draft-2026-W29`。
- 周中追加：已追加 2.1，记录 Copilot CLI + DeepSeek 长会话压缩 400 的现场证据、影响和临时规避。
- 周末签发：本周结束前，由维护者升级给 CEO 确认；确认后生成正式版本号，并归档到 `docs/workflow/operating-records/项目级 AI 共学周记/`。
- 自动化状态：周开始自动创建、每轮对话自动判断追加、周六中午自动更新和周末签发提醒尚未实现；当前仍通过 `项目级 AI 共学周记` prompt 手动维护。
