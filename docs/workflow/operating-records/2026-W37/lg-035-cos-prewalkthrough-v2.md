# LG-035 TriMMC UI·COS 预走查记录 v2（升级门首用：真实凭据全交互）

- 执行位: COS（小贾）· 升级版预走查门（真实凭据全交互，前轮教训整改后首用）
- 走查对象: TriModel 8b08427 树（TriMMC 重设计+S10/S11+D1-D8 修复，CTO 06:01 PASS 签发态）本机 3333 部署态
- 时点: 2026-09-12 00:3x-00:5x+0800
- 方法: playwright-core 真浏览器（chromium-1228）＋网络抓包（request/response 全录）＋文件系统断言
- 证据: w1-*/w2-* 截图 9 张＋网络抓包记录（本记录引文）

## 第一遍·设计符合 ✓
- 首启引导框 ✓／连接后「● 已连接」状态点 ✓／「当前生效」只读状态条（当前模型/来源/一致性三行，零输入控件）✓／TriMMC 卡（云图标+【TriMMC 信息】+待应用徽标+「保存后待应用」副注）✓／本机策略折叠区（灰调+「本机改动即时生效」）✓——v3 §2/§3/§5 全对表
- 亮主题（w2-01/02/03）＋暗主题（cos-walkthrough-dark.png）双拍完整渲染

## 第二遍·任务流（真实凭据全交互）——✗ 保存落盘未通（D7 修复未达实锤）
- 真实双 token 连接 → **已连接 ✓**（上轮 CEO 翻车点已修复实证）
- 级联过滤 ✓✓：选 deepseek→模型=[deepseek-flash,deepseek-v4-pro]；选 glm→模型=[GLM-5.3-Flash,GLM-5.3]（CEO 第三条批评的级联已正确实现）
- 添加条目/切当前使用/保存卡片——UI 态全部正常（待应用徽标 ✓、条目入列表 ✓、当前使用选项含新条目 ✓）
- **✗ 致命：trimmc-card.json 落盘失败**——两轮独立验证（清空态重试+重启进程重试）PUT 均 **400**：
  - 抓包：`PUT /v1/config/trimmc-card` body 为完整对象形态（结构正确）→ 响应 `400 {"error":"trimmc card validation failed: rules[0].entry_id 'f2-b-e1' does not reference an existing entry (悬挂引用)"}`
- **D7 修复未达的定量描述**：连接/级联/徽标三条修复 ✓；保存落盘链（D7 核心）✗——**前端「待应用」态与后端 400 并存＝失败仍不静默但前端未阻断显示**（修法②「失败不静默」部分达成：有报错文案；「前端不假绿」未达成：徽标仍翻待应用）

## 第三遍·破坏流
- 禁用按钮视觉：已灰化（上轮次级项核销实证）
- 错令牌：E7 断言绿（人话指引）
- **核心破坏流实锚**：D6 遗留悬挂引用（f2-b-e1）使**一切保存被 400 拒＝死锁态**——防御正确（拒了坏数据）但缺自愈/清理引导（用户无解）

## 核心谜题移交 CTO（代码级）
- **f2-b-e1 态的存储源不明**：文件系统两候选路径（TriModel/trimmc-card.json、D:/Code/ai/trimmc-card.json）均不存在（实勘+实码 trimmc-card.ts:62 候选路径对表）；重启 3333 进程后 400 依旧（非进程内存）；GET /v1/config/trimmc-card 却 200 且含 f2 态（card_file_present 语义 :36 待核）——**f2 态活于何处＝定位 D7 修复为何未达的钥匙**
- 次级：D6 遗留悬挂引用的自愈/清理引导缺位（死锁态无用户出路）

## 结论
- 升级版预走查**拦下第二次不合格 CEO 走查**（真实凭据全交互正是必要门——mock/E1-E8 全绿与此轮翻车并存再次实证）
- 预走查结论：**不通过**（保存落盘链未通）——D7 修复需回炉（含 f2 态存储源谜题定位+自愈引导+400 人话化收尾）
- 走查资产：截图 12 张归档本目录

## 第二轮·API 层定谳（15:4x+0800，绕开前端变量直测后端保存链）
- **实锚 1**：curl PUT（spec v2 正确对象形态：provider_entries=[{id,vendor,model,api_key_encrypted,enabled}]）→ **400 `{"error":"条目数据格式错误，请重新添加条目"}`**——照 CTO spec 写的形态被拒。
- **实锚 2**：GET（同 token）200——返回服务端默认态（provider_entries={}＋machine.name 自动填本机 hostname＋pending），**trimmc-card.json 文件全盘不存在**（TriModel 根/D:/Code/ai 根/LOCALAPPDATA/APPDATA 四路实勘）。
- **定谳：后端校验器实际期望的条目 schema 与三方（CTO spec v2 草案/CPO 增补件/前端表单）均不匹配——「实现即 spec」反模式实锤**。CEO 保存失败/STE F2「API 直灌过」/我的 spec 形态 400——三现象一根因（STE 用例照实码写所以过，spec 和前端照设计写所以挂）。
- 域界收手：校验器实际期望 schema 的提取归 CTO（实码 api/trimmc-card.ts 校验函数逆向成文），本席证据包全交。

## 第三趟·D9 后预走查（16:1x+0800，13ddb5b 树部署态）
- **八条核销进展 ✓✓**：首启空态灰态 ✓（八条①）/真实凭据连接→已连接 ✓（②）/级联双双 ✓✓（③）/待应用徽标 ✓/跨刷新连接保持 ✓/密钥零明文 ✓
- **✗ 唯一剩环：trimmc-card.json 四路+深搜均不存在**（TriModel 根/dist/D:/Code/ai 根/policies/ 空壳/LOCALAPPDATA 深搜超时中断）——但「保存后待应用 ✓」+「跨刷新保持 ✓」并存＝数据活于前端 localStorage（暂存态），**服务端落盘仍未发生**
- 现象集全录（交 CTO 定位存储解析）：UI 待应用✓/跨刷新✓/四路无文件/policies/ 空壳（D9 boot 建）/15:4x API 直灌 spec 形态 400「条目数据格式错误」（CTO 裁=数组形态触发 D7 防退化拒，实现正确，map 键值才是合同——**本席 15:4x「实现即 spec 反模式」定谳撤回**：spec 草案形态错、实现对）
- 域界收手：存储解析实码归 CTO（trimmc-card.ts:62+D9 路径规范化新码），本席证据全交

## 第四趟·13ddb5b 部署态（16:1x+0800）——预走查方法论边界收口
- 部署核验 ✓：现役进程 16:07:29 起=13ddb5b 树（trimmc-card 路由 grep ×1 在、时段规则 ×9 在、D17 badge 绑定 ×6 在——**非回滚，最新全量在役**）
- 矛盾读数：①「保存卡片」点击后**无 PUT 发出**（网络监听空）＋card 落盘 ✗——但「待应用」徽标翻（D17 诚实性应阻止假翻）②时段规则按钮点击后 time 控件=0
- **本席脚本可靠性边界自认**：locator 猜测式定位（nth 序号+正则文本）对新增字段（服务地址）与动态表单的适配脆弱——第四趟「key 填进服务地址框」已实证错位一次；「无 PUT+控件 0」大概率系脚本交互错位（CTO F2/F4 真链 E2E 是通的）但不能排除真缺陷——**无头脚本已不是合格的非作者手测证人**
- **移交裁定**：①CTO/STE 以实码视角判定第四趟现象=脚本错位 or 真缺陷（一记 console.log/一次有头走查即分辨）②「非作者手测」硬门建议回归**有头真人走查**（CEO/CTO/STE 任一非作者），无头脚本降级为回归辅助③本席预走查资产（12+9 张截图/网络抓包/文件断言/现象集）全交
