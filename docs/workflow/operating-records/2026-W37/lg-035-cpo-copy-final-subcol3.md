# LG-035 子栏三「模型切换策略」文案终稿（增补件 4 配套，FSD 照抄）

- sourceOfTruth: 本件（文案正身；控件行为语义以增补件 4 为准，本件只定字符串）
- syncMode: static
- lastSyncedAt: 2026-09-12T12:10+0800

| 位置 | 文案（逐字） | 备注 |
| --- | --- | --- |
| 子栏三标题 | 模型切换策略 | 不变 |
| 归属标注（标题行小字） | 以下策略应用于 TriMMC (sg) | 不变 |
| chips 行标签 | 可切换模型 | |
| chips 行副注 | 来自「模型信息」中已启用的条目 | 灰色小字 |
| chips 空态 | 暂无可切换模型——请在「模型信息」中添加并启用条目 | |
| chip 单个文案 | 条目名（模型名） | 例：sg-main（GLM-5.3） |
| 默认模型行标签 | 默认模型 | |
| 默认模型行副注 | 时间窗之外的时间使用默认模型 | 灰色小字 |
| 默认模型下拉 placeholder | 未设置（使用系统默认） | 清空后即此态 |
| 清除默认按钮 | 清除 | 无确认弹窗 |
| 时段规则区小标题 | 时段规则 | |
| 语义副注（区头一行） | 时间窗内优先使用窗口模型；时间窗之外使用默认模型（时区：北京时间） | 语义+时区合一行 |
| 时段规则空态 | 暂无时段规则——添加后，模型将按时间窗自动切换 | |
| 添加按钮 | 添加时段规则 | |
| 列表表头 | 模型｜时间窗｜启用｜操作 | |
| 表单标题（添加） | 添加时段规则 | |
| 表单标题（编辑） | 编辑时段规则 | |
| 字段标签 | 模型／开始时间／结束时间／启用 | |
| 模型下拉 placeholder | 选择模型（仅已启用条目） | |
| 时间输入 placeholder | 如 14:00 | 双输入各一 |
| 表单按钮 | 保存规则／取消 | |
| 校验：未选模型 | 请选择模型 | |
| 校验：时间格式 | 时间格式应为 HH:MM（如 14:00） | |
| 校验：start≥end | 结束时间需晚于开始时间 | |
| 校验：重叠 | 时间窗与现有规则重叠，请调整 | |
| 尾部保存区 | 保存卡片／重新读取 | 既有不变 |
| dirty 提示 | （有未保存改动，保存后待应用） | 既有不变 |
| 保存成功 | 卡片已保存，等待应用（应用完成后状态变为「已生效」） | 既有不变 |

禁用词表（本区零出现）：固定使用／固定规则／候选池／优先级／压制／回退（用「回落」）／挂载。
标点约定：全角冒号+「」引号+——破折号，与现有页面一致；英文术语仅 provider 内部名与 GLM-5.3 等模型名原样。
