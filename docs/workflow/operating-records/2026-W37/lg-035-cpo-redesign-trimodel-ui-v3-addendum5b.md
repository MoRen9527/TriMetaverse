# LG-035 v3 增补件 5b：策略字段微调（策略目的字段+示例统一）

- sourceOfTruth: 本件（增补件 5 微调；其余结构不动）
- syncMode: static
- lastSyncedAt: 2026-09-12T13:20+0800
- 触发: CEO 勘正（13:12+0800）——「白天生产/夜间省费」是策略目的非策略名

---

## 一、策略字段结构（修正版）

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 策略名 | ✓ | 卡内唯一；如「时段切换策略」 |
| 策略目的 | 可选 | 自由描述；如「闲时用 glm，忙时用 deepseek」——新增字段 |
| 模型条目引用 | 引用式 | **指向「模型信息」子栏已有条目，策略内不新建条目**（默认模型+规则引用的并集即该策略的条目集） |
| 默认模型 | 可空 | 「其余时段」的条目引用，与规则语义自洽 |
| 时段规则数组 | 可空 | 如 14:00–18:00 deepseek，其余 glm=默认承载 |

## 二、统一示例（文档与 UI 文案唯一基准例）

**策略名**「时段切换策略」｜**目的**「闲时用 glm，忙时用 deepseek」｜**条目**=deepseek＋glm 两条（引用模型信息子栏已有）｜**规则**=14:00–18:00 deepseek，其余 glm（默认）。

增补件 5 的「白天生产/夜间省费」示例即起废弃，全文档与走查断言统一用本例。

## 三、UI 文案增量（并入文案终稿）

| 位置 | 文案（逐字） |
| --- | --- |
| 编辑表单字段标签 | 策略目的（可选） |
| 策略目的 placeholder | 如：闲时用 glm，忙时用 deepseek |
| 活动策略详情行 | 目的：<该策略目的>（未设则整行不显示） |
| 策略名 placeholder | 如：时段切换策略 |

## 四、数据形态增量（供 CTO）

```
strategies[sid].purpose: string（可选，缺省不落字段；长度上限 CTO 定，建议 200 字内）
```
其余 schema（strategies 字典+active_strategy_id 可空指针+boot 迁移「当前配置」）照增补件 5 不变。

## 五、走查增补

1. 目的断言：设置后详情行显示；未设整行不显示；编辑表单 placeholder 正确。
2. 引用语义断言：策略内无法新建条目——条目下拉仅列「模型信息」已启用条目（引用式验证）。
3. 统一示例断言：走查脚本按第二节示例构建测试策略（命名/目的/条目/规则四段齐）。
