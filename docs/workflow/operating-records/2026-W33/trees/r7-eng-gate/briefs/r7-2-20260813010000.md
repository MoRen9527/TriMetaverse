# r7-2: 工程门禁独立验证 — TestEngineer 小柯

- **nodeId**: r7-2
- **agent**: TestEngineer (小柯)
- **日期**: 2026-08-13
- **状态**: done

## 做了什么

独立复核 r7-1 的工程门禁修复声明，不依赖小全自证。双仓（TriLC + TriMC）tsc + npm test 全量独立执行。

## 验证矩阵

| 验证项 | 独立执行结果 | 小全声明 | 判定 |
|---|---|---|---|
| TriLC tsc --noEmit | EXIT 0，零 error | 零错误 | ✅ 一致 |
| TriLC npm test | 253 tests / 251 pass / 2 fail | 251/253 | ✅ 一致 |
| TriMC tsc --noEmit | EXIT 0 | 未声明（隐含通过） | ✅ 通过 |
| TriMC npm test | 450 tests / 448 pass / 2 fail | "TriMC 未变（453/455 基线）" | ⚠️ 性质一致，数量有变 |
| 5 个原始失败现状 | 4 修复 + 2 TUI 预存 | 同 | ✅ 一致 |
| 路径污染修正 | 仓库根 trees/ 无残留 | — | ✅ 通过 |

### 5 个原始失败项逐一核对

| 测试文件 | r7-0 基线 | r7-1 后现状 | 独立证据 |
|---|---|---|---|
| heartbeat-runner.test.ts | FAIL | 9/9 PASS | 单独运行 exit 0 |
| r5-2.4-2.5-degraded.test.ts | FAIL (before not defined) | 13/13 PASS | import 已补齐 |
| qa-json-runtime-stub.test.ts | FAIL | 2/2 PASS | 单独运行 exit 0 |
| smoke.test.ts | FAIL | 10/10 PASS | 单独运行 exit 0 |
| tui/components.test.ts | FAIL | 仍 FAIL（预存） | 文件创建于 07-31 (8ac7f5f)，早于 r7-1 |
| tui/components-smoke.test.ts | FAIL | 仍 FAIL（预存） | 同上 |

### TUI 2 fail 归因核实（修正小全声明）

小全 brief 称 2 个 TUI 失败均为"预存 ink-testing-library 缺包"。独立核实结果：

- `components.test.ts`：确实 import ink-testing-library，包缺失 → **归因正确**
- `components-smoke.test.ts`：**归因不精确**。该文件注释声明"Does NOT require ink-testing-library"，实际失败根因是 `yoga-layout` 包缺失（`ERR_MODULE_NOT_FOUND: Cannot find package 'yoga-layout'` imported from `src/tui/ink/_stubs/yogaCounters.js`）。package.json 依赖的是 `yoga-layout-prebuilt`，stub 引用了未安装的包名。

结论：2 个失败均为预存环境问题（测试文件创建于 07-31，早于 r7-1 的 08-12），非 r7-1 引入——小全的核心结论正确，但缺包归因有一半不准确。

### TriMC 2 fail 独立核实

小全声明"TriMC 未变（453/455 基线）"。独立核实：

1. **失败性质 pre-existing 确认**：2 个失败均为 pipeline 测试（`test/pipeline-integration/pipeline.test.ts`），断言 `write_file` 被 subagent tier 拒绝时 reason 含 `requires tier "main"`。实际 reason 是 `requires tier "heartbeat"`——REQ-006 (commit ba2a873, 08-05) 将 write_file 所需 tier 从 main 改为 heartbeat，而测试断言写于 07-26 (06e32e0)。时间线：测试 07-26 < 行为变更 08-05 < r7-1 08-12。r7-1 的 diff 仅触及 `trilc-executor.ts` + `app.ts`，与 tier 权限无关。**pre-existing 确认**。
2. **测试总数变化**：455 → 450。因 agent-core 迁移清理 commit（9111222/182de4d）删除了旧位置重复测试副本（含小柯此前交付的 c8-c9/c10 测试副本），非 r7-1 行为引入。

## 为什么

工程门禁 3.1/3.2 是 TriLC 能力验证清单中全部能力项打勾的前提。r7-1 的修复声明必须经独立执行确认，防止"自证通过"。

## 遇到与解决

- **smoke 测试归因偏差**：小全 brief 说 ink-testing-library，实际 yoga-layout。通过直接运行测试文件获取真实 ERR_MODULE_NOT_FOUND 根因。
- **TriMC 测试数变化困惑**：清理 commit 导致 455→450，需区分"测试数变化"与"失败性质变化"——前者是清理，后者确认 pre-existing。

## 遗留

1. **TUI 测试环境缺包（M3）**：`ink-testing-library` + `yoga-layout` 需安装，或修正 `yogaCounters.js` stub 的包名引用（`yoga-layout-prebuilt`）。
2. **pipeline 测试与 REQ-006 行为漂移（M3）**：`pipeline.test.ts:367` 断言期待 "main"，实际 "heartbeat"。测试需随行为更新，或确认 write_file 的 tier 应为 heartbeat（REQ-006 设计如此）。
3. **dist 同步机制（M3）**：agent-core 迁移至 TriCompany 后，`@tricompany/agent-core` 的 dist 同步仍需手动。CEO 审阅中的公司维度包决策将自然消解此问题。

## 代码变更摘要

无（纯验证节点）。

## checkpoint

- **progress**: TriLC tsc EXIT 0；TriLC npm test 253/251/2（2 fail 均为预存 TUI 环境缺包，其中 smoke 根因是 yoga-layout 非 brief 所称 ink-testing-library）；TriMC tsc EXIT 0；TriMC npm test 450/448/2（2 fail 为 REQ-006 tier 行为漂移预存，r7-1 diff 未触及）；4/5 原始失败已修复；路径污染已清。判定：3.1/3.2 门禁 CONDITIONAL_PASS（2+2 预存失败均为非阻塞环境/测试债问题，非 r7-1 引入）。
- **artifactCommit**: 无（验证节点，无代码变更）
- **resumePoint**: 交接 r7-3 小贾收口判定
