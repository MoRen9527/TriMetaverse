# i3-2 实现任务包（小全，FullStackDeveloper）

> 树：init-collab-i3-project-registry（立项周 W33）
> 依据：briefs/i3-1-20260814.md（技术拆解 P1+P2 全量：注册点 schema §一 / 端点契约 §二 / SSE 事件族 §三 / 链态门 §四 / 幽灵路径 §五 / 任务包 §六 6 项 / 门禁 §七 7 条）
> 日期：2026-08-14（编排层派单）
> 状态：active — 注册点 schema 已冻结供 I4 并行

## 执行顺序（i3-1 brief §六 6 项）

1. 新文件 `TriLC/src/project/project-registry.ts`：schema（§一）+ 原子写 + 主键去重 + 惰性清理 + 内存态缓存（daemon 热更新源）
2. 新文件 `TriLC/src/project/project-link.ts`：六步原子序（§二表）+ GitHub 源克隆链（白名单规范化比对 + 系统凭据管理器）+ 失败分类 + 回滚
3. app.ts 增量：`/internal/v1/projects/{link, claim, inspect}` 三端点（internal 面同构）+ 链态门（仅 chainState=project-link 生效，其他 409）+ 热更新挂载（注册点写 + chain 快照更新同请求）
4. `init-cli-flow.ts` 增量：project-link 文本流程
5. 单测：注册点原子写/主键去重/惰性清理；门禁拒绝；认领绝不重复 add（mock git）；链态门 409；事件族与 status 投影一致；回滚路径（登记失败 → worktree remove 非 --force）
6. 活体：CEO 机本地源全流程（认领 + 建立）+ GitHub 源白名单/门禁——显式 TRILC_DATA_DIR 隔离（r19 教训）

## 降级判断点（i3-2 中段，08-16 上午）

**GitHub 源克隆若未收敛** → 按设计 §2.10 MVP 口径降级为「白名单 + 门禁 + 本地克隆等价路径」（克隆实测挂后续树）——本地源全流程 + 注册点 + 向导分流保交付，**报告编排层走 §8.2 条款 b**（自然触发照常 + 确认后显式触发）。判断时点：i3-2 中段。

## 护栏（违反即停，报告编排层）

- **TriPilot 仓 diff 零行**（本树零改动，workspaceRoot 现役字段够用）
- **git 单执行体**：全部 git 操作在 daemon 端点内单身份（OBS-20260814-002）；无 sudo 无多身份
- **junction 纪律**：hasNpmFileDeps 门禁拒绝自动 add；**`worktree remove --force` 全仓（含测试代码）禁用**（INCIDENT-20260814-001 教训——小柯误删事故即此类）
- **原子性 + 回滚**：add + 登记 + 链态更新同一请求；失败 → worktree remove（非 --force）回滚；认领路径零磁盘写
- **链态纪律**：非 project-link 态 409；转移仅经 transitionTo 线性表（本树不转出）
- **护栏零触碰**：init-state.ts / session-initializer.ts / init-chain.ts（除 project-link 阶段快照写入）diff 零行
- **密钥纪律**：密钥只走 env/config，永不进 git

## 环境提示

- TriLC HEAD=a5cdacf（I2 收官），测试基线 340/341
- 注册点落点 `%LOCALAPPDATA%\trilc\project-registry.json`（固定路径，不随 TRILC_DATA_DIR 覆盖——测试隔离只读无害）
- 链态：I2 assemble 后 = project-link（i2 活体验证路径可复用）；项目仓注册表 MVP = daemon 内置预置表（TriMetaverse 单条目）

## 落盘要求（完成后）

1. 回写 tree-op.json i3-2 node：status=done、checkpoint={progress（改动清单 + 护栏逐项 + GitHub 源收敛判定）、artifactCommit（TriLC commit hash）、resumePoint（i3-3 验证入口）}
2. commit 卫生：TriLC 仓自己 commit + push 裸仓（sg-server，权限拒绝则由编排层补推）；本仓登记自己 commit 或明说「落盘未提交」
3. 最终回复格式：6 项完成状态表｜护栏逐项｜单测/活体结果｜GitHub 源收敛判定（降级与否）｜artifactCommit

## 时间口径

- 08-14/15 内实现，08-16 上午 i3-3 验证 + i3-4 终审；超预期按降级判断点执行
