# r12-2 树后增量验证：BUG-002/003 修复独立复核 — TestEngineer 小柯

- **类型**: 树后增量验证（不属 r12 树原节点，tree-op 不动）
- **agent**: TestEngineer (小柯)
- **日期**: 2026-08-13
- **触发**: r12-2 验证发现的"BUG-003 未修未声明"闭环后，小贾派单轻量独立验证
- **方法**: v0.4.3-r12 产物隔离实例（8717 端口 + 独立数据目录，r12-2 同款），现役 8711 全程未扰动

## 验证矩阵

| 项 | 声称 | 独立验证结果 | 判定 |
| --- | --- | --- | --- |
| BUG-002 三处修复 | 版本注入 + 无 BOM 写入 + env.ts BOM strip 防御 | 三处代码实读齐备：build-desktop.ps1（-Version 提取 + WriteAllText UTF8Encoding($false) 无 BOM）；build-tricade.yml（注入 CI calver 替代复制源码 version.json）；TriLC env.ts resolveVersion（`raw.replace(/^﻿/, '')` BOM strip） | **PASS** |
| BUG-002 端到端 | 安装态 healthz 报 0.4.3（此前恒 1.0.0） | v0.4.3-r12 staging version.json = `{"version":"0.4.3"}`；隔离实例 healthz **version 字段实测 = "0.4.3"** | **PASS** |
| BUG-003 smoke | yoga 别名修复 → components-smoke 30/30 转绿 | components-smoke 独立重跑：**30 tests / 30 pass / 0 fail** | **PASS** |
| BUG-003 残留声明一致性 | "3 ToolCallLine fail + 套件挂起已回滚保持 r7 基线" | 残留实况：components.test.ts 以 `ERR_MODULE_NOT_FOUND: ink-testing-library` 失败（1 fail、**无挂起**、秒级正常退出）；smoke 转绿后 r7 基线已改善（2 挂 → 1 挂），挂点从 yoga 变为 ink 依赖缺失 | **PASS**（D10 表述修正建议） |
| r7 基线不漂移 | TriLC 全量 286/285/1 | 全量独立重跑：286 tests / 285 pass / 1 fail，与登记一致 | **PASS** |

## 差异与观察项

- **D10（登记表述修正建议）**：OP risks 中 BUG-003 登记写"已回滚保持 r7 基线"——实际 r7 基线是 yoga 缺依赖导致 components-smoke + components 两套件挂；现状是 smoke 已修复转绿（基线改善）、components 挂点为 ink-testing-library 缺依赖。建议登记改为"components.test.ts 以 ink-testing-library 缺依赖失败（1 fail 无挂起），归 CARRY-001"
- **O7（CI 写端对齐建议）**：build-tricade.yml 的 version.json 注入仍用 `Set-Content -Encoding UTF8`（PowerShell 5.1 会写 BOM，即 BUG-002 根因链的写端行为）——当前被 env.ts 读端 BOM strip 中和无害；runner 若为 pwsh 7+ 则无 BOM 问题。建议 CI 侧也改 `[System.IO.File]::WriteAllText` + UTF8Encoding($false) 与本地脚本对齐，闭环写端根因
- 附带发现：v0.4.3-r12 产物构建于 r13 迁移前（09:45 < Step 2 09:51），其内置 contracts 为 v2 时代 + 旧 resolver——属构建时点事实，下个产物将带 v3 合同，不属本验证缺陷

## 结论

**PASS** — BUG-002 修复端到端真实有效（0.4.3 实测），BUG-003 修复真实有效（smoke 30/30），残留声明实质一致但表述需按 D10 修正；O7 为 CI 写端防御对齐建议。无阻塞。提权正式安装后的复验由小贾单独安排。

## 证据

- v0.4.3 staging：`output/TriMetaverse-Desktop-v0.4.3-r12-windows/trilc/version.json` = 0.4.3
- 隔离实例 healthz 实测：version=0.4.3（8717，已终止清理；8711 全程 200）
- components-smoke：30/30；components：1 fail（ink-testing-library MODULE_NOT_FOUND，无挂起）
- 全量：TriLC 286/285/1 与登记一致
