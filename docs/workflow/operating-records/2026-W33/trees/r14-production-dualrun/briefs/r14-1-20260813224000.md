# r14-1 工作简报

- **节点**: r14-1
- **agent**: CTO (小狄)
- **树**: r14-production-dualrun
- **完成时间**: 2026-08-13T22:40:00+08:00

## 做了什么

1. **双向心跳契约规格**：`TriCompany/docs/engineering/heartbeat-dualrun-contract.md`（commit `bf36e4c`）——双面状态机 + 统一参数表 + 心跳接线升级 + 恢复对齐 + 落地指引 + 验收口径 8 项。
2. **服务器面只读盘点**（ssh BatchMode 实测，非纸面推断）：服务器可达、TriMC systemd active、8710 监听、部署形态 M1 dev 直跑、服务器落后本地 3 commits。
3. 硬卡五项登记 blocked（见 checkpoint 与规格 §五.2）。

## 契约核心（实测发现的盲区）

- **TriMC 侧节点可达性判定缺失**：heartbeat 端点为回显式（不登记状态），`markNodeUnknown` 零调用方。双向契约当前是单向的——只有 TriLC 知道自己降级。
- **契约参数**：TriLC 现有参数保留（10s 心跳 / 3 败降级 / 2 成恢复 / 5min 退避 / 60s 慢心跳）；TriMC 新增对称参数（stale 30s = 3×interval 与 failThreshold=3 对称；degraded 节点宽松 180s 防慢心跳误判；2 次心跳回归 known）。
- **恢复对齐**：TriLC replay 为主路径（现有）；markNodeRecovered 首批可选。commands 通道留扩展点不启用。

## 服务器面盘点结果

| 项 | 实测 |
| --- | --- |
| ssh sg-ecs-server | 可达 |
| git remote sg-server | 可达，HEAD 182de4d（落后本地 3 commits） |
| trimc.service | enabled + active（08-11 起 1d12h） |
| 部署形态 | M1 dev 直跑（/srv/fleet/TriMC + npx tsx），非生产形态 |
| 监听 | *:8710 |
| 联通面 | TriLC 默认连 127.0.0.1:8710，连服务器需 TRIMC_BASE_URL + 防火墙放行 |

## 硬卡清单（blocked → CEO 一次执行）

1. push sg-server（git 写权限）
2. 服务器 git pull + tsx 重启（服务器操作权限）
3. TRIMC_BASE_URL 指向服务器 + 防火墙放行 8710
4. M3 生产形态改造（dist 构建 + 版本化部署）——记 M3 后续里程碑动作，不入本树
5. UAC 提权安装（r14-2 提权清单）

## 遗留

- 无。r14-2（小全）按规格 §四落地指引执行：TriMC 心跳接线（recordNodeHeartbeat + 超时扫描 + markNodeUnknown 接线）+ 单测 + 全量回归；TriLC 侧核对两项（payload 带 state、replay 幂等）预期零改动。
