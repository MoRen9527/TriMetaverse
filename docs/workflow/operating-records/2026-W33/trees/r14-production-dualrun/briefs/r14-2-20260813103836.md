# r14-2 简报：TriMC 心跳接线 + 生产仓重建 + 双跑 runbook

- 执行人：小全（FullStackDeveloper）
- 树：r14-production-dualrun
- 时间：2026-08-13
- 依据：`TriCompany/docs/engineering/heartbeat-dualrun-contract.md` v1.0（r14-1，commit bf36e4c）

---

## A. TriMC 心跳对称接线（TriMC commit）

**改动**（契约 §4.1 三处接线）：
1. `src/mirror/store.ts`：节点心跳表 `nodeHeartbeats: Map<nodeId, {lastSeenAt, state, consecutiveHbs, unknown}>` + `recordNodeHeartbeat()`（2 次回归 known，与 TriLC recoverThreshold=2 对称）+ `getNodeHeartbeat()` + `scanStaleNodes()` 双阈值扫描（30s 常规 / 180s degraded 宽松）+ 时钟注入参数（测试用）
2. `src/server/app.ts` heartbeat 端点：解析后 `recordNodeHeartbeat(nodeId, state)`——**响应结构不变**（`{ok, serverTime, nodeId, commands: []}`，向后兼容）
3. `src/server/app.ts`：start 挂 10s 扫描定时器（`unref`），stop 清理

**markNodeUnknown 接线**：从零调用 → scanStaleNodes 内调用（验收项 3）。

**单测 5 项**（`test/mirror/heartbeat-node.test.ts`）全绿：
- 心跳登记（表/state/consecutiveHbs）
- 30s 标 unknown（29s 不标边界）
- 180s 宽松（degraded 节点 100s 不误判）
- 2 次回归 known（1 次仍 unknown 边界）
- markNodeUnknown 标非 terminal 任务为 unknown

**全量回归**：460 tests，457 pass，2 fail（r7-2 归因预存 pipeline tier 断言）+ 1 skip（O3 win32），**无新增失败**。455 基线 + 5 新增 = 460 ✓。

## B. 生产仓重建（v0.4.4-r14）

- 产物：`output/TriMetaverse-Desktop-v0.4.4-r14-windows.zip`（58.68MB）
- **contracts 快照 v3.0**：14/14 份 version "3.0"（r13-2 收敛后重建）；version.json 0.4.4（无 BOM，BUG-002 修复在列）；yoga-layout 别名在包
- **安装态实测**（staging 模拟 + TRILC_DATA_DIR 隔离 + 8716 临时端口，不扰现役）：healthz `version: 0.4.4` + `[contract-resolver] loaded 14 agent contracts` + roster 13 + company scope 返回（business-strategy 在列）
- 证据日志：`output/build-r14-20260813.log` + `output/r14-installed-daemon.log`

## C. 双跑 runbook

文档：`docs/execution/production-dualrun-runbook.md`，含：
- 当前形态与产物表（本地产物/现役安装/TriMC 服务器形态/心跳契约参数）
- 部署步骤（本地提权命令 + 门禁行为说明 + 安装后验证命令）
- 双向切换预案（TriMC 不可达→TriLC 续跑：3败降级/5min 退避/replay 恢复；TriLC 挂→TriMC fallback：超时 markNodeUnknown/2 次回归；恢复幂等）
- 回滚预案（trilc.bak 还原 + 降级门禁放行 + TriMC revert + 误判风暴观察项）
- **blocked 批量清单五项**（契约 §5.2）：push sg-server / 服务器 git pull+重启 / TRIMC_BASE_URL+防火墙 8710 / M3 生产形态改造记后续 / UAC 提权
- r14-3 验收核对点 8 项

## D. TriLC 侧核对（零改动确认）

实读 `src/server/app.ts` ConnectionManager：
| 契约参数 | 规格值 | 实读值 | 结论 |
| --- | --- | --- | --- |
| healthCheckIntervalMs | 10_000 | `opts.intervalMs ?? 10_000` | ✓ |
| failThreshold | 3 | `= 3` | ✓ |
| recoverThreshold | 2 | `= 2` | ✓ |
| DEGRADED_BACKOFF_MS | 300_000 | `5 * 60 * 1000` | ✓ |
| DEGRADED_SLOW_INTERVAL_MS | 60_000 | `60_000` | ✓ |
| 心跳 payload state | 带 state | `state: this.state`（385/506 行） | ✓ |
| 恢复 replay 幂等 | 幂等 | 2.5 已验（r5） | ✓ |

**零改动成立**（只核对不改）。

## Commit 清单

| 仓库 | Commit | 内容 |
| --- | --- | --- |
| TriMC | `feat(r14-2)` | 心跳对称接线（store + app + 5 单测） |
| TriMetaverse | `build(r14-2)` | v0.4.4-r14 产物 + 安装态证据日志 |
| TriMetaverse | （收尾 commit） | runbook + 简报 + tree-op |

## 给 r14-3（小柯）的验证提示

1. 契约参数两端实读一致（TriLC app.ts:300-303 实读 + TriMC store.ts scanStaleNodes 默认值）
2. TriMC 单测 5 项全绿（`test/mirror/heartbeat-node.test.ts`，时钟注入参数 now 说明测试确定性）
3. `grep -n "markNodeUnknown" src/` 应有调用方（store.ts scanStaleNodes 内）
4. 心跳响应向后兼容：curl 端点实测 `{ok, serverTime, nodeId, commands:[]}` 结构与 r14-1 前一致
5. TriMC 全量 457/460：2 fail 预存（subagent tier write_file 断言漂移，r7-2 归因）+ 1 skip（O3 win32）
6. TriLC 零改动：git status TriLC 应无未提交变更（r14-2 未动 TriLC 仓）
7. 服务器面清单：runbook §一表与 r14-1 盘点（ssh 可达/systemd active/8710 监听/落后 3 commits）一致
8. blocked 五项在 runbook §五完整登记

## 技术债务标记

1. **心跳误判风暴无运行时开关**：scanStaleNodes 双阈值无环境变量覆盖；若生产出现误判，需临时改代码或重启——建议 M3 生产形态改造时补 `TRIMC_HEARTBEAT_STALE_MS` 环境变量
2. **markNodeRecovered 未实现**（契约 3.4 可选增强）：恢复对齐靠 TriLC replay 主路径，TriMC 回流标记留 M3 后续
3. TriMC 服务器仍为 M1 dev 直跑形态（tsx），心跳接线在服务器生效需 blocked #1/#2 解锁
4. 现役安装（26.08.05.1）与新产物（0.4.4）版本命名空间双轨（ARP calver vs trilc semver）——安装时 -Force 语义说明见 runbook §2.1

## 使用依据

- `TriCompany/docs/engineering/heartbeat-dualrun-contract.md`（r14-1 交付）
- TriMC `src/mirror/store.ts` + `src/server/app.ts`（r13-2 后状态）
- TriLC `src/server/app.ts` ConnectionManager（实读核对）
- r12-1 交付的版本门禁/备份逻辑/提权清单（runbook 复用）
