# r14-3: 心跳接线 + 产物 + runbook 独立验证 — TestEngineer 小柯

- **nodeId**: r14-3
- **agent**: TestEngineer (小柯)
- **日期**: 2026-08-13
- **状态**: done

## 做了什么

按契约 `heartbeat-dualrun-contract.md` v1.0 §六 8 项验收独立重测：TriMC 心跳接线三处代码实读 + 单测/全量独立重跑；v0.4.4-r14 产物隔离实例实测；runbook 逐步可执行性核对；TriLC 零改动独立核对；服务器面事实用 ssh/ls-remote 独立实测。

## 验证矩阵

| 项 | 口径 | 独立验证结果 | 判定 |
| --- | --- | --- | --- |
| A.1 代码三处 | store.ts 心跳表 + recordNodeHeartbeat（2 次回归 known）+ scanStaleNodes 双阈值（30s/180s，时钟注入）；app.ts 端点登记 + 10s 定时器（unref + stop） | 实读全部确认：NodeHeartbeatRecord（lastSeenAt/state/consecutiveHbs/unknown）；recordNodeHeartbeat `unknown && consecutiveHbs>=2 → known`（时钟注入 now 参数）；scanStaleNodes `state==='degraded' ? 180s : 30s`（时钟注入）；端点响应结构不变（{ok, serverTime, nodeId, commands}）仅加登记副作用；定时器 setInterval 10s + `unref?.()` + 停服 clearInterval | **PASS** |
| A.2 markNodeUnknown | 零调用 → 有调用方 | 定义 store.ts:175（非 terminal 任务标 unknown）；调用方 scanStaleNodes store.ts:88 ✓ | **PASS** |
| A.3 测试 | 单测全绿 + 全量 457/460 无新增 | 心跳单测 test/mirror/heartbeat-node.test.ts **5/5 绿**；全量独立重跑 **460 tests / 457 pass / 2 fail（r7 归因预存 pipeline tier 断言）/ 1 skip（O3 win32）/ 0 cancelled**——与声称精确一致，无新增失败 | **PASS** |
| B 产物 v0.4.4-r14 | healthz 0.4.4 / 14-14 v3 / family Registry / forbidden 6 | ZIP 存在（58,681,442 字节）；version.json=0.4.4；**隔离实例（8719）实测 healthz version=0.4.4**；日志 "loaded 14 agent contracts" ✓；business-strategy family=**Registry** ✓（裁决已落地）；test-engineer forbidden **6 条** ✓（含 v1 回补条款） | **PASS** |
| C runbook | 可执行性逐步核对 | §2.1 命令/路径/门禁行为与 5.4 已验逻辑一致；§3 双向切换与代码行为一致（recordNodeHeartbeat 幂等实读）；§4 回滚四场景可执行（误判风暴无开关已如实登记观察项）；§5 blocked 五项齐全；§六 8 核对点与契约 §六对齐 | **PASS**（差异 D11） |
| D TriLC 零改动 | git status 独立核对 | 清理我方遗留 err1.txt 后 **CLEAN**，HEAD 仍为 r13-2 的 25439ca——小全"TriLC 未动"成立 | **PASS** |
| E 观察项 | agent-core test/ 进发布包 | v0.4.4 ZIP 内 `node_modules/@tricompany/agent-core/test/` **9 文件**——小狄发现复核确认，无害但应 exclude | **确认** |
| 服务器面事实（契约 §5.1/§六7） | ssh 可达 / systemd active / 8710 / 落后 commits | ssh sg-ecs-server BatchMode 实测：trimc.service **active + enabled** ✓；TriMC fetch 实测：**本地领先 4 commits、远程领先 0**（runbook §5 "3+ commits" 口径准确；契约 r14-1 盘点的具体 SHA 182de4d 已滚动过期属正常） | **PASS** |

## 差异与观察项

- **D11（runbook 一处路径不精确）**：§4 回滚场景 1 写"手动 node dist/index.js"——实际 RegRun 启动项为 `node.exe "...\dist\cli.js" start`（r12-2 D7 同型，cli.js 非 index.js）。实质动作（RegRun 自动拉起或手动重启）成立，建议修订为 dist/cli.js start
- **E（确认转登记）**：agent-core test/ 9 文件进包——建议 build-desktop.ps1 hygiene 或 CI assemble 加 exclude（或 agent-core package.json files 字段），随下次构建处理
- 附带：v0.4.4 安装态 company scope 未单独重测（小狄已验三件，我复跑关键项：healthz 0.4.4 + 14/14 实载 + family + forbidden，未全量重跑其三件——符合派单口径）

## 总体结论

**PASS** — 契约 §六 8 项验收全部独立复核通过：心跳对称接线真实有效（双阈值/2 次回归/时钟注入/向后兼容），产物 v0.4.4 四项关键项实测通过，runbook 可执行，TriLC 零改动成立，服务器面事实与盘点定性一致。D11 为 runbook 路径表述小疵，E 为产物排除建议。建议 r14-4 收口时：D11 修订 + E 转 exclude 登记；服务器面盘点 SHA 滚动更新随下次盘点。

## 为什么

- 心跳验证不只看测试——三处代码逐行实读（含 unref/stop 清理、时钟注入可测性、响应结构向后兼容）
- 服务器面事实用 ssh BatchMode + git fetch 独立实测，不采信盘点声明
- 产物验证用隔离实例（8719 + 独立数据目录），现役 8711 全程未扰动

## 代码变更摘要

| 文件 | 变更 | 意图 |
| --- | --- | --- |
| briefs/r14-3-20260813104603.md | 本简报 | 验证结论与差异明细 |
| trees/r14-production-dualrun/tree-op.json | r14-3 status=done + checkpoint | 交接 r14-4 收口 |
