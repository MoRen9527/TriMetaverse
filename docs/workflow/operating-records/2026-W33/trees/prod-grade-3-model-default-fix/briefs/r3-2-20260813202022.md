# r3-2 工作简报（独立验证）

- **节点**: r3-2
- **agent**: TestEngineer (小柯)
- **树**: prod-grade-3-model-default-fix
- **完成时间**: 2026-08-13T20:20:22+08:00（编排层机器时间口径）
- **判定**: **验证面 1 PASS；验证面 2 环境阻塞（非树回归）；验证面 3 随 v0.4.6-r16 装后验收执行**

## 一、验证面 1：模型名全链一致 —— PASS

- **修复点抽查**：TriLC ff725ef（16 处名字面量：pro 8 + flash 8，8 文件——key-cache 兜底 / local-node / app.ts 六处默认 / cron timer / heartbeat 三处 / compact / TUI useChat / criticalFallbacks 名单）+ TriPilot ea3936f（4 处：tripilot-cli DEFAULT_MODEL / extension 偏好列表 3 条目 / welcome-setup 两处）——diff 逐点核对，全部为默认值/名单位置，零路由逻辑改动 ✓
- **残留扫描**：TriLC + TriPilot `grep deepseek-v4`（任意形式，排除 tmv- 前缀）**零残留** ✓
- **注册表事实**：TriModel 注册表含 tmv-deepseek-v4-pro/flash/chat/reasoner；活体 `/v1/models` 返回同四条目 ✓
- **活体铁证**：源码态冒烟 daemon 日志 `[trilc] /v1/messages model=tmv-deepseek-v4-pro`——不传 model 的请求经修复后的默认值位置解析为 tmv 名，**无 "not in registry" 错**（本次事故的原始症状在默认值位置已消除）✓
- **观察项 O-R3-1**（转小狄通道，与 FALLBACK_MAP 同批）：TriModel 自身 `src/api/keys.ts` DEFAULT_MODEL 仍为 'deepseek-v4-pro'——注册表 legacy 条目尚可解析故无立即故障，但一致性上应同步 tmv-*；不在本树范围（本树只动 TriLC + TriPilot）。

## 二、验证面 2：chat 冒烟（真实模型调用）—— 环境阻塞（非树回归）

- **方法**：源码态 daemon（TRILC_PORT=8712 + 独立 TRILC_DATA_DIR，不干扰安装版 8711）→ POST /v1/messages（不传 model，命中修复默认值）。
- **结果**：请求到达模型调用层，模型名解析正确（model=tmv-deepseek-v4-pro，registry check 7 models fallback chain ok）；随后 `tmv-deepseek-v4-pro failed (depth=0), trying stream fallback tmv-deepseek-chat` → 返回 `empty_assistant_message`。
- **根因（环境）**：冒烟 daemon 日志 `[trilc:keys] no cached keys and fetch failed — chat disabled`——本机模型服务链全断：**TriModel(3333) / TriStaciss(8000) / TriMC(8710) 均无监听**（netstat 仅 8711 在听）；我的 shell 无 DEEPSEEK_API_KEY/ANTHROPIC_API_KEY；TriModel 仓无 .env（仅 .env.example）。密钥取不到 → chat disabled → 模型空回复。
- **定性**：失败点在密钥/提供方层，**名字修复链已验证通到提供方边界**——非本树回归。修复树的价值路径（默认值解析）在活体日志中已实证。
- **恢复条件**：编排层拉起 TriModel/TriStaciss/TriMC（或确认其托管位置）后，我可 2 分钟重跑同脚本完成真实模型调用冒烟（冒烟脚本与数据目录已备，daemon 已停、端口已释放、scratch 已清）。

## 三、验证面 3：装后 TriCade 可用性 —— 随 v0.4.6-r16 装后验收执行（派单口径）

## 四、纪律核销

- 只读验证：冒烟 daemon 独立端口 + 独立数据目录，安装版 daemon(8711) 与 TUI 进程零触碰；冒烟后进程已停、scratch 已清。
- 无路由/逻辑改动复核：两 commit 均纯名字面量。

## 五、使用依据

- tree-op.json + r3-0 brief + r3-1 checkpoint（routedInput 摘要）
- TriLC ff725ef / TriPilot ea3936f diff 全文
- TriModel src（registry/models.ts、client.ts、keys.ts）
- 活体：源码态 daemon 日志（D:/Code/ai/.r3-2-smoke-daemon.log）、/v1/models、netstat 端口核查
