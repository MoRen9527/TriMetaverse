# r1-3 工作简报（独立验证）

- **节点**: r1-3
- **agent**: TestEngineer (小柯)
- **树**: prod-grade-1-trimc-weekly-cron
- **完成时间**: 2026-08-13T17:09:13+08:00（编排层机器时间口径）
- **判定**: **CONDITIONAL_PASS** —— 代码与活链四验证面全部实测通过；两个阻塞项均为服务器 ops 配置（非代码缺陷），修复命令已 scratch 实证有效，修复后即 PASS。

## 一、测试判断

scheduler 代码、共享 core 消费面、五段链、写路径机制全部验证通过；**周日 23:00 自然触发的完整自动化当前不成立**：真裸仓 push 段被 safe.directory 拦截（必败）+ 5 个锁定 loose-object 目录（概率败）。迁移本体（python 五段链 + commit）不受影响，失败后可 root 手动补 push 兜底。阻塞项修复责任在小贾侧（编排层 ops），不重验代码。

## 二、测试策略

- 四验证面：①scheduler 触发链路 ②五段链迁移正确性 ③周平面文件写路径 ④deadline 前可触发性。
- 方法：代码走读 + 36 用例门禁 + 第二方法交叉验证（croner 直调 vs 手算 epoch；ISO 周双算法）+ 服务器活链实测（测试 job 经真 HTTP API 全链路，测试根 --sync，**未触碰真根、未跑真 job**）+ scratch 隔离环境验证 git 段机制 + 只读探针核查真裸仓/真克隆权限。
- 安全边界：真 job 全程只读（list/status/healthz）；真迁移零触发；真裸仓探测均为可逆/只读（probe ref 未创建即失败退出，touch 探针无落盘）。

## 三、测试结果

### 3.1 验证面① 触发链路 —— PASS

- nextRunAtMs：croner 直调与手算 epoch 双方法一致 = 1786892400000 = 2026-08-16T15:00:00Z = 周日 23:00 Asia/Singapore，与真 job jobs.json 完全一致。
- week token：触发时刻（周日 08-16 23:00）→ to=W34/from=W33/start=2026-08-17；周一~周六补跑仍 W34；下周日转 W35（单测 10 用例 + 独立 UTC 算法复核 + 活链实测 token 替换 W32→W33 精确）。
- stale-run 恢复：service.start() 重置 runningAtMs≠null（代码走读 + 单测「start() resets stale runningAtMs」）✓。
- degraded 聚合：max(consecutiveErrors)≥3（代码 + 单测 + 活体 status {degraded:false, consecutiveFailures:0}）✓。
- 隐式 catchup：JobExecutor 首 tick 执行 nextRunAtMs≤now 的 due job（代码确认）。
- 活链全链路（我的独立实测，测试 job 1fe11fda，已删）：HTTP add → run → token 替换 → runuser fleet(uid 1001) → python3.8 → exit 0 → per-run 日志（/var/lib/trimc/cron/logs/，0600）→ log 端点审计 → jobs.json 持久化 → 删除后状态还原。✓

### 3.2 验证面② 五段链（测试根 --sync）—— PASS（1 个数据质量缺陷）

- 测试根 /tmp/r1-3-testroot（fixture：W32 index active + doneTreesThisWeek 1 树 + unresolved-items）实测 exit 0：
  - create：W33 index 全字段、active、latestActiveWeek ✓
  - migrate：doneTreesThisWeek → doneTreesFromPreviousWeek ✓；W32 retire（closed、latestActiveWeek=false、nextWeekRef）✓
  - carry_over：继承自/平移日期头改写 ✓；.shift-ade.json status=pass ✓
  - validate + agent_close：pass ✓
- **幂等重跑实测安全**：重跑 exit 0，doneTreesFromPreviousWeek 无重复（previous_done 取自源周不累积），runbook「重复跑安全」数据级成立 ✓。
- **缺陷 D1（非阻塞，TriCompany 脚本既有）**：carry_over 周数 bump 正则 `(CARRY-\d+[^|]*\|\s*)(\d+)w\+` 只命中短格式行（| CARRY-001 | 6w+ |）；§1 主表长格式行（| `CARRY-…` | 标题 | 来源 | 负责人 | 6w+ | 状态 |）因中间列被管道截断**静默不 bump**。实测：fixture §4 行 6→7w+ ⚠️ 正常、§1 行 6w+ 不变；真实数据同证（08-10 W32→W33 迁移前后 §1 行均为 6w+ ⚠️⚠️）。后果：4w/8w 自动升级与 8w+ 提取对 §1 主表失效。本树复用脚本（r1-1 §2.3），修复需 TriCompany runtime 单独变更通道。

### 3.3 验证面③ 写路径 —— 机制 PASS，真裸仓 BLOCKED

- fleet `git add` 真克隆实测可写（lock+rename 经 fleet 属主 .git 目录，探针文件成功 staged 后已清）✓。
- commit 内联身份 scratch 实证：author=committer=TriMC Scheduler <trimc@tri.company> ✓。
- push 机制 scratch 全真模拟（root 属主裸仓 g+w + fleet 全局 safe.directory）：exit 0、ref 更新、小增量 loose 对象以 fleet 身份安装 ✓ —— 修复方案有效性已实证。
- **阻塞 B1（周日必败）**：fleet push 真裸仓被 git 2.43.7 safe.directory 拦死（"detected dubious ownership"，exit 128）。r1-2 全链测试止步于 python dry 段（exit 1 → && 短路），**git push 段从未在真裸仓实测过**（brief 仅登记 add/no-op 两项）。修复（已 scratch 验证）：`runuser -u fleet -- git config --global --add safe.directory /srv/git/TriMetaverse.git`。注意：模板内 `-c` 内联不生效（receive-pack 子进程不继承，已实证），必须全局 config。
- **阻塞 B2（周日概率败）**：真裸仓 5 个 loose-object hash 目录（03/79/7c/3a/90）root:root 无组写（touch 探针 Permission denied 实证）。周日小增量 push（<100 对象 → loose 解包，scratch 已证该路径）对象 hash 落入即败，约 8-15 对象 → 概率 ~10-25%。修复：`find /srv/git/TriMetaverse.git/objects -maxdepth 1 -type d -not -perm -g=w -exec chmod g+w {} +`。**每次 root push 后复发**，需登记为常态步骤（runbook 增补或 post-receive hook）。
- **风险 R1（边缘）**：fleet 克隆 .git 内 26 个 root 属主文件（含 index/refs）+ 1 个 root 属主 object dir（3a）——root pull 复发所致。git add/commit 有 ~3% 概率撞 3a 失败。修复：重跑 runbook §2.3 `chown -R fleet:fleet /srv/fleet/TriMetaverse/.git`；后续 pull 改用 `runuser -u fleet` 或 pull 后必跑 chown。

### 3.4 验证面④ deadline 前可触发性 —— 就绪（受 B1/B2 约束）

- 真 job（b00b0070）：enabled、idle、nextRunAtMs=1786892400000、runCount=0，payload 与 r1-1 §5.1 模板逐字一致（python3.8 / /srv/fleet 路径 / runAs fleet）✓。
- trimc.service：active + enabled（重启存活）、Restart=on-failure、drop-in TRIMC_CONFIG_DIR=/var/lib/trimc ✓。
- 服务器：+0800、python3.8=3.8.17（/usr/bin/python3.8）、fleet uid/gid 1001、JobExecutor 最大延迟 60s → 23:00 后 ≤60s 触发 ✓。
- 三端 dev 同步：本地=裸仓=舰队克隆=4a0cb7bd，周日回流无分叉风险 ✓。
- 兜底链完好：B1/B2 未修时自然触发在 push 段失败，迁移文件已写已提交，可 root 手动补 push（或手动 run job 后 root push）。

### 3.5 非阻塞观察（汇总）

| # | 项 | 说明 |
| --- | --- | --- |
| O1 | 服务器 notify 为 render_only | fleet 无 SMTP 配置（/home/fleet/.trimetaverse/notify.json 不存在、服务 env 无 NOTIFY_*）→ 设计 §4.4「投递邮件通知」实际不投递；审计由 .shift-ade.json + git 提交承担。需小贾确认接受或补配置。 |
| O2 | 控制台审计进 /tmp/trimc-run.log 而非 journal | trimc-start.sh 重定向 stdout；设计称「同时进 journal」。per-run 文件日志 + log 端点正常；run log 无轮转，建议 runbook 补。 |
| O3 | agent-core stagger 启发式误判 | "0 23 * * 0" 被 TOP_OF_MINUTE_CRON_RE 判为 sub-hourly → stagger 0。结果恰好符合本 job 意图（23:00 精确），但启发式对日/周级定点模式有误判，走共享包变更通道登记。 |
| O4 | pipeline-integration 2 个既有失败已被 b7eeb50 修复 | CTO 确认纯测试断言对齐（write_file 最低 tier 由 main 改 heartbeat 为 REQ-20260805-006 刻意变更），零行为变更。r1-2 brief 的观察项可关闭。 |
| O5 | 隐式 catchup 边界 | 周日宕机、W34 窗口内恢复 → 补跑 token 正确 ✓；宕至 W35 才恢复 → migrate 因 W34 index 缺失 exit 1 响亮失败（不静默腐化），需人工处置。 |

### 3.6 补充验证点（编排层补派，2026-08-13 并入）

| # | 补充点 | 结果 |
| --- | --- | --- |
| S1 | restart 三查之 ③：GET /internal/v1/cron/status 返回 200（①② 小全 P4 已验：journalctl 无启动错 + healthz cron 块出现） | **HTTP=200**，body `{"ok":true,"status":{"running":true,"degraded":false,"consecutiveFailures":0,"jobCount":1}}` ✓ |
| S2 | TIER 小改单票复核：npm test 全绿 + git show b7eeb50 确认纯测试改动 | 全量 490/491 全绿（1 skip 既有）✓；b7eeb50 diff 实测 **5 行**（两处 hunk）全在 test/pipeline-integration/pipeline.test.ts：测试标题/注释/断言串 "main"→"heartbeat"，零行为变更 ✓。CTO 语义认定（REQ-20260805-006 刻意变更）与 diff 一致，单票通过 |

Python 兼容性（A' 落地态 + 3.11 备用 + 两级回滚）CTO 已正式认定 APPROVE，按补派指示不再重验。

### 3.7 兜底复核票（修复后只读复核，2026-08-13 补记）

编排层执行 B1/B2/R1 修复后，按派单只读复核（不重启服务、不动真 job、不触发迁移）：

| # | 复核项 | 结果 |
| --- | --- | --- |
| B1 | fleet 全局 safe.directory + dry-run push | config 已含 `/srv/git/TriMetaverse.git`（重复登记两条，功能无碍、可去重）；`push --dry-run` → **Everything up-to-date，exit 0，无 dubious 拦截** ✓ |
| B2 | 裸仓锁定目录计数 + 写探针 | `find … -not -perm -g=w \| wc -l` = **0** ✓；原锁定目录 03 写探针（touch + rm）**PROBE_WRITE_OK** ✓ |
| R1 | 克隆 .git root 属主文件计数 | **0** ✓ |

**复核票闭环**：三修复实态确认，周日 23:00 自然触发链完整成立（代码层 PASS + ops 配置就绪）。残留提醒：B2/R1 会在后续 root push/pull 后复发（机制性），runbook 增补常态步骤落地前，每次 root push/pull 后需重跑对应 sweep/chown。

## 四、质量门禁评估

- TriMC 仓全量：**491 用例 490 过 0 败 1 skip**（含 cron 36 用例全绿）；`npm run check` clean；`scripts/validate.mjs` verdict PASS（typeCheck ✓ allTests ✓ minTestCount ✓）。
- 纪律核查：agent-core 零改动（TriCompany 工作树干净）；TriMC 工作树干净；代码仅 r1-2 的 7ba75db/8c612de/579162f/d3fe63e + b7eeb50（测试修复）。
- 门禁结论：**代码层 PASS**；交付层 CONDITIONAL_PASS（B1/B2 为服务器 ops 配置，修复后自然触发链完整，无需重验代码）。

## 五、给 r1-4 的收口动作建议

1. 执行三修复（B1/B2/R1 命令见 §3.3），建议顺手验证：`runuser -u fleet -- git -C /srv/fleet/TriMetaverse push --dry-run /srv/git/TriMetaverse.git HEAD:dev`（dry-run 不再报 dubious 即 B1 已修）。
2. 周日 08-16 23:00 自然触发（主路径）；cron 未触发则 `trimc cron run b00b0070-2f82-4e7d-a98` 兜底；次日本地 `git pull sg-server dev` 回流 + diff 审查。
3. runbook 增补：root push 后裸仓 g+w 常态步骤；fleet pull 或 pull 后 chown；/tmp/trimc-run.log 轮转。
4. D1（carry_over bump 正则）转 TriCompany runtime 变更通道；O3（stagger 启发式）转 agent-core 变更通道。
5. 回写 TriMC/docs/registry/code-state.md（src/cron/ 新模块 + src/cli.ts 登记，r1-2 遗留）。

## 六、使用依据

- tree-op.json + r1-0/r1-1/r1-2 briefs（范围、方案、实现声明）
- TriMC 仓 src/cron/ 五文件 + src/cli.ts + app.ts/env.ts 装配（代码走读）
- TriCompany/packages/agent-core/src/scheduler/（cron-engine/job-executor/job-store/stagger）
- TriCompany/runtime/cognition/weekly_plane_shift.py + weekly_plane.py + notify.py（五段链契约）
- 服务器实测：sg-ecs-server（jobs.json、healthz、cron endpoints、journal、/tmp/trimc-run.log、权限探针、scratch 环境、活链测试 job）
- 真实数据佐证：d4cc5c26（08-10 W32→W33 迁移提交）前后 unresolved-items 行对比
