# r1-3 延伸验证简报（D1 验证票）

- **节点**: r1-3 延伸（D1 验证票，小贾批准执行链 / 编排层转达）
- **agent**: TestEngineer (小柯)
- **树**: prod-grade-1-trimc-weekly-cron（验证面延伸）
- **完成时间**: 2026-08-13T17:29:59+08:00（编排层机器时间口径）
- **结论**: **PASS**（afdae93，四检查项全过，附 3 项观察）

## 一、① diff 审查（afdae93 与两段式规格一致性）

- 变更范围：TriCompany `runtime/cognition/weekly_plane_shift.py` 1 文件 +28/-12，仅 `shift_carry_over` bump 逻辑。
- 与规格一致点：守卫收窄为 `|` 开头且含 `CARRY-\d+` 的表行（散文/非 CARRY 表行/RISK 行天然排除）；`_WEEK_CELL_RE` 吞计数格旧标记按新计数重算（计数=单一真源）；行级处理（列序无关，§1 第 5 列计数正确命中）；`count=1`（首 token 原则）；行数守恒（splitlines/join）。
- 两段式边界正确：**A 段职责全部在脚本内**（bump 机制修复）；**B 段职责显式留给编排层**（数据校正，脚本不越界改数据——commit message 声明的 transiently downgraded + part B checklist 与实现一致）。
- Python 3.8：`ast.parse(feature_version=(3,8))` 通过；`re.Match` 注解在 `from __future__ import annotations` 下安全。

## 二、② 单测（§1/§4 两型 fixture）

- **事实**：TriCompany HEAD（afdae93 及后续）无 weekly_plane 自动化单测文件——本票以独立两型 fixture 实测为事实证据（见 §3 方法一）。
- 建议：两型 fixture 落为自动化测试（小狄/小贾裁量，截止线内可不阻塞）。

## 三、③ 测试根 dry 链（不碰真根）

- 全新 fixture 无 --sync 实测：`dry_run=true`；carry_over → `would-write` + `bumped:true`（bump 逻辑 dry 模式生效）；**零写入**（2026-W33 目录未生成）。
- 整体 exit 1 为从零 dry 既有行为（migrate/validate/agent_close 因 W33 未写而 fail），非本修复引入。

## 四、④ 数据校正口径（W33 §1 计数对齐 §4 实效值——编排层触发前 checklist 项）

**对齐值（供编排层执行，写权在编排层）**：

| 行 | 现值 | 校正为 | 说明 |
| --- | --- | --- | --- |
| §1 `CARRY-20260728-001` | 6w+ ⚠️⚠️ | **7w+ ⚠️** | 对齐 §4 实效值 |
| §1 `CARRY-20260728-003` | 6w+ ✅ | **7w+ ⚠️** | ✅ 同步清除（闭合信息留状态列） |
| §1 `CARRY-20260728-004` | 6w+ ⚠️⚠️ | **7w+ ⚠️** | 同上 |
| §1 `CARRY-20260728-005` | 6w+ ⚠️⚠️ | **7w+ ⚠️** | 同上 |
| §1 `CARRY-20260728-006` | 6w+ ⚠️⚠️ | **7w+ ⚠️** | 同上 |
| §1 `RISK-20260728-001` | 2w | 不动 | RISK 异型另案（无 w+） |

- 依据：08-10 迁移（d4cc5c26）§1 漏 bump 一次（6w+→6w+），§4 短行正常（6→7）——§1 恰落后一档；§4 头注「W33(8/10) 结束即 8 周」确认 W34 实效为 8w+。
- **校正后周日实跑仿真已验**（scratch 副本 + 校正 + --sync）：exit 0、两表同至 **8w+ ⚠️⚠️（10 行 = §1 五 + §4 五）**、escalation_8w = **10/10**、ADE pass。
- 注意：校正后周日提取清单为 10 条且 §1/§4 ID 重叠（同一事项两行），CEO 裁决时按 ID 去重阅读即可；§4 行自带建议字段，可作裁决依据。

## 五、两法重验证据（详见 r1-3 主 brief 与回报）

- 方法一（两型 fixture --sync）：§1 长行 6w+ ⚠️⚠️→7w+ ⚠️、6w+ ✅→7w+ ⚠️（闭合信息留状态列）；§4 短行 7→8w+ ⚠️⚠️ 无脏叠加；RISK「2w」/非 CARRY 行/散文行零变更；行数守恒；escalation 正确。
- 方法二（真实 W33 → 临时 W34）：§1 五行 6→7w+ ⚠️、§4 五行 7→8w+ ⚠️⚠️、变更行恰 12、散文零变更、8w 提取 5/5、W34 index 迁移字段正确、真源零污染。

## 六、观察项（不阻塞 PASS）

| # | 项 | 处置建议 |
| --- | --- | --- |
| O-D1-1 | splitlines/join 使产物丢失末尾换行（130 行内容全守恒，wc 计 129） | 可选 1 行补丁或调整守恒检查口径 |
| O-D1-2 | Windows GBK 控制台 print 崩溃（写操作完成后发生，服务器 UTF-8 无碍） | 本地手工跑加 `PYTHONIOENCODING=utf-8`（已实证有效） |
| O-D1-3 | 自动化单测缺失 | 见 §二，小狄/小贾裁量 |

证据留存：`D:/Code/ai/.r1-3-d1-test/`（fixture / realdata / dry / corrected 四组 + stdout + diff）。

## 使用依据

- TriCompany afdae93（diff 全文）+ git log/d4cc5c26（08-10 迁移前后对照）
- 真实 W33 文件（TriMetaverse/docs/workflow/operating-records/2026-W33/）
- weekly_plane_shift.py / weekly_plane.py / notify.py 源码
- 派单口径（小贾 D1 验证票 + 编排层转达四检查项）
