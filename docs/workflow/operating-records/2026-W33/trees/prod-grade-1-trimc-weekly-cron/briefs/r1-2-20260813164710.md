# r1-2 工作简报

- **节点**: r1-2
- **agent**: FullStackDeveloper (小全)
- **树**: prod-grade-1-trimc-weekly-cron
- **完成时间**: 2026-08-13T16:47:10+08:00（编排层机器时间口径）

## 做了什么

按 r1-1 方案（§七实现清单 + §5.3 checklist）完成 TriMC 仓 scheduler 服务域适配器全链：P1 适配器核心 → P2 CLI → P3 测试 → P4 服务器部署。

**P1 适配器核心（TriMC 仓 src/cron/，只动 TriMC 仓，agent-core 零改动）**

1. `src/cron/week-math.ts` — ISO week 纯函数（toWeek=ISO week of today+1，跨年 wrap 到去年最后一周，W 两位补零），10 测试
2. `src/cron/command-handler.ts` — JobHandler：token 替换 {fromWeek}/{toWeek}/{startDate} → spawn /bin/bash -e → runAs 降权（runuser 模式，对齐 M1 session-bridge；fleet=uid 1001）→ 超时默认 10min 可覆盖（进程组 SIGKILL，detached）→ stdout/stderr 捕获 → per-run 日志 `<jobId>__<ISO>.log`，6 测试
3. `src/cron/service.ts` — createCronService：JobExecutor 装配 + CRUD + stale-run 恢复（start() 重置 runningAtMs≠null）+ degraded 聚合（consecutiveErrors≥3）+ runJob（force 语义对齐 TriLC runJobNow：非 force 拒 disabled、running 恒拒），8 测试
4. `src/cron/routes.ts` — /internal/v1/cron/* 七端点（对齐 TriLC 路由语义：POST 201、list {ok,jobs,count}、run {ok,ran,reason}），9 测试
5. `src/server/app.ts` — cron 路由 if 块（heartbeat 端点后）+ start()/stop() 钩子 + healthz cron 块 {enabled, jobCount, degraded, consecutiveFailures}
6. `src/config/env.ts` — cronEnabled（TRIMC_CRON_ENABLED）/ cronLogDir（TRIMC_CRON_LOG_DIR）
7. `src/cron/index.ts` — barrel + 命名边界声明（§2.1 定案点①）

**P2 CLI**：`src/cli.ts` + package.json bin `trimc -> ./dist/src/cli.js`（tsconfig rootDir "." 布局），子命令 add/list/run/log/status/update/remove + `--plane-shift` 预设（§5.1 模板 + 周日 23:00 Asia/Singapore + runAs fleet）。build 确认进 dist。

**P3 测试与门禁**：新增 36 用例全绿（week-math 10 / command-handler 6 / service 8 / routes 9 / app-cron 3）；`npm run check` clean；`validate.mjs` typeCheck 通过。全量 491 中 488 过、2 失败为 pipeline-integration 既有失败（改动前 HEAD 基线同败，与本树改动无关，登记既有观察项）。

**P4 服务器部署（§5.3 checklist 6 项全完成 + 触发链路实测）**

| # | 项 | 结果 |
| --- | --- | --- |
| 1 | 裸仓写权限 | /srv/git/TriMetaverse.git 已 chgrp fleet + g+w（原 root:root，fleet 不可写） |
| 2 | Python 兼容性 | 3.6.8 实测 SyntaxError（`future feature annotations is not defined`）证实不兼容 → 按 CTO 三级预案定案 A'：`dnf module install python38`（3.8.17，系统 python3.6 未动）→ `--help` + 测试根/真实根 dry 链门禁通过 → 模板解释器固定 python3.8（另装 python3.11 备用） |
| 3 | agent-core 链 | /srv/fleet/TriMC/node_modules/@tricompany/agent-core/dist 可解析 ✓ |
| 4 | fleet git 身份 | uid/gid 1001，无全局 git config（-c 内联身份按方案） |
| 5 | TRIMC_CONFIG_DIR | /var/lib/trimc 已建 + trimc.service drop-in Environment 注入（daemon-reload） |
| 6 | Node engines | engines >=20.0.0 → >=18.20.0（服务器 Node v18.20.8）；新代码 Node 18 API 面自查无禁项 |

- 代码到达服务器：编排层 push 补齐（7ba75db/8c612de/5db873e/579162f/d3fe63e），fleet pull → **npm run build（关键：trimc 为 dist 生产形态 r15-1 ④，非 tsx 直跑）** → systemctl restart trimc → healthz cron 块可见 {enabled:true, jobCount:0}
- 装真 job：`trimc cron add --plane-shift` → nextRunAtMs=1786892400000 = **2026-08-16 周日 23:00 Asia/Singapore 精确**（stagger 0，时区正确）
- 触发链路实测：测试 job（测试根 dry 链命令 + runAs fleet）经 `trimc cron run` 全链路通过——token 替换、runuser 降权、spawn、输出捕获、per-run 日志（/var/lib/trimc/cron/logs/，0600）、log 端点审计、jobs.json 持久化（/var/lib/trimc/cron/jobs.json）全部落位；测试 job 已删
- 真 job 当前状态：weekly-plane-shift（b00b0070）enabled、idle；status {running:true, degraded:false, consecutiveFailures:0, jobCount:1}

**设计文档与 runbook**：`docs/engineering/trimc-scheduler-adapter-design.md`（r1-1 APPROVED，随实现同仓落地：bin 路径、日志 `__` 分隔、python3.8 定案、engines 修正、门禁实测登记）；新增 `docs/ops/trimc-cron-plane-shift-runbook.md`（部署/装 job/回流边界/异常处置）。

## 事故记录（已恢复，透明上报）

验证 pipeline 测试是否为既有失败时，临时 worktree + npm install 后 `git worktree remove --force` 在 Windows 穿透 junction 误删：
1. TriCompany/packages/agent-core 工作区文件 → git restore 完整恢复（该仓其他进行中变更未碰）
2. D:/Code/ai/TriModel 全目录含 .git → 从 sg-server 克隆恢复（HEAD a445b0e 与服务器裸仓/舰队克隆一致）
已重建两仓 node_modules + dist，TriMC check 恢复 clean。残留风险：TriModel 被删前若有未 push 本地提交则丢失（无证据，服务器 HEAD 一致）。预防：baseline 验证改用只读方式。

## 观察项与技术债务

- O-r12-1 pipeline-integration 2 个既有失败（tool-gater tier 断言，HEAD 基线同败）——非本树引入，待归仓路由
- 五段链 dry 模式（无 --sync）整体 exit 1（validate/agent_close 因未写而 fail）——脚本既有行为；测试 job 的 error 记录为预期，真 job --sync 写后应为 exit 0
- cron `0 23 * * 0` 在 agent-core stagger 启发式下返回 0（误判 sub-hourly 豁免）——nextRun 精确 23:00，符合方案意图，r1-3 复核
- /usr/local/sbin/squid-allowlist-sync 继续使用系统 python3.6，本次未动（CTO 预案影响面结论）
- 跟进项（MVP 裁减，r1-1 §8 已登记）：自动重试 backoff 接入、store 迁 PostgreSQL、K8s 多副本 lease

## 小狄审核观察项落实（2026-08-13 后续补记）

1. runbook git pull 执行身份已注明（root pull 或 runuser -u fleet，二选一记录在案）+ 新增 TriMetaverse .git 属主修正步骤——P4 实测发现 root pull 残留 838 个 root 属主文件（index/refs/objects）阻断 fleet 的 git 段，`chown -R fleet:fleet /srv/fleet/TriMetaverse/.git` 修复后实测：fleet `git add` 可写 index（ADD-OK）、`git diff --cached --quiet` no-op 幂等（NOOP-OK）
2. /home/fleet 实测存在（fleet:fleet 700），command-handler 的 HOME=/home/fleet 覆盖有效（runuser 实测 HOME=/home/fleet 一致）

## 遗留

- 真 W33→W34 迁移触发时机（编排层定案）：**不做人工提前点火**——r1-3 验证 PASS 后走自然触发（cron 周日 08-16 23:00 Asia/Singapore 自动首跑）；`trimc cron run b00b0070-2f82-4e7d-a98` 仅作 cron 未触发时的兜底。job 已就绪可触发（nextRunAtMs=2026-08-16T15:00:00Z = 周日 23:00 SGT）
- 本地回流（`git pull sg-server dev`）为运维步骤，编排层执行，调度器不负责（runbook §4 已声明）
- r1-4 收口时回写 TriMC/docs/registry/code-state.md（新模块 src/cron/ + src/cli.ts 登记）

## 使用依据

- r1-1 方案 brief（briefs/r1-1-20260813160943.md）+ 设计文档（TriMC/docs/engineering/trimc-scheduler-adapter-design.md，含 CTO 三级预案与 checklist #6 增补）
- @tricompany/agent-core scheduler API（TriCompany/packages/agent-core/src/index.ts L124-178 导出面）
- TriLC src/cron（行为对标基准：runJobNow force 语义、路由响应形态、healthz cron 块）
- TriCompany/runtime/cognition/weekly_plane_shift.py（CLI 契约：--from/--to/--start-date/--operating-root/--sync）
- 生产级开发期方案（§三写锁、§5.3 归仓路由、§5.4 中改通道）
