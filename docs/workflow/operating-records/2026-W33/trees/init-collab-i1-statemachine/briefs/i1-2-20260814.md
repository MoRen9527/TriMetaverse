# i1-2 实现任务包（小全，FullStackDeveloper）

> 树：init-collab-i1-statemachine（立项周 W33）
> 依据：briefs/i1-1-20260814.md（技术拆解交付，任务包 §六 + 门禁 §七）
> 日期：2026-08-14（编排层派单）
> 状态：active — 第一步 = r19 前置 gate

## 〇、执行顺序（硬序）

1. **r19 前置 gate（第一步，不可跳过）**：复现 → 三级日志采集 → 根因分类 → 最小修复 + 回归断言。完成且通过后方可进入状态机实现。
2. init-chain.ts（链路状态机）
3. init-selfcheck.ts（五探测 + summary + 防重入）
4. app.ts 三处增量 + onboarding.ts prompt 真源措辞一处
5. 单测（转移函数全对 / mid-phase 崩溃恢复 / 原子写 / 五探测 mock / status 端点一致性）
6. 活体冒烟（CEO 机 r19 装后态复验，TRILC_DATA_DIR 显式隔离——r19 教训）

## 一、r19 前置 gate（问周面修复）

- **诊断协议**：CEO 机 r19 装后态复现「TriPilot 问周面」→ daemon 日志三级采集（launcher / daemon / key-cache 401 面）→ 根因分类：
  - **A = 模型链族**（key/token/3333/8008 面）
  - **B = 提示注入族**（buildWeeklyPlaneHint 注入点）
  - **C = TriPilot 渲染族**（SSE 事件消费面）
- **修复归属**：A/B（TriLC 面）→ 随本树修复；C（TriPilot 渲染层）→ 拆 TriPilot 独立树，I1 主线把「第五探测」挂起态标记、其余检查面先行，**不阻塞**。
- **修复断言（门禁）**：问周面冒烟 PASS（第五探测 green）+ r19 已修项回归（EBUSY 单层化、token 兜底）不退化。

## 二、实现要点（详细契约见 i1-1 brief，此处为执行摘要）

1. **init-chain.ts**（新文件，`TriLC/src/company/`）：七态状态机 + 状态文件 `{dataDir}/company/init-chain.json`（schema 见 i1-1 brief §二）+ **真 tmp→rename 原子写** + eventSeq 单调 + 无任何 git 操作 + 断点续跑 load + `transitionTo(next, entry)` 内部函数 + 事件发布封装。
2. **init-selfcheck.ts**（新文件）：五探测（healthz / tripilot / trimodel / tristaciss / plane-hint-probe 第五探测）+ summary 规则（任一 blocked 级 fail → blocked；仅 degraded → degraded；全 ok → pass）+ 并发防重入（运行中再触发 409）。
3. **app.ts 三处增量**（最小 diff）：
   - 端点：`GET /internal/v1/init/chain/status` + `POST /internal/v1/init/selfcheck/run`（防重入 409）
   - 启动：InitChain load + chainState=uninitialized 自动转 selfcheck（记录转移 + 发布事件，不自动执行探测）
   - heartbeat 注册块判定：`isOnboardingPending() || chainState === 'onboarding'`（行为等价扩展）
4. **onboarding.ts**：叙事文本不动；「进度状态规则」真源措辞由「对话历史」→「state.json progress + init:step-event 事件流」（一处，附 diff 说明）。
5. **SSE 事件族**：init:chain-changed / init:selfcheck-started / init:selfcheck-progress / init:selfcheck-finished / init:step-event（复用既有 publish({type}) 通道，事件序号与状态文件 eventSeq 一致）。

## 三、护栏（逐项守，违反即停）

- **6.4 会话初始化器零改动**：session-initializer.ts diff 零行
- **公司态分离**：init-state.ts diff 零行（已知 tmp/rename 缺陷 + REQ-019 隐患只登记观察项，不修）
- **两入口零本地执行**：本树不改 TriPilot / trilc chat 仓入口代码；新端点 localhost-only internal 面
- **密钥纪律**：任何密钥只走 env/config 注入，永不进 git

## 四、落盘要求

- 完成后回写 tree-op.json i1-2 node：status=done、checkpoint={progress（改动清单 + 护栏逐项结果 + r19 gate 结论）、artifactCommit（TriLC 仓 commit hash）、resumePoint（i1-3 验证入口）}
- 产物 commit 卫生：自己 commit（双仓纪律：TriLC 仓 → 裸仓 → 本仓文档登记）；落盘后 commit 或明说「落盘未提交」由编排层补

## 五、时间口径

- I1 目标 08-15 内闭环；本节点（i1-2）建议今天（08-14）内完成 r19 gate + 状态机主体，08-15 上午完成单测 + 冒烟
- 超预期不阻塞不冒险：显式报告编排层，走 §8.2 条款 b 路径（自然触发照常 + 确认后显式触发）
