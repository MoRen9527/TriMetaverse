# r13-3: 合同收敛 10 项口径独立验证 — TestEngineer 小柯

- **nodeId**: r13-3
- **agent**: TestEngineer (小柯)
- **日期**: 2026-08-13
- **状态**: done

## 做了什么

按预告验收顺序（⑩ 链核对 → ②④ 静态 → ①③⑤ 动态 → ⑥⑧⑨ 回归产物）对 M3 首树 10 项口径全部独立重测：golden 防污用 git 历史取回旧 resolver + 旧合同重新生成快照比对；动态层用仓库自身 harness（node --import tsx --test，规避跨仓 ESM 依赖链问题）；30 差异逐条语义审计；O4 用 v0.4.3-r12 真实发布 ZIP 全量 namelist grep。

## 验证矩阵（10 项）

| # | 口径 | 独立验证结果 | 判定 |
| --- | --- | --- | --- |
| ① | golden 等价性零 diff | **golden 防污**：v1-trimc.json 用旧 TriMC resolver（a95b9df~1）+ 旧 registry（f176f34）重生成 11/11 零 diff；v2-trilc.json decisionRights 用旧 TriLC resolver（25439ca~1）+ 旧 source-agents 重生成 14/14 全等。**等价性**：现 TriLC adapter 实载 14 + decisionRights/systemPromptLength vs golden **零 diff**（仓库 harness 实测） | **PASS**（观察 O5） |
| ② | 负路径 1.0/2.0 拒绝 + 迁移指引 | 旧 v2 合同 + 旧 v1 合同（git 历史取回）经 loadContractV3 均 rejected，错误含 `unsupported contract version ... expected "3.0"` + 迁移指引（spec §三） | **PASS** |
| ③ | strict 14/14 | loadContractV3 对 14 份 3.0 合同全部 success（`.strict()` 实读 + 实测） | **PASS** |
| ④ | 死 schema 双清零 | A 层 agent-core src+dist（含 .d.ts）旧 5 符号 + @deprecated **grep 零**；B 层 TriLC `ContractYaml` 零 + TriMC 6 旧解析符号零；三仓 tsc --noEmit **零错误** | **PASS** |
| ⑤ | TriLC 回归 14/14 | 全量 **286 测试 285 pass / 1 fail**（tui/components = ink-testing-library 预存债，与声称精确一致）；实载 14/14 + prompt 非空在 ① 验证中确认 | **PASS** |
| ⑥ | TriMC 切换 + suites | employee-registry 测试含真实 source-agents 断言（`employees.length === 14`）6/6 绿；全量 **455 测试 452 pass / 2 fail（r7 归因预存 tier 断言）/ 1 skip（O3 win32）/ 0 cancelled**——cancelled 14→0 ✓ | **PASS**（差异 D9） |
| ⑦ | 双域字段级一致 | 抽查 3 份（test-engineer/business-strategy/deployment-engineer）11 组字段（agent_id/family/identity 4/paths 6/responsibilities/decision_rights/collaborators 3/tools/io_contract/instructions 存在性/runtime_baseline）双端全等；schema 输出层 paths=6、responsibilities 非空、runtime_baseline 对象存在 | **PASS** |
| ⑧ | O3 W_OK + 负路径 | TriMC session-initializer accessSync(W_OK) 实读（:178）+ 1 skip = win32 平台设计跳过（负路径 Linux/CI 执行），符合声明 | **PASS** |
| ⑨ | O4 发布包零残留 | v0.4.3-r12 ZIP（09:45，Step 1 后构建）内 agent-core 自身 contracts 模块 6 文件（agent-contract/index/resolver 各 .js+.d.ts）**旧符号零命中**；r12 基线 15 文件（3 ts + 12 dist）已清零 | **PASS** |
| ⑩ | 迁移序列可追溯 | Step 1 9cd325b（agent-core 新 schema）→ Step 2 57a6241（14 合同 v3 + 3 缺口）→ Step 3 25439ca（TriLC adapter）→ Step 4 a95b9df（TriMC 迁移 + O3）→ Step 5 2c2ccf5（v1 退役）+ 27b7cd3（负路径 fixture）——commit message 均对应步骤；golden 生成时间 09:39:10/17 早于 Step 1 commit 09:39:46 | **PASS** |

## 30 差异审计（小贾关注点 1）

- **性质结论：全部为 v1→v2 历史演进，零迁移丢失**。逐条审计 13 条非原文命中（含 12 条合并/拆分/改写 + 1 条真丢）：
  - 合并改写：如 ceo freeze 3 条 v1 → v2 合并为 1 条；ceo escalate 2 条 → 1 条；cto approve "技术方案设计（在既有模块边界内）" → "技术方案设计（不改变模块架构边界）"（语义等价改写）
  - 拆分改写：如 cfo forbidden "替代 CPO/CTO 做产品和技术的实现判断" → 拆为 "替代 CPO 做产品范围实现判断" + "替代 CTO 做技术方案实现判断"；cmo/coo 同理
  - 唯一真丢：test-engineer forbidden 的 "替代 BusinessStrategy 做中央战略裁决" 在 **v2 golden 中已缺席**（迁移前既成事实，非 B 段丢失）——小全"v2 为活文档超集"表述对此条不准确（是 drop 非 superset）。建议 r13-4 转 CTO 评估是否回补该条款
- **D8（计数不可复现）**：小全声称"30 处表述差异"——我的两种枚举均不吻合（38 条 v1 非原文条目 / 41 个 diff key 槽）。计数口径不明，但定性结论一致（演进非丢失）

## business-strategy family（小贾关注点 2）

- 现状记录：family="Role"（v2 原值保留，语义为中央 Registry），小全未擅改，已转 CTO 裁决——按口径记录现状，不阻塞判定 ✓

## 差异与观察项汇总

- **D8**（证据精度）："30 处"计数不可复现，见上
- **D9**（证据精度）：TriMC 全量 455/452 vs 声称 454/451（+1 test +1 pass；2 fail/1 skip/cancelled 0 全吻合）——多出 1 个测试待溯源（可能 27b7cd3 后新增）
- **O5**（方法备注）：v2 golden prompt 长度与 f176f34 worktree 重跑有 24~90 差——根因 git worktree 的 autocrlf 换行转换，非快照不实；当前工作树文件与 golden 精确一致（ceo 5837=5837 等）
- **O6**（现状记录）：business-strategy family="Role" 待 CTO 裁决
- **建议**：test-engineer v1 forbidden 的 BusinessStrategy 条款在 v2 演进中被删，建议 CTO 评估回补

## 总体结论

**PASS** — 10 项口径全部独立复核通过，零阻塞缺陷，零迁移丢失。golden 防污、死 schema 双清零、strict/负路径/等价性/双域一致五层证据全部成立；差异 D8/D9 为证据计数精度问题（实质结论不受影响），O5/O6 为方法备注与现状记录。建议 r13-4 收口时：D8/D9 以本简报计数为准登记；test-engineer 条款回补转 CTO；business-strategy family 裁决结果入清单。

## 为什么

- golden 防污不用"信任冻结声明"——用 git 历史取回迁移前代码与合同，重新生成快照与 golden 逐字段比对（v1 11/11、v2 decisionRights 14/14 零 diff）
- 动态层用仓库自身测试 harness 运行真实 adapter 代码（临时测试文件跑完即删，未混入套件计数）
- O4 用真实发布 ZIP 全量 namelist + 内容 grep，且区分了 agent-core 自身 contracts 与 @types/node 的 @deprecated 噪声

## 代码变更摘要

| 文件 | 变更 | 意图 |
| --- | --- | --- |
| briefs/r13-3-20260813102110.md | 本简报 | 10 项口径验证结论与差异明细 |
| trees/r13-contract-convergence/tree-op.json | r13-3 status=done + checkpoint | 交接 r13-4 收口 |
