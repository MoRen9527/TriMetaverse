# r13-2 简报：B 段合同收敛迁移（Step 2-5）

- 执行人：小全（FullStackDeveloper）
- 树：r13-contract-convergence
- 时间：2026-08-13
- 依据：`TriCompany/docs/engineering/agent-contract-v3-spec.md`（CTO 小狄 r13-1）+ golden 基线 + agent-core 9cd325b

---

## Step 2 合同迁移（TriCompany commit 57a6241）

**11 份有 v1 对应者**：迁移工具 `TriMetaverse/scripts/migrate-contract-v3.mjs` 一次性合并（v2 保留 paths/decision_rights/runtime_baseline/family + v1 搬运 identity 去 family/responsibilities/collaborators/tools/io_contract/instructions），commit message 可对应步骤（验收项 10）。

**3 份缺口新写**（以 employee-roster + 五件套为源）：
| 员工 | identity | reports_to | 素材 |
| --- | --- | --- | --- |
| business-strategy | BusinessStrategy（registry 岗位） | CEO | agent-body frontmatter + 核心职责 5 条 |
| customer-success-officer | 小成 | chief-operating-officer | roster + body 6 职责 |
| deployment-engineer | 小布 | chief-technology-officer | roster + body 6 职责 |

**colleagues_social 坑点处理**：迁移时拆分 colleagues + social 两字段（规格标注坑点），11 份合并 + 3 份新写全部生效。

**门禁**：`loadContractV3` 14/14 strict 全绿。

## Step 3 TriLC adapter（TriLC commit 25439ca）

- `loadOne` 解析+校验换 agent-core `loadContractV3`；`ContractYaml` 接口删除（B 层 grep 为零）；colleagues_social 归一化逻辑删除
- 本域保留：目录遍历/watcher/五件套路径组装/roster/system prompt 组装（thin adapter 边界）
- 测试 fixture 迁 v3 + 新增负路径（v2 形状 loadAll=0）
- **门禁**：tsc 零错误；14/14 实载；**golden 等价性零 diff**（vs v2-trilc.json：decisionRights 四键 + systemPromptLength 14/14）；全量 285/286（唯一 fail 预存 ink-testing-library 债）

## Step 4 TriMC 迁移 + O3（TriMC commit a95b9df）

- `resolver.ts` 重写 thin wrapper：loadContractV3 → 本域投影（v1 兼容形状，消费方零改动）；`ContractYamlRaw`/`validateRequiredFields`/`validateFamily`/`validateRiskLevel`/`normalizeResponsibilities`/`normalizeTools` 全删（B 层 grep 为零）；resolveContracts 支持 flat + per-agent 双布局
- `agent-contract.ts`：runtime_baseline 类型改 v3 对象形状（spec 2.4 裁决，无消费方影响）
- `dispatch-proxy`：registryDir 语义 docs/registry → source-agents
- `session-initializer`：合同加载迁 loadContractV3（O2-A 落地）
- **O3**：initializeSession 新增 W_OK 工作目录校验（基准 TriLC session-initializer.ts:44 accessSync W_OK），读只目录负路径 → SessionInitError
- 测试：contract-resolver 改写 v3（14 实载 + v1 负路径全拒）；employee-registry fixture 迁 v3 + **真实 source-agents 14 员工实载**（3 缺口在列）；session-initializer fixture 迁 v3 + O3 负路径（win32 平台 chmod 不映射 W_OK，按设计 skip，Linux/CI 执行）
- **门禁**：tsc 零错误；全量 451/454（2 fail = r7 归因预存 pipeline tier 断言；1 skip = O3 win32）；cancelled 14 → 0
- **拆树阈值监控**：新增失败全部集中在 3 个 contract 链测试文件（改写即绿），改造面远低于 1.5 倍阈值，未触发拆树

## Step 5 v1 退役（TriCompany commit + TriMC 测试适配）

- docs/registry 11 份 v1 `git rm`（内容已并入 3.0，git 历史即归档）
- TriMC 负路径测试改自建 fixture（不再依赖 docs/registry 真实文件）

## 等价性比对结果（如实声明）

| 比对 | 结果 |
| --- | --- |
| v1 搬运字段（identity 4 字段/collaborators.reports_to/tools 名称集/io_contract 长度） | **零 diff（11/11）** |
| decision_rights vs v2 golden（v2-trilc.json） | **零 diff（14/14，TriLC 侧比对）** |
| decision_rights vs v1 golden | 30 处表述差异——**非迁移丢失**：v1→v2 两代合同历史演进（v2 为活文档超集，如 CTO approve 3→4 条、forbidden 2→5 条、多处合并改写），B 段规则"decision_rights 值保留 v2"（spec 3.2） |
| responsibilities vs golden | golden v1 快照不含此字段（r13-1 冻结范围），无法比对，已记录 |

**迁移无损结论**：v1 全部搬运字段零 diff + decision_rights 以 v2 为真值。v1 内容在 3.0 合同中无丢失（超集覆盖人工抽查通过：approve/escalate/forbidden 的 v1 条目均以原文或合并改写形式存在于 v2 值中）。

## 5 步 commit 清单（验收项 10）

| Step | 仓库 | Commit | 内容 |
| --- | --- | --- | --- |
| 2 | TriCompany | `57a6241` | 14 份 v2→v3 + 3 缺口新写 |
| 3 | TriLC | `25439ca` | thin adapter 切换 + 回归 + 等价性 |
| 4 | TriMC | `a95b9df` | resolver 迁移 + registryDir + O3 + 14 员工实载 |
| 5 | TriCompany | `2c2ccf5` | v1 11 份退役 |
| 5 附 | TriMC | `27b7cd3` | 负路径测试改自建 fixture |
| 工具 | TriMetaverse | （收尾 commit） | migrate-contract-v3.mjs + 简报 + tree-op |

## 给 r13-3（小柯）的验证提示

1. **TriLC**：`grep -rn "ContractYaml" src/ test/` 应为零；14/14 实载（`[contract-resolver] loaded 14`）+ prompt 14/14 非空；golden 等价性比对脚本可复跑（brief 已给口径）
2. **TriMC**：`grep -rn "ContractYamlRaw\|validateRequiredFields\|normalizeResponsibilities\|normalizeTools" src/` 应为零；tsc 零错误；全量 451/454 的 2 fail 为预存（subagent tier write_file，r7 归因）；O3 负路径在 win32 是 skip（平台限制），需 Linux 环境或人工确认 accessSync 逻辑（src/onboarding/session-initializer.ts）
3. **等价性**：v1 搬运字段零 diff；decision_rights 比对以 v2 golden 为准（v1 表述演进 30 处已归因，勿按迁移丢失判）
4. **grep 三层**：A 层 agent-core 双清零（r13-1 小狄交付）；B 层 TriLC ContractYaml / TriMC validate+normalize 符号为零（本段已清）；C 层 TriMC agent-contract.ts 类型投影为域内投影（runtime_baseline 已改对象形状），tsc 零错误为准
5. **employees 14 实载**：employee-registry 测试含真实 source-agents 断言（3 缺口在列）

## 技术债务标记

1. **business-strategy family 疑点**：v2 合同 family: "Role"，但其语义是中央 Registry（body 自称 Strategy Registry）。迁移保留 v2 原值（不改语义，等价性优先）。建议 CTO 裁决是否改 "Registry"（涉及 spec 2.2 family 枚举语义）
2. **O3 负路径 win32 盲区**：Windows chmod 不映射 Node W_OK，负路径仅 Linux 可验；TriMC 生产在 Linux 不受影响，本地开发跳过
3. **v1 演进表述差异 30 处**：如实记录于本简报，不需修复（v2 为真源），但若有岗位认为 v1 某条表述更准确，可向 CTO 提合同修订
4. TriMC 本域投影（agent-contract.ts）保留 v1 兼容形状——O2-A 收敛后若 TriMC 编排层愿意改消费形状，可逐步切到 agent-core v3 类型 re-export（C 层允许方向）

## 使用依据

- `TriCompany/docs/engineering/agent-contract-v3-spec.md`（§二 schema、§三映射、§四序列、§五验收 10 项、§五.2 验证细节）
- golden 基线：`trees/r13-contract-convergence/golden/v1-trimc.json` + `v2-trilc.json`（未改动）
- agent-core `9cd325b`（loadContractV3/resolveContractsV3）
- TriLC `src/config/contract-resolver.ts`（r11 状态）、TriMC `src/contracts/resolver.ts`（r11 状态）
