# r13-1 工作简报

- **节点**: r13-1
- **agent**: CTO (小狄)
- **树**: r13-contract-convergence
- **完成时间**: 2026-08-13T21:00:00+08:00

## 做了什么

A 段完整收口：schema v3.0 规格文档 + agent-core 收敛实现 + 单元测试 + Step 0 golden 基线冻结。

1. **规格文档**：`TriCompany/docs/engineering/agent-contract-v3-spec.md`（source-only，含元信息头）——schema v3.0 完整定义、v1→v3 / v2→v3 字段映射表、5 步迁移序列、thin adapter 边界、可检验性 10 项、决策记录。
2. **agent-core 收敛实现**（commit `9cd325b`，TriCompany 本地）：`src/contracts/` 原位重建——`AgentContractV3Schema`（zod `.strict()`、version/type literal `3.0`/`agent-contract`、family 枚举、io_contract inputs/outputs 非空必填）、`loadContractV3`/`resolveContractsV3`/`ContractV3Error`（负路径错误含版本与迁移指引）。旧死形状符号与 @deprecated 双清零。
3. **单元测试**：`test/contract-v3.test.mjs`（node:test，测 dist）8/8 全绿——正例默认值填充、1.0/2.0 版本拒绝、strict 未知字段拒绝、缺 io_contract、坏 family 枚举、批量收集、文件缺失。`npm test` = build + node --test。
4. **Step 0 golden 基线**：`golden/v1-trimc.json`（TriMC 旧 resolver 对 11 份 v1 解析快照）+ `golden/v2-trilc.json`（TriLC 旧 resolver 对 14 份 v2 解析快照）——r13-2/r13-3 等价性比对的唯一基准。
5. **验证**：TriMC / TriLC `tsc --noEmit` 零影响（旧导出删除无消费方）。
6. **登记**：`TriCompany/docs/registry/code-state.md` 收敛决策登记（规格 + 实现 + golden + 后续指向）。

## Schema 设计要点

- **v3.0 = v2 基础 + TriMC 编排字段**：contract（agent_id/family/version/type）+ paths 六文件 + identity + responsibilities + decision_rights 四键 + collaborators + tools + io_contract + instructions（可选）+ runtime_baseline（对象）。
- **无向后兼容分支**：1.0/2.0 输入必须解析失败并指向迁移指引。
- **形状冲突裁决**：`family` 归 contract 下（v2 位置）；`runtime_baseline` 对象形状（v2 现实，TriMC 旧数组期望作废）；`decision_rights` 四键超集 + 默认空数组；`responsibilities` union 保留。
- **默认值填充**：user_invocable=true、decision_rights 四键空数组、peers/supervises 空数组、tools=[]、tools[].scope=[]、requires_approval=false、runtime_equivalent=''。

## B 段执行指引（r13-2 小全，checkpoint 详见 tree-op.json）

- 规格文档 §三（映射表）、§四（迁移序列）为执行依据；每步一个 commit + 测试门禁。
- **重要提醒**：v2 合同 paths 存在 `colleagues_social` 合并字段变体（TriLC 旧 loadOne 有归一化逻辑），迁移 3.0 时须拆成 `colleagues` + `social` 两字段。
- 3 份缺口合同（business-strategy、customer-success-officer、deployment-engineer）的 identity/io_contract 以 employee-roster.json + 对应五件套为源新写。
- O3 同 Step 4 做：TriMC initializeSession 加 W_OK 校验（基准 TriLC `src/company/session-initializer.ts:44`）+ 负路径测试。

## 单元测试结果

```
node --test test/*.test.mjs：tests 8 / pass 8 / fail 0
TriMC tsc --noEmit: OK
TriLC tsc --noEmit: OK
```

## 遗留

- 无。A 段完整收口，B 段待小全执行；拆树触发条件（TriMC 编排层测试改造面超预估 1.5 倍）由 r13-2 观察。
