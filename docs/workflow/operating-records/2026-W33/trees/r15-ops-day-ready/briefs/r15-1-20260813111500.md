# r15-1 简报：③b 联通面 + ④ dist 生产形态（CEO 授权）

- 执行人：小全（FullStackDeveloper）
- 树：r15-ops-day-ready
- 时间：2026-08-13
- 授权：CEO 授权 ③b+④（含 daemon 重启与服务器部署）

---

## ③b 联通面配置（本地）

### 轮次记录（CEO 授权重启）

| 项 | 重启前 | 重启后 |
| --- | --- | --- |
| healthz | ok=true, trimc=degraded, uptime=12205s, version 1.0.0 | ok=true, **trimc=connected**, uptime 增长正常 |
| 进程 | PID 17060, node dist/index.js（RegRun 形态） | PID 14620, Start-Process 独立进程 |
| 子系统 | heartbeat 1 agent / cron 2 jobs / reaper ok | 同上，全部正常 |

**操作序列**：
1. `setx TRIMC_BASE_URL "http://47.245.122.61:8710"`（用户级持久，成功）
2. `POST /shutdown` 优雅停止（`{"ok":true,"message":"shutting down"}`）
3. 重启：bash 后台方式进程意外死亡一次（HUP 环境问题），改用 PowerShell `Start-Process -RedirectStandardOutput` 独立进程成功——**登记：daemon 重启方式规范为 Start-Process，bash & 方式不可靠**
4. 验证 healthz 恢复 + 本地功能正常

### 生效验证（与预期不符，如实记录）

- **预期**：degraded（③a 防火墙未放行）
- **实际**：**connected**——`[trilc:conn] recovered → connected` + `replay: no pending events`。**服务器 47.245.122.61:8710 从本机实际可达**（防火墙/安全组已放行或默认允许），③a 的"未放行"前提不成立
- **双向心跳契约 §3.1 演练前半段完成**：恢复对齐路径真实演练（recovered → connected + replay 幂等空队列）
- degraded 三态已由 2.5（r5 轮）验证过，本次重启前现役 daemon 即 degraded 态（未设 TRIMC_BASE_URL 时的常态），无需重复演练
- 本地功能不受影响：heartbeat/cron/reaper 全正常，pending 队列正常
- 远程 TriMC 心跳端点实测：`{"ok":true,"serverTime":...,"nodeId":"probe-r15-verify","commands":[]}`（响应结构向后兼容）

### 回滚步骤

`reg delete "HKCU\Environment" /v TRIMC_BASE_URL /f`（或 setx 置空）+ 重启 daemon（Start-Process 方式）。

## ④ TriMC 生产形态改造（dist 部署完成）

### 部署物与路径

| 部署物 | 本地路径 | 服务器路径 |
| --- | --- | --- |
| dist 产物 | D:\Code\ai\TriMC\dist\（tsc 构建，tsconfig rootDir="." → dist/src/index.js） | /srv/fleet/TriMC/dist/src/index.js |
| 启动脚本 | — | /usr/local/sbin/trimc-start.sh（已切 dist 形态） |
| 回滚稿 | — | /usr/local/sbin/trimc-start.sh.tsx-bak |
| agent-core 包 | D:\Code\ai\TriMC\node_modules\@tricompany\agent-core | /srv/fleet/TriMC/node_modules/@tricompany/agent-core（真实目录） |
| unit 修改稿 | docs/execution/trimc-service-dist.unit + trimc-service-tsx-rollback.unit | （未使用——实际 unit ExecStart 走 trimc-start.sh 脚本，改脚本而非 unit 本身） |

### 部署过程（两个坑，如实记录）

1. **服务器 agent-core 是失效 symlink**：服务器 git 仓库里 `@tricompany/agent-core` 是指向 `/d/Code/ai/TriCompany/packages/agent-core`（Windows 路径）的符号链接（file: 依赖在 Windows 开发的 git 提交产物），服务器上解析失败 → MODULE_NOT_FOUND。修复：本地 `tar czhf`（跟随链接）打包真实内容 scp 过去替换 symlink
2. **tar 打包 symlink 坑**：首次 `tar czf` 只打包链接本身，需 `-h` 跟随

### 部署结果

- trimc.service：**active**（systemd restart 成功，sudo 免密可用）
- 服务器 healthz：`{"ok":true,"service":"trimc"}`（127.0.0.1:8710 服务器本地 + 公网 probe 均通）
- 心跳端点向后兼容实测 ✓
- **r14-2 心跳接线随 dist 生效**（dist 含 r13-2/r14-2 代码，绕过 git pull blocked）

### 回滚步骤

```bash
sudo cp /usr/local/sbin/trimc-start.sh.tsx-bak /usr/local/sbin/trimc-start.sh
sudo systemctl restart trimc
```

## blocked 清单更新

| 原 # | 项 | 状态变化 |
| --- | --- | --- |
| 1 | push sg-server | **仍 blocked**（服务器代码已 scp 部署，但 git 落后 3+ commits 未 push——git 一致性待解锁） |
| 2 | 服务器 git pull + 重启 | **已部分解锁**（dist 直接部署替代 pull，服务已重启为新代码；git 本身仍落后） |
| 3 | TRIMC_BASE_URL + 防火墙 8710 | **已解锁**（③b 实测连通，防火墙实际放行） |
| 4 | M3 生产形态改造 | **部分完成**（dist 构建+部署已做；git 版本化/CI/包管理记 M3 后续里程碑） |
| 5 | UAC 提权安装 | **仍 blocked**（本地现役安装 26.08.05.1 未升级，contracts 0/0 缺陷仍在现役） |

## 技术债务标记

1. **TriMC package.json start 脚本与产物不符**：`"start": "node dist/index.js"` 但 tsconfig rootDir="." 产物在 dist/src/index.js——start 脚本实际失效，需修（rootDir 改 src 或 start 改路径）
2. **服务器 file: 依赖 symlink 问题**：Windows 开发环境 git 提交的 file: 依赖链接在服务器失效——服务器部署需 tar -h 打包真实内容（本次已修 agent-core；TriModel/trimodel 同类 symlink 风险待查）
3. **daemon 重启方式**：bash 后台 & 不可靠（HUP 杀进程），规范为 PowerShell Start-Process 独立进程
4. 现役安装 contracts 0/0 缺陷仍在（提权安装 blocked #5 未执行；r12-1 产物 v0.4.4 已就绪待装）
5. 服务器 git 与部署物漂移：scp 部署后服务器 dist 新于 git HEAD，下次 pull 前需先 push 对齐

## Commit

- TriMC：`chore(r15-1)` package-lock 同步
- TriMetaverse：本简报 + tree-op + unit 稿（收尾 commit）

## 给 r15-2 的验证提示

1. ③b：本机 healthz trimc=connected + `[trilc:conn] recovered → connected` 日志（r15-daemon-restart2.log）
2. ④：ssh sg-ecs-server `systemctl is-active trimc` = active + 服务器 healthz ok + `/tmp/trimc-run.log` 无新 ERR_MODULE_NOT_FOUND
3. 心跳双向：本地 TriLC 心跳持续到服务器（服务器心跳表内存态，可用 30s+ 扫描无 stale 告警间接验证）
4. 回滚稿就位：trimc-start.sh.tsx-bak + reg delete 命令
