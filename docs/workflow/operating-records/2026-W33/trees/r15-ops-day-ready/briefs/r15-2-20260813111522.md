# r15-2: ③b connected 现役事实 + ④ 服务器 dist 部署独立验证 — TestEngineer 小柯

- **nodeId**: r15-2
- **agent**: TestEngineer (小柯)
- **日期**: 2026-08-13
- **状态**: done

## 做了什么

按派单口径（③b 以现役 connected 事实为准）独立重测：本地 env.ts 读取路径实读 + 现役 8711 healthz/连接视图独立确认；服务器面全部 ssh BatchMode 独立实测（服务态/进程形态/symlink/回滚稿/healthz/心跳端点）；8710 本地可达性独立测试；坑记录复核。

## 验证矩阵

| 项 | 声称 | 独立验证结果 | 判定 |
| --- | --- | --- | --- |
| ③b TRIMC_BASE_URL 生效 | setx 设置 → daemon 重启 → connected | env.ts:91 实读 `process.env.TRIMC_BASE_URL ?? 'http://127.0.0.1:8710'` ✓；用户环境变量实测 = `http://47.245.122.61:8710` ✓；现役 8711 healthz **trimc=connected**（此前 degraded → connected 转变即联通面生效直接证据）✓ | **PASS** |
| ③b 服务健康 | 重启后无回归 | 8711 healthz ok:true（uptime 529s+ 持续增长，重启后稳定）；cron 2 jobs / sessionReaper / heartbeat agentCount 均正常 | **PASS** |
| ④ 服务器服务态 | systemctl restart → active + healthz ok | ssh 实测：trimc.service **active**；服务器 8710 healthz `{"ok":true,"service":"trimc"}` | **PASS** |
| ④ 进程形态 | node dist 替代 npx tsx 直跑 | ps 实测：**`node dist/src/index.js`**（非 tsx src）✓；unit 文件 ExecStart=/usr/local/sbin/trimc-start.sh + Restart=on-failure ✓ | **PASS** |
| ④ symlink 修复 | 服务器 agent-core 失效 symlink → tar -h 替换为真实目录 | ls -ld 实测：`drwxr-xr-x ... /srv/fleet/TriMC/node_modules/@tricompany/agent-core` **真实目录** ✓ | **PASS** |
| ④ 回滚稿 | tsx-bak 就位 + 回滚命令可执行 | `/usr/local/sbin/trimc-start.sh.tsx-bak` 存在（218B）✓；start.sh 头部回滚命令（cp tsx-bak → restart）语法与路径可执行 ✓ | **PASS** |
| 双向契约现况 | 服务器心跳端点与本地 connected 对应（§3.1 前半段现役完成） | 本地 POST 服务器 `/internal/v1/heartbeat` → `{"ok":true,"serverTime":...,"nodeId":...,"commands":[]}` 契约响应形状 ✓——r14-2 接线随 dist 部署已现役生效 | **PASS** |
| blocked #3 解锁 | 8710 本地可达 | curl http://47.245.122.61:8710/healthz 本地直达 `{"ok":true}` ✓ | **PASS** |
| 坑记录复核 | HUP 死亡 / symlink 失效 | r15-1 简报三处坑记录准确：bash & 起 daemon HUP 死亡→Start-Process 规范 ✓（与我 nohup 隔离实例经验一致）；服务器 file: 依赖 symlink 失效 + tar 需 -h ✓；TriModel/trimodel 同类 symlink 风险已如实登记待查 ✓ | **PASS** |

## 差异与观察项

- 无阻塞差异。两点附带记录：
- **O8（版本观察）**：现役 daemon healthz version 仍为 **1.0.0**——联通面③b 已启用但 v0.4.4 本地安装（blocked #5 UAC）尚未执行，现役仍是旧安装（ARP 26.08.05.1 时代产物）。运营日 UAC 安装后 version 应升 0.4.4，届时可随 blocked 执行复验
- **服务器 healthz 字段精简**：服务器侧 healthz 仅返回 `{ok, service}`（本地 TriLC 侧有完整字段集）——属服务端既有实现差异，非本树引入，记录备查

## 总体结论

**PASS** — ③b connected 现役事实与 ④ 服务器 dist 部署全部独立复核通过：联通面真实生效（degraded→connected）、服务器已切生产形态（node dist + 真实依赖目录 + 回滚稿就位）、双向契约前半段现役完成、blocked #3 解锁独立确认。无阻塞差异。建议 r15-3 收口时：blocked 清单更新（#3 已解锁、#2 部分解锁、#4 部分完成）；O8 随 #5 UAC 执行后复验。

## 为什么

- 服务器面全部用 ssh BatchMode + curl 独立实测，不采信部署声明（服务态/进程形态/依赖目录/回滚稿四层各自独立取证）
- 现役事实以 healthz 实时字段 + 环境变量注册表双源核对（uptime 增长佐证重启时间线）

## 代码变更摘要

| 文件 | 变更 | 意图 |
| --- | --- | --- |
| briefs/r15-2-20260813111522.md | 本简报 | 验证结论与观察 |
| trees/r15-ops-day-ready/tree-op.json | r15-2 status=done + checkpoint | 交接 r15-3 收口 |
