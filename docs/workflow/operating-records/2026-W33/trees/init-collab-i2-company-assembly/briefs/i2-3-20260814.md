# i2-3 独立验证（小柯，TestEngineer）

> 树：init-collab-i2-company-assembly（立项周 W33）
> 依据：briefs/i2-1-20260814.md §九 门禁 9 条 + §十 解冻面口径 + i2-2 checkpoint（实现方结论 + 验证配方 + 契约口径注记 A/B/C）
> 日期：2026-08-14（编排层派单）
> 状态：active — 依赖 i2-2（TriLC 02e17e8 + TriPilot 350bcb4，三仓已补推）

## 验证范围（门禁 9 条，i2-1 brief §九）

1. **6.4 零改动延续**：TriLC session-initializer.ts 与 TriMC 同源文件 diff 零行（init 模式 = 新增分支）
2. **公司态分离**：init-state.ts diff 零行（save() 机制沿用调用不算改动）
3. **两入口零本地执行**：TriPilot / trilc chat 无文件写、无装配/探测执行——只渲染 + 指令
4. **叙事态同 release 下线**：注册块 / auto-resume / onboarding.ts 三处全移除，无并存路径
5. **I1 前置项③**：ENOENT vs 解析错区分 + .corrupt 备份 + 单测三件套
6. **契约修正⑧**：detail 用实际错误串（lastFetchError 截 120）
7. **装配端点**：白名单枚举 + 校验先行 + 回滚注入测试 + 幂等重试 + 409/422 门禁 + 事件发布与状态帧一致
8. **I1 回归**：全量基线（317/318 → 本树后 337/338，1 fail = components.test.ts 环境缺口既有）
9. **产品断言**：A1 阶段卡可见 / A2 完成判据 = CEO 名 + 员工名单 / A3 断点续跑不重复提问可回看 / A4 0 人拦截 + 默认勾选 5 岗（D1 常量）；I4 实测

## 契约口径注记 A/B/C（i2-2 checkpoint，i2-4 终审复核，验证时按落地形态判定）

- **A（小狄 A' 已裁决对齐）**：selfcheck 完成自动推进 onboarding——小全按候选 A 落地（assemble 受理 onboarding 直行 + selfcheck 已完结 pass|degraded 提交段先补转移；advancedFromSelfcheck 标记）。验证：该路径活体走通 + 422 门禁（selfcheck 未完结时）仍然生效。
- **B**：business-state.md / AGENTS.md 既有真实内容不覆盖（缺失才写占位，响应 preserved 报告）。验证：白名单产物核查含 preserved 语义。
- **C**：loadEmployeeRoster 路径候选扩展（TriCompany 根 docs/registry 真源）。验证：role-catalog 13 岗正常（503 不出现）。

## 验证方法（独立重测，不沿用实现方自测）

1. **单测独立重跑**：`node --import tsx --test test/init-assemble.test.ts test/init-chain.test.ts test/init-selfcheck.test.ts test/contract-resolver.test.ts`（预期 42 用例全绿）+ `npm test` 全量（预期 337/338）
2. **活体验证**（独立隔离目录 + 端口 8727+，不复用实现方 8726 残留）：i2-2 resumePoint 配方全链（chain/status → role-catalog → progress → assemble 校验矩阵 400/422 → selfcheck → assemble 200 → 白名单产物 → 重入 422 → SSE 事件族 → init 模式 bootstrap）
3. **diff 级**：TriLC `git diff 186296a..02e17e8`（14 files）+ TriPilot `git diff 7057a4d..350bcb4`（3 files）护栏面复核
4. **I4 走查**（3 次真实含 1 次两入口交替）——**需 CEO 机装后态 r20 就绪**；若 r20 未装，走查记「待 r20」阻塞项上报编排层，不静默
5. **解冻面**：chat 选人 / 公司开张交互面完整回归（解冻 ≠ 豁免验证）

## 落盘要求（完成后）

1. 回写 tree-op.json i2-3 node：status=done、checkpoint（逐项门禁结果表 + 独立重测数据 + I4 走查结果/阻塞项）
2. commit 卫生：自己 commit 或明说「落盘未提交」由编排层补
3. 最终回复格式：门禁 9 条逐项结果（PASS/FAIL + 证据）｜独立复跑数据｜A/B/C 注记复核意见｜I4 走查结果｜差异点
