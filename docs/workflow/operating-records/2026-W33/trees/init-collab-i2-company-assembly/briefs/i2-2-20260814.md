# i2-2 实现任务包（小全，FullStackDeveloper）

> 树：init-collab-i2-company-assembly（立项周 W33）
> 依据：briefs/i2-1-20260814.md（技术拆解，任务包 §八 12 项 + 门禁 §九 + 解冻面 §十）
> 日期：2026-08-14（编排层派单）
> 状态：active — D1 已闭环，按 12 项任务包执行

## 〇、D1 决策已闭环（2026-08-14，CPO 小乔确认）

**默认勾选 5 岗固化（DEFAULT_SELECTED_ROLES 常量）**：
- ceo-chief-of-staff / full-stack-developer / chief-administrative-officer / chief-human-resources-officer + **chief-technology-officer**（第 5 岗，playbook 1.2 表 4 员工岗 + CTO 补位）
- 决策记录：4 员工岗覆盖治理（CAO/CHO）+ 协调（总助）+ 执行（全栈），技术负责人缺位则技术交付链走不全；当前主线 = 初始化到协同技术交付链（CTO 职责域）；岗位取自标准目录不发明新岗；默认值仅影响初始勾选（≥1 岗可开张，<5 提示）
- A4「默认勾选 5 岗」断言据此落地，无悬空

## 一、执行顺序（12 项任务包，按 i2-1 brief §八）

1. **D1 常量固化**（第一步，已完成确认）：DEFAULT_SELECTED_ROLES 常量。
2. contract-resolver 增 `getRoleCatalog()` 只读访问器 + 单测（identity 面解析 + resolver 未初始化/roster 缺失 → 503 不开天窗）。
3. init-chain.ts load() 前置项③（§六：ENOENT 静默默认帧 vs 解析错 .corrupt 备份 + console.error + 默认帧）+ 单测三件套。
4. init-selfcheck.ts detail 硬编码「fetch 401」→ `ks.lastFetchError` 实际错误串（截断 120 字符）+ 断言更新。
5. **新模块 init-assemble.ts**：校验 schema（≥1 岗/去重/名字必填/阶段门禁 422/防重入 409）+ 落点白名单（枚举，逃逸拒绝）+ 预写 tmp→rename + .bak 回滚 + 幂等重试 + 事件发布（§一）+ role-catalog / onboarding state-progress 端点逻辑。
6. app.ts 增量：① GET role-catalog ② POST assemble ③ GET init/events（SSE 转发 localbus init:*，无重放缓冲）④ onboarding/state + progress 两端点 ⑤ 删除叙事 agent 注册块（app.ts:2926-2939）⑥ tasks/submit init 模式路由判定（§三）。
7. cli.ts/tui：init 模式文本化流程（编号多选 + 命名问答 + 汇总确认 + assemble 提交）+ 删除 hb_company-onboarding auto-resume 分支（cli.ts:520-548）。
8. 删除 onboarding.ts（叙事态同 release 下线三处全部移除，无并存期）。
9. **TriPilot 仓**：webview init 阶段卡（chain/status 首拉 + init/events SSE 订阅复用 trilcDirect/sseParser + role-catalog + onboarding/state + assemble 指令；呈现层组合规则：blocked 行置顶 + trimodel blocked 且 plane-hint ok 时注记「问周面冒烟绿 ≠ 模型链可用」）——零本地执行。
10. 单测：装配校验矩阵、白名单逃逸拒绝、回滚（注入失败点）、幂等重试（state 成功 transition 失败路径）、防重入 409、阶段门禁 422、事件序与状态帧一致。
11. 活体冒烟：CEO 机装后态（TRILC_DATA_DIR 显式隔离，r19 教训）。
12. Registry 回写：TriLC/docs/registry/code-state.md 补 I1 + I2 条目（I1 未回写缺口，本树收尾补齐）。

## 二、护栏（违反即停，报告编排层）

- **6.4 零改动延续**：TriLC session-initializer.ts 与 TriMC 同源文件 diff 零行（init 模式 = 新增路由分支）
- **公司态分离**：init-state.ts diff 零行（save() 机制沿用调用不算改动；假原子写缺陷观察项不动）
- **两入口零本地执行**：TriPilot / trilc chat 无文件写、无装配/探测执行——只渲染 + 指令
- **密钥纪律**：密钥只走 env/config 注入，永不进 git
- **白名单纪律**：装配落点路径白名单枚举，任何逃逸直接拒绝执行

## 三、环境提示

- TriLC HEAD=186296a（I1 交付），TriPilot HEAD=ea3936f（工作树干净）
- 测试基线：全量 317/318（1 fail = components.test.ts ink-testing-library 环境缺口，r19 基线既有，非本树引入）
- 契约真源：i2-1 brief（装配端点契约 §一 / 载荷 schema §二 / init 模式路由 §三 / 渲染落点 §四 / 下线判定 §五 / 前置项③ §六 / 契约修正 §七）
- I4 走查（3 次含两入口交替）= i2-3 动作，本节点只需活体冒烟就绪

## 四、落盘要求（完成后）

1. 回写 tree-op.json i2-2 node：status=done、checkpoint={progress（改动清单 + 护栏逐项 + 12 项完成状态）、artifactCommit（TriLC + TriPilot 仓 commit hash）、resumePoint（i2-3 验证入口）}
2. commit 卫生：TriLC/TriPilot 仓自己 commit + push 裸仓；本仓登记自己 commit 或明说「落盘未提交」由编排层补
3. 最终回复格式：D1 常量固化位置｜12 项完成状态表｜护栏逐项｜单测/冒烟结果｜artifactCommit（两仓）

## 五、时间口径

- 08-14/15 内完成实现（跨两仓：TriLC daemon 增量 + TriPilot 渲染），08-16 上午 i2-3 验证 + i2-4 终审
- 超预期不阻塞不冒险：显式报告编排层，走 §8.2 条款 b 路径（自然触发照常 + 确认后显式触发）
